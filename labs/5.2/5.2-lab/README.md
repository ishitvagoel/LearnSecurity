# Lab 5.2

Authorized: this directory only.

Confidentiality of a note at rest is not 'we base64ed it.' Encoding is not encryption.

pytest tests/test_property.py --impl vulnerable (must fail) then --impl fixed.

Forbidden: protect() is reversible as base64 to 'secret'.
