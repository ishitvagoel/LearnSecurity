# Lab 7.3

Authorized: this directory only.

Webhook bodies without a valid lab HMAC are rejected. TLS to the peer is not authenticity of this callback.

pytest tests/test_property.py --impl vulnerable (must fail) then --impl fixed.

Forbidden: unsigned webhook accepted. Uses stdlib HMAC locally.
