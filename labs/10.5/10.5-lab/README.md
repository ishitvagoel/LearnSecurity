# Lab 10.5

Authorized: this directory only. No live targets.

An incident cannot close until recovery is marked done and logs do not contain note bodies.

pytest tests/test_property.py --impl vulnerable (must fail) then --impl fixed.

Forbidden: closing an incident with recovery still todo.
