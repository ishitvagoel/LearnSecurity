# Lab 6.2

Authorized: this directory only.

Note HTML encoding must treat '<' as data. CSP3 (Working Draft) is not a substitute for encoding.

pytest tests/test_property.py --impl vulnerable (must fail) then --impl fixed.

Forbidden: raw <img in HTML. Teaching encode, not an exploit kit.
