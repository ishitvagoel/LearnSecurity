# Lab 8.5

Authorized: this directory only. No live targets.

A crash report must not include the note body. Mobile privacy is a 1.1 privacy cell, not a policy PDF.

pytest tests/test_property.py --impl vulnerable (must fail) then --impl fixed.

Forbidden: crash JSON contains the note body.
