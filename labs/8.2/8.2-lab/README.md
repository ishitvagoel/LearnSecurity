# Lab 8.2

Authorized: this directory only. No live targets.

An offline-cached note body must not sit as plaintext on the lab disk map. Encoding or a world-readable prefs file is not confidentiality.

pytest tests/test_property.py --impl vulnerable (must fail) then --impl fixed.

Forbidden: DISK still holds the literal body 'secret'.
