# Lab: 0.1-orientation

**Invariant:** `example.com` is not an authorized target. Local lab hosts are. Scope is a **property**, not a disclaimer footer.

**Root cause class:** trust / authorization of the *tester*.

Run `--impl vulnerable` (public host allowed — forbidden) then `--impl fixed`.
