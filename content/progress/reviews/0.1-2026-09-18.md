# 0.1 — independent review, 2026-09-18

**Reviewed commit:** `aca2ba58` (`aca2ba5`) on `deepen/0.1`, "Deepen module 0.1: five teaching claims, upgraded lab, item bank, honest metadata"
**Reviewer:** independent quality + lab-safety review (read-only agent session)
**Independence statement:** I did not author or revise any file in this review's scope. I had no prior context on module 0.1 before this session; every judgement below comes from reading the files at the commit above, from running the commands recorded in §Lab, and from live network fetches recorded in §Standards. I temporarily mutated `labs/0.1/0.1-orientation/fixed/scope.py` three times (a regex-search fake, a `try/except`-removed variant, a hard-coded URL-table fake, and an `endswith` fake) to test claims by construction, and restored it with the original content each time; `git status --porcelain` is clean and the fixed pair passes 7/7 again. The only file I have written under `content/` is this artifact.

## Files inspected

Read in full:

- `content/modules/0/0.1/spec.md`
- `content/modules/0/0.1/module.yaml`
- `content/modules/0/0.1/lessons/01-property.md` through `08-review.md` (all eight)
- `content/modules/0/0.1/assessment/items.md`
- `content/modules/0/0.1/assessment/rubric.md`
- `content/assessment/keys/0.1.md`
- `labs/0.1/0.1-orientation/README.md`
- `labs/0.1/0.1-orientation/conftest.py`
- `labs/0.1/0.1-orientation/vulnerable/scope.py`, `vulnerable/SECURITY.md`
- `labs/0.1/0.1-orientation/fixed/scope.py`, `fixed/SECURITY.md`
- `labs/0.1/0.1-orientation/tests/test_scope.py`
- `content/standards/pins.yaml` (entries `nist-csf-2.0`, `owasp-wstg-4.2`, `nist-sp-800-181r1-nice`, `wcag-2.2`, plus the diff against the pre-deepen commit for all four)
- `content/progress/STATUS.yaml` (0.1 and 1.4 entries, for shape)
- `content/modules/1/1.4/module.yaml` (governance-field shape: `reviewer`, `lastReviewedAt`, `nextReviewAt`)
- `.cursor/rules/lesson-prose.mdc`, `.cursor/skills/quality-gate/references/publishability.md`, `.cursor/skills/deepen-module/assets/review-artifact.md`
- `scripts/lint_content.py` (`body_words`, L006/L007/L009 implementations)
- `site/lib/contentRoot.ts`, `site/lib/loadCurriculum.ts` (key-isolation enforcement)

Read as precedent for review rigor: `content/progress/reviews/1.4-2026-09-16.md` in full (both the initial review and its addendum).

Read for corroboration: `git show 85c4cc3:content/modules/0/0.1/module.yaml`, `git show 85c4cc3:labs/0.1/0.1-orientation/fixed/scope.py`, `git show 85c4cc3:content/standards/pins.yaml`, `git diff 85c4cc3 aca2ba5 -- content/standards/pins.yaml`, `scripts/lint_baseline.json` (old vs. new).

## Claim-by-claim verification

**1. Five teaching claims, coverage contract.** Verified. `spec.md`'s teaching-claims table cites `items.md` #1 (C1), #2 (C2), #3/#5 (C3), #4 (C5), #6 (C4); the coverage contract's Assessment-item column cites the same six rows against the same item numbers. I checked every cell against `items.md`'s own `Claim assessed:` tags directly (not against the author's script): item 1→C1, item 2→C2, item 3→C3, item 4→C5, item 5→C3, item 6→C4, item 7→C1–C5. **All match exactly; no transposition remains.** All six `module.yaml` `outcomes` have a non-empty coverage-contract row, one-to-one, in order. C1, C3, and C5 (three of five, above the two-claim minimum) carry real lab assertions; C2 and C4 are honestly declared non-code-testable with a specific, correct reason each (C2: no input channel for "a standard was cited"; C4: the fixture never fetches, so it has no body to capture).

**2. The lab bug.** Verified by construction, not by reading the diff. `git show 85c4cc3:labs/0.1/0.1-orientation/fixed/scope.py` has no `try`/`except`; running it directly against `"http://[::1/path"` raises `ValueError: Invalid IPv6 URL` uncaught, and against `12345` raises `AttributeError: 'int' object has no attribute 'decode'` uncaught — both propagate straight out of `target_is_authorized`, exactly as claimed, and exactly contradicting `lessons/04-build.md`'s stated rule ("if the URL cannot be parsed, deny"). The current `fixed/scope.py` wraps the parse in `try/except (ValueError, AttributeError, TypeError): return False` and both inputs now return `False`. I additionally removed just the `try/except` from the current file (keeping the rest) and reran the suite: exactly one test fails, `test_malformed_and_non_string_url_fails_closed` — the fix is precisely targeted, not a hedge that happens to also change something else.

**3. Lab test matrix.** Run from `/home/user/LearnSecurity/.claude/worktrees/agent-a9bc632b02fefef20` at `aca2ba5`, clean tree:

```
$ python3 -m pytest labs/0.1/0.1-orientation/tests --impl vulnerable -v
test_localhost_lab_is_in_scope PASSED
test_named_lab_domain_is_in_scope PASSED
test_public_host_is_out_of_scope FAILED
test_lookalike_hosts_are_not_authorized FAILED
test_malformed_and_non_string_url_fails_closed FAILED
test_case_and_scheme_do_not_change_the_public_verdict FAILED
test_anti_fake_generalizes_beyond_the_three_literal_example_urls PASSED
========================= 4 failed, 3 passed in 0.04s ==========================

$ python3 -m pytest labs/0.1/0.1-orientation/tests --impl fixed -v
======================= 7 passed in 0.02s =======================
```

Matches the claimed 4/7 fail (vulnerable) and 7/7 pass (fixed) exactly. Every vulnerable failure is a plain `AssertionError` on the security property (`assert True is False`), never a `KeyError`/crash — the vulnerable file fails for the stated reason, not by accident.

**Anti-fake verification, three constructions, not two.** I built the two fakes the author claims to have hand-verified, plus a third of my own:
- Hard-coded URL table (`url in {two literal URLs}`): 6/7, fails only `test_anti_fake_generalizes_beyond_the_three_literal_example_urls`.
- `host.endswith(allowed)` in place of exact membership (parse-failure handling kept): 6/7, fails only `test_lookalike_hosts_are_not_authorized`.
- My own, independently constructed: a regex `re.search` for an allowed name anywhere in the URL string, wrapped in `try/except TypeError` (a plausible "looks safe because it fails closed" fake a careless reviewer might accept): 5/7, fails `test_lookalike_hosts_are_not_authorized` (substring match) **and** `test_anti_fake_generalizes...` (case-sensitivity gap on `LOCALHOST`). Caught, for reasons beyond what either of the author's two fakes exercises.

Each mutation was restored from a scratch backup immediately after use; `git status --porcelain` was empty after every restoration.

**4. Coverage-contract / teaching-claims citation cross-check.** Performed independently (not via the author's script). See item 1 above — zero mismatches found, in either table, cell by cell.

**5. Standards spot-checks.** Two live-fetched independently, as required, plus corroboration on the other two via `pins.yaml`'s own diff:
- **NIST CSF 2.0** (`https://www.nist.gov/cyberframework`, fetched live): page confirms CSF 2.0 is current, states its purpose is "to reduce cybersecurity risk," and I found no language framing CSF as testing authorization. Matches `01-property.md`'s and the pin's characterization.
- **OWASP WSTG 4.2**: the citation's *content* is accurate — a live fetch of `https://owasp.org/www-project-web-security-testing-guide/` confirms v4.2 is the current stable release, v5.0 is "currently developing," and the guide is a testing-methodology catalogue with no scope/authorization language. **But the stored canonical `url` is dead** — see Blocker 1 below.
- **NIST SP 800-181r1 / WCAG 2.2**: not independently re-fetched by me beyond a `curl` reachability check (both 200); their pin notes, added in this same pass, describe fetches consistent with the module's claims and I found no contradiction reading them against the lesson prose.

**6. Coverage contract (module.yaml outcomes).** Verified: six outcomes, six coverage-contract rows, one-to-one, no empty cells; ≥2 claims lab-tested (three: C1, C3, C5).

**7. Mechanical gates.**

```
$ python3 scripts/lint_content.py content/modules/0/0.1 --no-baseline
0 error(s), 0 warning(s)

$ npm --prefix site run build
...
✓ Generating static pages using 3 workers (643/643) in 12.9s
```

Both match the claim exactly, including the 643-page count.

**Lint baseline delta**, computed directly from `scripts/lint_baseline.json` before and after (not from the changelog's assertion): total findings 2319 → 2287, a change of exactly **32 deletions, 0 insertions** — every changed row moved from count 1 to count 0 across `L001`, `L002`, `L003`, `L005`, `L013`, `L016`, `L017`, all under `content/modules/0/0.1/`. Matches the claim exactly.

**estimatedMinutes.** Recomputed the lesson-word half of the formula using the linter's own `body_words()` function (excludes code fences and other non-prose per its own rules) on all eight lessons: 1389+948+965+972+934+973+1070+1011 = **8262**, matching `spec.md`'s stated word count exactly, not approximately. Full formula: 8262/200 ≈ 41.3 + 45 (Tier-1 lab) + 7×12=84 (items) + 60 (transfer) = 230.3 → rounds to 240. `estimatedMinutes: 240` is the formula's exact output.

**8. Lab safety.** `grep`ed the entire lab tree for `socket`, `subprocess`, `requests`, `urllib.request`, `http.client`, `os.system`, `open(` — none found; the only import in either implementation is `urllib.parse.urlparse`, a pure parser. Both `SECURITY.md` files state "authorized directory only" and forbid fetching `example.com`. The transfer lesson (`07-transfer.md`) uses `staging-wp.acme-customer.example`, in the IANA-reserved `.example` TLD (RFC 2606) — no real third-party host is named anywhere as a live target. `target_is_authorized("http://[::1]:8000/")` (IPv6 loopback) correctly returns `False`, matching the residual named in `spec.md` ("fails closed, not falsely accepted"). No secrets of any kind appear in the fixture. **Lab-safety review: passes, no findings.**

## Metadata honesty (the D12-class check)

Verified by diff against `85c4cc3`: `module.yaml` previously carried `reviewer: pending independent quality and lab-safety review` alongside `lastReviewedAt: '2026-08-24'` and `nextReviewAt: '2027-02-24'` — real dates with no review artifact under `content/progress/reviews/` to back them, the same D12 pattern the changelog says was found in 1.4/2.1/4.3. The current commit correctly reverts both to `null` while leaving `reviewer: pending...` and `status: draft` untouched. This pass did not touch `depth`, `quality`, or set `status: published` — correct, since none of those are an authoring pass's to set.

## Blockers

1. **`content/standards/pins.yaml`'s `owasp-wstg-4.2` entry — and its mirror in `content/modules/0/0.1/module.yaml`'s `standardsRefs[].url` — cite a dead canonical URL, contradicting this pass's own claim to have verified it.** The stored `url:` is `https://owasp.org/www-project-web-security-testing-guide/v42/`; I confirmed by direct fetch (`curl`, and independently via `WebFetch`) that this URL returns **HTTP 404**. The correct, live, current canonical URL is `https://wstg.owasp.org/v4.2/` (HTTP 200), confirmed by live fetch to be the actual v4.2-stable page the project's own landing page links to. `spec.md`'s standards section and `module.yaml`'s changelog both state that this pass "live-fetched and re-verified... against their canonical pages on 2026-09-18," and the pin's own note says the pass "live-fetched the project landing page" — a *different* URL from the one stored as this citation's canonical `url`. In other words: the page that was actually fetched is not the page recorded as the citation, and the page recorded as the citation does not resolve. The *content* claim (WSTG 4.2 is current, v5.0 is in development, WSTG is silent on authorization) is correct — I verified it independently against the live landing page — but the specific, checkable claim "verified against their canonical pages" is false for one of the four standards as currently written. **This is pre-existing** (the same dead `/v42/` URL is present in `pins.yaml` since at least `85c4cc3`, and is shared with modules 9.3/9.5) **but this pass had the opportunity to catch it while live-fetching WSTG and did not.** **To close:** update the `url:` field in both `pins.yaml`'s `owasp-wstg-4.2` entry and `module.yaml`'s WSTG `standardsRefs` entry to `https://wstg.owasp.org/v4.2/`, and note in the pin that the previous `/www-project-web-security-testing-guide/v42/` link is dead. This is a one-line, mechanical fix in each file; it requires no change to any lesson's argument, the lab, or the assessment bank.

Improvements, not blockers, recorded so they are not lost:

- **`01-property.md` L57 references two other modules by title, with no Markdown link**, in violation of `lesson-prose.mdc` §Cross-references ("Every reference to another module is a titled Markdown link... No bare `(2.3)`. No `(later)` without a named destination"). Both named titles are real and correctly attributed (`1.1 Security as invariants under attack`, `9.5 Penetration testing, reporting, and remediation`), so the destination is real and correctly named, but a linkable path is missing. `scripts/lint_content.py`'s L006/L007 cannot see this — it only matches a bare `(2.3)`-shaped parenthetical or the literal word "later" inside parentheses, not a spelled-out title in running prose — which is the same linter blind spot noted in the 1.4 review.
- **`spec.md`'s "Threat-model prompts" and `module.yaml`'s `threatModelPrompts` are not the same four prompts.** Both lead with "What can a tired learner paste into a proxy?" and both close with a "who benefits from a silent parse failure" question, but `spec.md`'s second prompt ("What written artifact would make a company staging URL in-scope?") has no counterpart in `module.yaml`, whose second and third prompts (`/etc/hosts` aliasing and a redirect leaving `127.0.0.1`) have no counterpart in `spec.md`. Not a blocker — these are two different, both-legitimate lists, not a citation of a fact that has to match — but worth aligning since both lists are meant to describe the same module.

## Verdict

- [x] `quality: competent` — **withheld, pending Blocker 1.** Scored against `publishability.md`: every dimension I evaluated is ≥2 and most score 3 — the property is falsifiable and system-specific (`01-property.md`), the attacker/trust model predicts a non-obvious failure (`02-model.md`'s `endswith` trap), causal explanation separates root cause/precondition/impact for both C3 and C5 diagnosis items, the lab genuinely fails/passes for the security reason and its anti-fake tests survived three independently constructed attacks (two the author's, one mine), assessment items map to outcomes with zero citation mismatches, operations content is unusually strong (`06-operate.md`'s discard-not-store recovery step), transfer materially changes actors and stakes, and safety is structurally enforced rather than promised. The one dimension I cannot score above 2 is **Standards accuracy**: the module's own explicit, checkable claim — "live-fetched and re-verified... against their canonical pages" — is false for one of four standards, as shown in Blocker 1.
- [ ] `depth: publishable` — **withheld.** Per this task's governance rule, a real blocker withholds every grant, not only `depth`. Blocker 1 does not require re-authoring any lesson, the lab, or the assessment bank — it is a two-line URL fix in `pins.yaml` and `module.yaml` — so I expect a narrowly-scoped re-review to clear it quickly, the same way 1.4's five blockers were closed and re-reviewed without touching the lab or the lessons' arguments.
- [ ] `quality: transfer-ready` — not available at this pass; that state requires satisfactory *learner* evidence from the transfer task (`items.md` #7), and no learner evidence exists yet. The transfer task itself is well-built (see above) and ready to produce that evidence once attempted.

**Lab-safety review: passes, no findings** (see §8 above) — recorded here so a future re-review does not have to redo it, but it does not by itself authorize any grant while Blocker 1 stands, per this task's rule that both the quality side and the lab-safety side must pass before `depth: publishable`/`status: published` can be set.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real blocker exists. `content/modules/0/0.1/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as this pass set them; `content/progress/STATUS.yaml` is left untouched. STATUS was updated by: nobody yet. An authoring agent may not write that line, and neither may an independent review that found a live blocker.
