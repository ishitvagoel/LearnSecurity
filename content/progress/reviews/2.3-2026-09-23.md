# 2.3 — independent review, 2026-09-23

**Reviewed commit:** `e43c153` on `deepen/2.3` ("Deepen module 2.3: finish validation and cross-checks after rate-limit continuation"), following the rate-limited WIP checkpoint at `7186fb0`.
**Reviewer:** independent quality reviewer, also performing the lab-safety check for this pass (read-only agent session).
**Independence statement:** I did not author or revise any content file in this review's scope before writing this artifact. I had no prior context on module 2.3 before this session. Every judgement below comes from reading the files at the commit above, from running the commands recorded in §Lab myself, from constructing my own fakes (both the documented one and a second one of my own invention) rather than trusting the changelog's description of them, and from live fetches recorded in §Standards. Files I constructed to test the lab (fake `app.py` variants, a scratch copy of `tests/`/`conftest.py`) live only under my scratchpad directory and a local pytest-check copy, never under `labs/` or any tracked path; `git status --porcelain` on this worktree shows only this review artifact as new before I commit it.

## Files inspected

Read in full:

- `content/modules/2/2.3/spec.md`
- `content/modules/2/2.3/module.yaml`
- `content/modules/2/2.3/lessons/01-property.md` through `08-review.md` (all eight)
- `content/modules/2/2.3/assessment/items.md`
- `content/modules/2/2.3/assessment/rubric.md`
- `content/assessment/keys/2.3.md`
- `labs/2.3/2.3-browser-policy/README.md`
- `labs/2.3/2.3-browser-policy/conftest.py`
- `labs/2.3/2.3-browser-policy/vulnerable/app.py`, `vulnerable/SECURITY.md`
- `labs/2.3/2.3-browser-policy/fixed/app.py`, `fixed/SECURITY.md`
- `labs/2.3/2.3-browser-policy/tests/test_browser_policy.py`
- `content/progress/reviews/4.3-2026-09-17.md`, `content/progress/reviews/0.2-2026-09-22.md` (both in full, for shape and rigor)
- `content/standards/pins.yaml` (the shared `owasp-asvs-5.0.0` pin, current state on this branch and on `origin/deepen/2.2`)
- `content/progress/STATUS.yaml` (the 2.3 entry)
- `scripts/lint_baseline.json` (diffed against the parent of `e43c153`)

External sources fetched live to verify standards claims rather than assert them from memory:

- `https://raw.githubusercontent.com/OWASP/ASVS/v5.0.0/5.0/docs_en/OWASP_Application_Security_Verification_Standard_5.0.0_en.json` (200; parsed as JSON and checked the exact `Description` text for `V3.3.1`, `V3.3.2`, `V3.3.4`, `V3.4.2`, `V3.4.3`, `V3.4.6`)
- `https://owasp.org/projects/asvs` (200)
- `https://www.w3.org/TR/CSP3/` (200; confirmed "W3C Working Draft", dated 16 September 2026)
- `https://www.w3.org/TR/trusted-types/` (200; confirmed "W3C Working Draft", dated 23 June 2026)

## What I verified as true

**Lab test matrix, run myself, not trusted from the README.**

```
$ python3 -m pytest labs/2.3/2.3-browser-policy/tests --impl vulnerable -q
6 failed, 3 passed in 0.14s
$ python3 -m pytest labs/2.3/2.3-browser-policy/tests --impl fixed -q
9 passed in 0.09s
```

Matches the claimed 6/9 fail (vulnerable), 9/9 pass (fixed) exactly. `bash scripts/run_labs.sh 2.3` also reports `failed / passed / OK`.

**The exact documented cookie fake, constructed and run against the *current* test file, not trusted from the changelog.** I loaded the current `tests/test_browser_policy.py`'s `_cookie_attribute_tokens` helper directly and a hand-built `app.py` whose `/login` calls `response.set_cookie("sc_session", "tok-HttpOnly-Secure")` — the exact fake the module.yaml changelog, the README, and the isolated key all describe. Result: `Set-Cookie: sc_session=tok-HttpOnly-Secure; Path=/; SameSite=lax`, parsed tokens `{'path=/', 'samesite=lax'}` — neither `httponly` nor `secure` present. Both `test_forbidden_outcome_session_cookie_missing_httponly` and `test_secure_attribute_is_also_present` correctly fail against this fake. The claimed fix (parsing into `;`-delimited attribute tokens instead of a raw substring search) is genuine and does close the documented gap.

**My own, different adversarial cookie fake — not the documented one.** I tried smuggling literal semicolons into the cookie *value* itself (`response.set_cookie("sc_session", "abc; HttpOnly; Secure")`), hoping a naive split-on-`;` parser could be fooled by value-embedded attribute-looking text. Python's `http.cookies` module octal-escapes the semicolons in the emitted header (`sc_session="abc\073 HttpOnly\073 Secure"; Path=/; SameSite=lax`), so the parser's tokens are still just `{'path=/', 'samesite=lax'}` and both tests still correctly fail against this fake too. The parsing fix is robust against both the documented cheat and this independently-constructed one.

**Standards — all six ASVS citations verified against the live v5.0.0 JSON, text matched exactly.** `V3.3.1` (Secure), `V3.3.2` (SameSite), `V3.3.4` (HttpOnly), `V3.4.2` (CORS `Access-Control-Allow-Origin` fixed-or-allowlist-validated), `V3.4.3` (enforcing CSP, `object-src 'none'`/`base-uri 'none'` floor), `V3.4.6` (`frame-ancestors`) — every `Description` field I fetched matches what the lessons, the lab, and the keys claim for it, including the exact `object-src`/`base-uri` floor language `fixed/app.py`'s `CSP` constant and `test_csp_is_enforced_not_only_reported` both encode. `owasp.org/projects/asvs`, `w3.org/TR/CSP3/`, and `w3.org/TR/trusted-types/` all resolve 200, and CSP3/Trusted Types are both still labeled "W3C Working Draft" on the live pages today. This module's standards work is accurate.

**Shared `content/standards/pins.yaml` — consistent with, but will collide on merge with, module 2.2's parallel fix.** Both `deepen/2.3` (this branch) and `origin/deepen/2.2` branched from the same ancestor (`6ac0147`) and independently edited the same `owasp-asvs-5.0.0` pin's `url` and `notes` fields after finding the same dead `www-project-application-security-verification-standard` URL. **The two fixes agree on the substantive fact**: both set `url: "https://owasp.org/projects/asvs"` (the same value), and both append a narrative to the shared `notes` string describing their own module's verification. There is no factual contradiction between the two branches. There **is** a mechanical git-merge collision waiting: both branches touched the same lines of the same YAML block (2.2 sets `reviewedAt: "2026-09-22"`, 2.3 sets `reviewedAt: "2026-09-23"`, and both append different prose to the same `notes` field), so merging both to `main` will need a textual reconciliation (keep the later `reviewedAt` and concatenate or fold both notes) even though nothing here needs a human policy decision. I did not attempt to resolve this myself, per the task's instructions — flagging it is the deliverable.

**Coverage contract completeness.** All four rows of `spec.md`'s coverage contract (matching `module.yaml`'s four `outcomes`, one-to-one) are populated across all seven columns; no empty cells. C1, C2, C3, and C4 each carry a genuine lab assertion (`spec.md`'s own teaching-claims table), well past the ≥2-claims minimum; C5 is honestly declared non-code-testable with a specific, checkable reason (no second origin server or WebView runtime in this fixture) rather than mapped to a test that would not actually assert it.

**Teaching-claims table (`spec.md` §Teaching claims), cross-checked cell by cell against `items.md`'s own tags — clean.** `items.md`'s tags: #1→C1,C4; #2→C2,C3; #3→C2; #4→C1; #5→C2; #6→C4; #7→C1,C5; #8→C3. Checked against the union of each claim's citations: C1→{#1,#4,#7} — every item tagged C1 is #1, #4, #7 and no others, exact match. C2→{#2,#3,#5} — exact match. C3→{#2,#8} — exact match. C4→{#1,#6} — exact match. C5→{#7} — exact match. **This table is genuinely correct**, and the pass's claim to have fixed it holds up.

**Mechanical gates, run myself.**

```
$ python3 scripts/lint_content.py content/modules/2/2.3 --no-baseline
0 error(s), 0 warning(s)

$ npm --prefix site run build   # after npm install under site/
✓ Compiled successfully in 19.9s
✓ Generating static pages using 3 workers (643/643) in 24.0s
```

Both match the claim.

**Lint baseline delta.** `git show e43c153 -- scripts/lint_baseline.json` shows 33 deletions and 0 additions (all removed lines are `content/modules/2/2.3/...` entries whose per-file finding counts dropped to zero). The baseline fell, never rose, matching the changelog's claim.

**`estimatedMinutes: 330` recomputed from the formula.** Lesson word counts (via `wc -w`): 1578+1494+1265+1160+1231+1138+1200+1105 = 10171. `10171/200 ≈ 50.9 + 120 (Tier 2 lab) + 8×12=96 (eight items) + 60 (transfer) = 326.9 → rounds to 330`. Matches exactly.

**Metadata-honesty.** `module.yaml`'s `reviewer: pending independent quality and lab-safety review`, `status: draft`, `lastReviewedAt: null`, `nextReviewAt: null` — correct; no `depth`/`quality` field set anywhere by the authoring pass.

**Lab safety: passes, no findings.** Grepped the entire `labs/2.3/2.3-browser-policy/` tree for `socket|subprocess|requests|urllib|http.client|os.system|open(|environ` — none found. Both `app.py` files import only `fastapi`; `conftest.py`'s `--impl` is restricted to `choices=("vulnerable", "fixed")` and loads only `ROOT/<name>/app.py`. Both `SECURITY.md` files state "local fixture only" / "authorized scope: this directory only" and name only synthetic values (`synthetic-session`, placeholder note text). Reset is `git checkout -- labs/2.3/2.3-browser-policy` with no persisted state. No lesson instructs contacting a real browser, a live site, or a real WebView/clinic build; every "Use it somewhere new" and "What this page is not doing" section explicitly disclaims it. Minimum-five and all five required test categories (normal, forbidden outcome, boundary, malformed/failure, anti-fake — two anti-fake tests, in fact) are present, satisfying Tier-2 `lab-realism.mdc`.

## Blockers

**1. A specific, repeated, independently-falsified quantitative verification claim: the module says the `origin.endswith("securecollab.example")` fake fix "passes 6 of 9 tests," but it actually passes 7 of 9.** *(Editorial integrity / metadata honesty — the claim is presented as something this pass "independently re-verified, by constructing and running it.")*

The claim appears three times, verbatim in shape, across `module.yaml`'s 2026-09-23 changelog entry, `labs/2.3/2.3-browser-policy/README.md`'s "Verify" section, and `content/assessment/keys/2.3.md`'s Lab section — always as "passes 6 of 9 tests... fails exactly `test_anti_fake_lookalike_domain_is_not_treated_as_the_trusted_origin` and `test_origin_vs_site_boundary_subdomain_scheme_and_port_are_each_denied`."

I built this exact fake myself (an `app.py` whose `/notes` handler gates on `origin.endswith("securecollab.example")` instead of exact set membership, cookie/CSP otherwise identical to `fixed/app.py`) and ran the real, current 9-test suite against it twice, independently: once by importing the test module and calling each `test_*` function directly against a `TestClient` built on my fake, and once through a real `pytest` invocation in an isolated copy of `tests/` and a patched `conftest.py` accepting a third `--impl` choice.

```
$ python3 -m pytest tests --impl endswith_fake -q
2 failed, 7 passed in 0.10s
FAILED tests/test_browser_policy.py::test_origin_vs_site_boundary_subdomain_scheme_and_port_are_each_denied
FAILED tests/test_browser_policy.py::test_anti_fake_lookalike_domain_is_not_treated_as_the_trusted_origin
```

Both runs agree: **exactly the two named tests fail, and the other seven — including `test_forbidden_outcome_attacker_origin_gets_no_credentialed_access` — pass.** 9 − 2 = 7, not 6. The module's own claim is arithmetically self-contradictory as written (it names exactly two failing tests and separately asserts a pass count one lower than that arithmetic requires), and it is wrong in the same direction and by the same amount in all three places it appears, which suggests one miscount that was then copied forward rather than three independent errors. This is not a difference in what fails — the two tests the module names as the failure points are exactly the two that do fail, and the fake's actual behavior (accepting the lookalike and the sibling subdomain while still correctly denying the unrelated attacker origin) is exactly what the module's own narrative describes — only the total pass count copied into three files is off by one.

**To close:** change "passes 6 of 9 tests" to "passes 7 of 9 tests" in all three locations (`module.yaml`'s changelog entry, the lab README's "Verify" section, `content/assessment/keys/2.3.md`'s Lab section). No code, test, or lesson-reasoning change is needed — the underlying fake, and the two tests that catch it, are correct; only the arithmetic in the prose describing them is wrong.

**2. `spec.md`'s coverage-contract table's `Claim` column does not match the union of the claims its own cited `Assessment item` cells carry, in three of its four rows — despite the 2026-09-23 changelog's explicit claim to have "corrected all cells to match items.md's tags exactly."** *(Assessment alignment — the same defect class 4.3's, 2.1's, and 1.4's reviews each found, which this pass's own changelog claims is now fixed.)*

I re-derived `items.md`'s own `Claim assessed:` tags directly (not from any table) and cross-checked every coverage-contract row cell by cell:

- **Row 1** ("Identify by exact header name...", Claim column: `C1, C4`; cites items.md #1, #4). Tags: #1→C1,C4; #4→C1. Union = {C1, C4}. **Matches exactly.**
- **Row 2** ("Produce a browser policy matrix...", Claim column: `C1, C2, C4`; cites items.md #2, #5, #6, #8). Tags: #2→C2,C3; #5→C2; #6→C4; #8→C3. Union = **{C2, C3, C4}**. The row's stated Claim column (`C1, C2, C4`) both **includes C1**, which appears in none of the four cited items, **and omits C3**, which appears in two of them (#2 and #8).
- **Row 3** ("Given an Origin header and the allow-list...", Claim column: `C2, C3`; cites items.md #3 only). Tag: #3→C2. The row's Claim column names C3, but the only assessment item this row cites is not tagged C3 at all — no assessment item backs that half of the row's own claim.
- **Row 4** ("Given a new reader of `sc_session`...", Claim column: `C5`; cites items.md #7). Tag: #7→C1, C5. The row's Claim column omits C1, which its own cited item explicitly carries (item 7 asks the learner which of C1–C5 transfer *and* which change — it genuinely tests both).

Only Row 1 is internally consistent. This is the identical citation-transposition defect class the 2026-09-23 changelog entry says it fixed for the *teaching-claims* table (which I independently confirmed is now clean — see §What I verified) and separately claims to have fixed for this coverage-contract table too ("corrected all cells to match items.md's tags exactly") — but the coverage-contract table's `Claim` column was never actually re-derived from `items.md`'s tags with the same rigor; only the `Assessment item` column's citations (which item numbers to list) appear to have been corrected, not the `Claim` column naming which claims those items assess.

**To close:** in `spec.md`'s coverage contract, change Row 2's Claim column from `C1, C2, C4` to `C2, C3, C4`; change Row 3's Claim column from `C2, C3` to `C2` (or add a C3-tagged item's citation to that row's Assessment-item cell if C3 genuinely belongs there); change Row 4's Claim column from `C5` to `C1, C5`. No lesson content needs to change — only these three cells.

## Verdict

- [ ] `quality: competent`
- [ ] `depth: publishable`
- [ ] `quality: transfer-ready`

**None granted.** Both blockers are real, and I verified each by construction rather than by trusting the changelog: Blocker 1 by running the exact named fake against the exact named test suite twice (manual invocation and a real `pytest` run) and getting 7/9, not the claimed 6/9, both times; Blocker 2 by re-deriving `items.md`'s own tags directly and checking every coverage-contract cell against them by hand, finding three of four rows wrong in ways the changelog's own "corrected all cells... exactly" claim rules out.

Neither blocker requires re-authoring any lesson's argument, and everything else I checked — the lab's actual code and its 9-test matrix, both anti-fake tests (verified against the documented fake and against a second fake of my own construction), all six ASVS citations against the live v5.0.0 text, the CSP3/Trusted Types URLs and their Working Draft status, the teaching-claims table's citations, the mechanical gates, the lint baseline delta, `estimatedMinutes`, the metadata-honesty fields, and lab safety — held up under independent verification. The lesson prose itself (all eight lessons, each well over 900 words with a derivation, a worked example, and a counterexample) is genuinely strong and I found no defect in it.

Per this task's governance rule and the 0.2/4.3/2.1 precedent, a real blocker withholds every grant even where most of the module's work is sound. I am not writing `depth`, `quality`, `reviewer` beyond the existing "pending" line, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere — not in this file, not in `content/modules/2/2.3/module.yaml`, not in `content/progress/STATUS.yaml`. `module.yaml`'s `reviewer: pending independent quality and lab-safety review` / `status: draft` are left exactly as this pass set them; `content/progress/STATUS.yaml`'s `2.3` entry (`quality: developing`, `depth: map-complete`) is left untouched.

STATUS was updated by: nobody yet. An authoring agent may not write this line, and neither may an independent review that found a live blocker.

I also separately flag, for whoever eventually merges `deepen/2.2` and `deepen/2.3`: the shared `content/standards/pins.yaml` `owasp-asvs-5.0.0` pin was edited on both branches from the same ancestor, with the same corrected `url` value but different `reviewedAt` dates and different appended `notes` text — not a factual conflict, but a textual merge that will need manual reconciliation (see §What I verified). I did not attempt to resolve it.
