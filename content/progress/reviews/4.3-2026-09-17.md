# 4.3 — independent review, 2026-09-17

**Reviewed commit:** `7813829` on `deepen/4.3` ("Deepen module 4.3 (B0 pilot) to publishable-depth prose and lab tests")
**Reviewer:** independent quality reviewer (read-only agent session), also performing the lab-safety check for this pass
**Independence statement:** I authored no file in this review's scope and had no prior context on module 4.3 before this session. Every judgement below comes from reading the files at the commit above and from running the commands recorded in the Lab section myself. I temporarily copied `fixed/token.py` into a scratch `fake/` directory, then swapped it into `fixed/` to run the anti-fake check against a hand-built fake, then restored the original file from that same commit — `git status --porcelain` was empty afterward. The only files I have written in this repository are this artifact and a scratch verification script (`asvs_check.py`, removed before finishing).

## Files inspected

Read in full: `content/modules/4/4.3/spec.md`, `module.yaml`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, `content/assessment/keys/4.3.md`, `labs/4.3/4.3-lab/{README.md,conftest.py,vulnerable/token.py,vulnerable/SECURITY.md,fixed/token.py,fixed/SECURITY.md,tests/test_property.py}`, `content/standards/pins.yaml` (the `owasp-asvs-5.0.0` entry), `.cursor/skills/quality-gate/references/publishability.md`, and `content/progress/reviews/1.4-2026-09-16.md` as the precedent artifact shape. Fetched and parsed the live OWASP ASVS 5.0.0 `v5.0.0` tag JSON (`raw.githubusercontent.com/OWASP/ASVS/v5.0.0/5.0/docs_en/OWASP_Application_Security_Verification_Standard_5.0.0_en.json`) directly, rather than trusting the module's or `pins.yaml`'s account of what it says. Diffed `HEAD~1` against `HEAD` for `labs/4.3/4.3-lab/` and `content/modules/4/4.3/module.yaml` to independently confirm the two historical claims (lab test count before this pass; the metadata-honesty defect's prior state).

## What I verified as true

**C3 lab-bug claim — confirmed genuine, not fabricated.** `git show HEAD~1:labs/4.3/4.3-lab/tests/test_property.py` has exactly three tests, all channel-only (query/cookie/header); `git show HEAD~1:.../vulnerable/token.py` and `.../fixed/token.py` both define only `session_from_request` — `session_is_active` does not exist in either file before this commit. So "the lab previously tested only C1, and `session_is_active` is new in this pass" is true, not a story.

**Hand-built distinguishing input.** I traced `session_is_active({"issued_at": now-432_000, "last_seen_at": now-1}, now)` by hand and then ran it: `vulnerable/token.py` returns `True` (only checks `now - last_seen_at`), `fixed/token.py` returns `False` (also checks `now - issued_at`). This is exactly the "recent activity, ancient creation" case the task asked me to construct, and the two implementations disagree exactly as claimed.

**Anti-fake test genuinely catches the described fake.** I built the fake myself: copied `fixed/token.py`, replaced the `now - issued_at >= ABSOLUTE_TIMEOUT_SECONDS` comparison with `if issued_at == 1_000_000.0 - (43_200*10): return False` (special-casing the forbidden-outcome test's exact constant), swapped it into `fixed/` and ran the suite:

```
$ python3 -m pytest tests --impl fixed -v
... 1 failed, 8 passed ...
FAILED tests/test_property.py::test_anti_fake_absolute_expiry_with_fresh_never_elsewhere_used_values
```

Exactly the claimed 8/9, and the one failure is the anti-fake test, not an unrelated one. Restored the original file; `git status --porcelain` clean.

**Full pass/fail counts, run myself, not trusted from the README:**

```
$ python3 -m pytest labs/4.3/4.3-lab/tests --impl vulnerable
3 failed, 6 passed in 0.04s
FAILED test_query_string_token_is_rejected
FAILED test_forbidden_outcome_activity_alone_does_not_extend_the_absolute_limit
FAILED test_anti_fake_absolute_expiry_with_fresh_never_elsewhere_used_values

$ python3 -m pytest labs/4.3/4.3-lab/tests --impl fixed
9 passed in 0.02s
```

Every vulnerable failure is a plain `assert False`/`assert True is False` on the security property, not a `KeyError` or collection error — the vulnerable variant fails for the claimed reasons, not because the file doesn't run.

**Metadata-honesty corrections — both genuine, matching the claim exactly.** `git show HEAD~1:content/modules/4/4.3/module.yaml` has `reviewer: pending independent quality and lab-safety review` alongside `lastReviewedAt: '2026-08-24'` and `nextReviewAt: '2027-02-24'` — real-looking dates with no review artifact under `content/progress/reviews/` to back them (confirmed: no `4.3-*.md` existed there before this pass). `HEAD`'s `module.yaml` correctly reverts both to `null`. The prior `outcomes` list's first entry was the literal string `'Demonstrate: Sessions, cookies, and tokens'` — confirmed present verbatim at `HEAD~1`, confirmed absent at `HEAD`, replaced with six specific, observable behaviors.

**Mechanical gates — run myself:**

```
$ python3 scripts/lint_content.py content/modules/4/4.3 --no-baseline
0 error(s), 0 warning(s)

$ npm --prefix site run build
✓ Compiled successfully
✓ Generating static pages using 3 workers (643/643)
```

`scripts/lint_baseline.json`: `git diff HEAD~1 HEAD` shows 40 deletions, 0 insertions, confirming the "40 deletions, 0 insertions" claim. (The claimed absolute totals, "2359 to 2349", do not match what I computed by summing the baseline's own `counts` — old total 2389, new total 2349, a 40-item drop either way. This absolute-count discrepancy doesn't appear in any committed file I could find — it isn't a claim `module.yaml`, `spec.md`, or the changelog actually makes — so I'm recording it as a minor unresolved discrepancy in the validation report handed to me, not as a defect in the module itself.)

**ASVS 5.0.0 citations — live-fetched and checked against the canonical `v5.0.0` tag, all nine identifiers, wording and chapter/level.** All nine requirement IDs exist, are correctly numbered, and are attributed to the right claim:

| ID | Chapter | Level | Module's paraphrase | Verdict |
|---|---|---|---|---|
| 14.2.1 | V14 Data (Data Protection) | L1 | "sensitive data ... only in HTTP bodies or headers, never URLs or query strings" | Accurate |
| 3.4.5 | V3 Web (Web Frontend Security) | L2 | "a referrer policy to stop technically sensitive data leaking via Referer" | Accurate |
| 7.2.4 | V7 Session (Session Management) | L1 | "a new session token is generated on authentication ... and the prior token is terminated" | Accurate, near-verbatim |
| 7.3.1 | V7 Session | L2 | "an inactivity (idle) timeout enforced per documented risk decisions" | Accurate |
| 7.3.2 | V7 Session | L2 | "an absolute maximum session lifetime enforced per documented risk decisions" | Accurate |
| 7.4.1 | V7 Session | L1 | "session termination ... disallows further use of the session; for reference tokens or stateful sessions this means invalidating the data at the backend" | Accurate, near-verbatim |
| 3.3.1 | V3 Web | L1 | "cookies carry the Secure attribute" | **Incomplete** — see below |
| 3.3.2 | V3 Web | L2 | "a cookie's SameSite attribute is set according to its purpose" | Accurate (omits "user interface redress attacks" alongside CSRF, minor) |
| 3.3.4 | V3 Web | L2 | "a cookie not meant to be read by client-side scripts ... carries HttpOnly" | Accurate (omits the requirement's second clause about the value only ever traveling via `Set-Cookie`, minor) |

The module's own claim that it corrected a prior mischaracterization of `14.2.1` as belonging to `V3` rather than `V14` is also confirmed — `review triggers` in `spec.md` states this explicitly and the live tag confirms `14.2.1` sits under `V14 Data`, not `V3`.

**Minor finding — ASVS 3.3.1's paraphrase drops a mandatory clause.** The actual requirement text is: *"cookies have the 'Secure' attribute set, and if the '\_\_Host-' prefix is not used for the cookie name, the '\_\_Secure-' prefix must be used for the cookie name."* Every citation of 3.3.1 in this module (`spec.md` line 116, `04-build.md`'s attribute table, `keys/4.3.md`) describes it only as "cookies carry the Secure attribute," never mentioning the cookie-name-prefix clause. This isn't a fabricated citation — the Secure-attribute half is real — but the module's own worked cookie name, used consistently across every lesson (`sc_session`), does not carry `__Secure-` or `__Host-`, so the module's canonical example would not itself satisfy the literal text of the Level-1 requirement it cites as C5's anchor. Not a blocker on its own (C5's teaching point about network eavesdropping stands regardless), but worth fixing alongside the item below.

**Lab safety — pass, no findings.** Both `SECURITY.md` files and the lab README state local-only, synthetic-data scope; `session_from_request` and `session_is_active` are pure functions over dicts/floats with no network, filesystem, or subprocess access; every fixture value (`secret`, all timestamps) is fabricated and labeled as such in every lesson's closing "What this page is not doing" section; reset is `git checkout --` with no persisted state; no live-target instructions, no real PII, no production secrets.

## Blocker

**`spec.md`'s coverage contract cites the wrong assessment item for 5 of its 6 outcome rows.** This is the same defect class the 1.4 and 2.1 reviews found (wrong item-outcome citations) — here it is more extensive. Comparing the "Assessment item" column against `items.md`'s own `**Claim assessed**`/`**Outcome**` labels for each item:

| Coverage-contract row (Claim) | Cites | What that item is actually tagged as | Correct citation |
|---|---|---|---|
| C1 (channel) | items.md #1 | #1 = C1 | Correct |
| C2 (fixation) | items.md #3 | #3 = **C3** (lifetime-checker gap) | Should be #2 |
| C3 (idle/absolute) | items.md #5, #6 | #5 = **C5** (cookie-setting call); #6 = **C4** (logout design) | Should be #3, #4, #7 |
| C4 (revocation) | items.md #8 | #8 = **C1–C5** (transfer item) | Should be #6 (and #9) |
| C5 (cookie attributes) | items.md #4, #9 | #4 = **C3**; #9 = **C3, C4** (operate) | Should be #5 |
| Transfer (C1–C5) | items.md #7, #9 | #7 = **C3** (two-clocks design); #9 = **C3, C4** | Should be #8 |

Only the first row is correct. Every other row points at an item that, by its own stated Claim and Outcome, tests something else entirely — the C2 row's citation is a C3 item, the C3 row's citations are a C5 item and a C4 item, the C4 row's citation is the transfer item, the C5 row's citations are two C3-flavored items, and the transfer row's citations are a C3 item and an operate item. Item #9 never receives a correct citation anywhere in the table, despite testing a real outcome (C4's revocation signal). The underlying `items.md` bank is itself internally consistent and well-constructed — the defect is entirely in `spec.md`'s cross-reference table, which is exactly the kind of "the module's own account of itself does not match the artifact" failure `metadata-honesty.mdc` and this repo's review culture exist to catch, and it is more extensive here (5 of 6 rows) than the four-citation defect found on 2.1.

**To close:** rewrite the Assessment-item column of `spec.md`'s coverage contract using the table above (or by re-deriving it from `items.md`'s own `Outcome:` labels row by row, the way the 1.4 remediation did), and re-check the result cell by cell against `items.md` rather than against the item-type ordering (2 discrimination / 3 diagnosis / 2 design / 1 transfer / 1 operate), which is what I suspect produced this — the table reads as though it assumed claims and item types line up sequentially rather than checking what each item is actually tagged as.

## Minor finding (not a blocker)

`items.md` #9's own `**Claim assessed:** C3, C4` is inconsistent with its own `**Outcome:**` field, which names only the C4-outcome ("Distinguish server-side revocation..."). The item's content (two log lines: one for idle/absolute expiry, one for logout) genuinely spans both claims, so either the Outcome field should be broadened or the Claim field narrowed to match; worth a one-line fix alongside the coverage-contract rewrite above.

## Coverage contract — no empty cells

Checked all six outcome rows in `spec.md` against `module.yaml`'s six `outcomes` entries: they match one-to-one, and every cell (Explanation, Worked example, Practice, Assessment item, Transfer) is populated. C1 and C3 carry genuine, verified lab assertions (the ≥2-claims bar is met); C2, C4, and C5 are honestly declared non-code-testable with a stated, checkable reason each (module 4.2 owns the login event for C2's mint-fresh mechanism; this fixture has no persisted store for C4's revocation; C5 is a response-header decision this request-parsing fixture never makes) — I checked each of these three claims and they hold: none of them is quietly assumed solved, and none is a claim with literally no evidence anywhere.

## Verdict

- [ ] `quality: developing`
- [ ] `quality: competent`
- [ ] `depth: publishable`
- [ ] `quality: transfer-ready`

**None granted.** The lesson prose, the state-diagram model, the lab (including a real, verified anti-fake pair and a real, previously-nonexistent C3 test suite), the ASVS standards work (eight of nine citations fully accurate, the ninth accurate but incomplete), and the metadata-honesty corrections (reviewer/dates reverted to null, placeholder outcome replaced) are all genuinely strong and independently confirmed — this is not a thin or padded pass. But `spec.md`'s coverage contract, the document this repo's own quality gate uses to certify that every outcome has real assessment evidence, cites the wrong item for five of its six rows. That is a checkable, substantive defect in the module's own self-description, in the same family the 1.4 and 2.1 reviews used to withhold `depth: publishable` even when the underlying lessons and lab were sound — and per this review's governing instructions, one substantive issue is enough to withhold. I am not writing `quality` or `depth` into `STATUS.yaml`, and I am not touching `module.yaml`'s `reviewer`, `lastReviewedAt`, or `nextReviewAt` fields, consistent with the 1.4 precedent of leaving those fields untouched while a blocker stands. The fix is a one-table edit to `spec.md` (plus the optional one-line 3.3.1/item-9 touch-ups above) and does not require re-authoring any lesson or the lab; I would expect a re-review limited to the changed lines to close quickly.

STATUS was updated by: nobody. This artifact records the verdict; `content/progress/STATUS.yaml` and `content/modules/4/4.3/module.yaml` were deliberately left untouched.
