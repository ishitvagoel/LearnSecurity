# 2.2 — independent review, 2026-09-23

**Reviewed commit:** `c26f92b` on `deepen/2.2` ("Finish deepening module 2.2: item bank, real lab bug, citation fixes"), which continues the rate-limited checkpoint at `6c49552` ("WIP: deepen module 2.2 (rate-limited mid-pass, continuing separately)").
**Reviewer:** independent quality reviewer (read-only agent session), also performing the lab-safety check for this pass.
**Independence statement:** I authored no file in this module's scope and had no prior authoring context before this session. Every judgement below comes from reading the files at `c26f92b` and from commands I ran myself. Where I modified `fixed/app.py` to construct an adversarial fake, I restored the original file afterward and confirmed `git status --porcelain` was empty before moving on; the only files I have written in this repository are this artifact and scratch files under `/tmp` outside the repo tree.

## Files inspected

Read in full: `content/modules/2/2.2/spec.md`, `module.yaml`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, `content/assessment/keys/2.2.md`, `labs/2.2/2.2-request-path/{README.md,conftest.py,vulnerable/app.py,vulnerable/SECURITY.md,vulnerable/cache.py,fixed/app.py,fixed/SECURITY.md,fixed/cache.py,requirements.txt,tests/test_cache_key.py,tests/test_tls_hop.py}`, `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry (both on this branch and on `origin/deepen/2.3`), and the precedent artifacts `content/progress/reviews/4.3-2026-09-17.md` and `content/progress/reviews/0.2-2026-09-22.md`. Live-fetched the OWASP ASVS `v5.0.0` tag's full requirements JSON directly (not trusted from `pins.yaml`'s account of it), and live-checked both the old and new ASVS project-page URLs and both cited RFC pages with `curl -L`.

## What I verified as true

**The `is None` vs `is not True` bug — confirmed genuine, precisely as claimed.** `fixed/app.py`'s real `hop_is_trustworthy` reads `if cert_trusted_ca is not True: return False`. I edited a scratch copy to the described fake (`if cert_trusted_ca is None: return False`) and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed
1 failed, 13 passed
FAILED tests/test_tls_hop.py::test_explicitly_untrusted_ca_is_rejected_even_with_matching_hostname_and_version
```

Exactly and only the new test fails — no other test in the 14-test suite depends on this distinction. I restored the file (`cp` from a pre-edit copy) and confirmed `git status --porcelain` was empty.

**Full pass/fail counts, run myself, not trusted from the README or changelog:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable
8 failed, 6 passed
FAILED test_cache_key.py::test_other_company_does_not_receive_cached_body
FAILED test_cache_key.py::test_forwarded_company_header_cannot_grant_a_different_companys_note
FAILED test_cache_key.py::test_anti_fake_cache_key_with_fresh_never_elsewhere_used_company_and_note
FAILED test_cache_key.py::test_anti_fake_forwarded_header_with_fresh_never_elsewhere_used_values
FAILED test_tls_hop.py::test_hostname_mismatch_with_trusted_ca_is_rejected
FAILED test_tls_hop.py::test_tls_1_2_boundary_is_still_accepted_1_1_is_not
FAILED test_tls_hop.py::test_anti_fake_hostname_prefix_match_is_rejected
FAILED test_tls_hop.py::test_anti_fake_hostname_substring_anywhere_is_rejected

$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed
14 passed
```

This matches the claimed "vulnerable 6/14 [pass], fixed 14/14" and items.md's own evidence checklist ("vulnerable/ tests show 8 of 14 failing"). Every vulnerable failure is a plain assertion on the security property (a `404` expected, a `200` returned, or vice versa for the hop check), not a collection or import error — the vulnerable variant fails for the claimed reasons. `bash scripts/run_labs.sh 2.2` independently reports the same shape (`vulnerable failed, fixed passed, RESULT OK`).

**Coverage-contract and teaching-claims citations — re-derived independently from `items.md`'s own tags, cell by cell, and found fully correct.** I built my own map from each item's `Claim assessed` tag: #1→C1, #2→C2, #3→C3, #4→C1,C2, #5→C4, #6→C1,C5, #7→C1–C5, #8→C1,C3,C5. Checked against `spec.md`'s teaching-claims table:

| Claim | spec.md cites | Items actually tagged with this claim | Match |
|---|---|---|---|
| C1 | #1, #4, #6, #7, #8 | #1, #4, #6, #7, #8 | Exact |
| C2 | #2, #4, #7 | #2, #4, #7 | Exact |
| C3 | #3, #7, #8 | #3, #7, #8 | Exact |
| C4 | #5, #7 | #5, #7 | Exact |
| C5 | #6, #7, #8 | #6, #7, #8 | Exact |

Then checked the coverage contract's four outcome rows against each item's own `Outcome:` text (not against module.yaml's slightly longer paraphrase, since `items.md` shortens it, but the meaning and claim scope agree in every case): row 1 (C1,C2 → #1,#2,#4) — all three items carry that exact outcome text; row 2 (C3,C4 → #3) — only item #3 carries that outcome text; row 3 (C1,C2,C4 → #5) — only item #5 carries it; row 4 (C1,C2,C5 → #6,#7,#8) — all three carry it. **All nine cells across both tables are correct.** This is the defect class every prior module's review (1.4, 2.1, 4.3, 0.1, 0.2, 1.1) found broken on first pass; here it is genuinely clean, confirming the second pass's own claim to have fixed it.

**Standards — all nine ASVS 5.0.0 citations verified against the live `v5.0.0` requirements JSON, exact text and exact level, no discrepancies:**

| ID | Level (live) | Module's paraphrase | Verdict |
|---|---|---|---|
| 4.1.3 | L2 | forwarded headers not end-user overridable | Accurate |
| 12.1.1 | L1 | only latest TLS versions (1.2, 1.3) | Accurate |
| 12.2.1 | L1 | TLS for all external connectivity | Accurate |
| 12.2.2 | L1 | publicly trusted certificates | Accurate |
| 12.3.2 | L2 | TLS clients validate certs before communicating | Accurate |
| 14.2.2 | L2 | sensitive data not cached in server components | Accurate |
| 14.3.2 | L2 | anti-caching headers to browsers | Accurate |
| 14.2.5 | L3 | web cache deception, correctly labeled advanced/residual | Accurate |
| 16.3.4 | L2 | log security-control failures incl. backend TLS failures | Accurate |

Every level the module and `pins.yaml`'s note claim matches the live document exactly. RFC 9110 ("HTTP Semantics") and RFC 9846 both resolve 200 and are titled as cited; RFC 9846's live page confirms it obsoletes RFC 8446 (and RFC 5246/5077/6961/7627/8422), exactly as `module.yaml` states.

**Dead-URL fix — confirmed genuine, resolves correctly.** `curl -L` on the old `owasp.org/www-project-application-security-verification-standard/` URL: two `308` redirects then a final `404`. `curl -L` on the corrected `https://owasp.org/projects/asvs`: `200`. The fix is real and correctly targeted.

**Metadata honesty — confirmed clean.** `module.yaml` parses with `status: draft`, `reviewer: pending independent quality and lab-safety review`, `lastReviewedAt: null`, `nextReviewAt: null`. `module.schema.json` validation passes.

**Mechanical gates — run myself:**

```
$ python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline
0 error(s), 0 warning(s)

$ python3 scripts/lint_content.py
0 error(s), 0 warning(s), 2182 suppressed by baseline   # matches the claimed 2215->2182 drop

$ npm --prefix site run build   # after npm install under site/
✓ Compiled successfully
✓ Generating static pages using 3 workers (643/643)
```

Key isolation (the same check `.github/workflows/content.yml` runs): no `href="...assessment/keys..."` anywhere in `site/out`, and `keys/2.2.md`'s first substantive line does not appear verbatim in the export. Both pass.

**Coverage contract — no empty cells; ≥2-claim lab-assertion bar exceeded.** All four `module.yaml` outcomes have a matching, fully-populated row in `spec.md`'s coverage contract (Explanation, Worked example, Practice, Assessment item, Transfer). C1, C2, and C3 each carry genuine, verified lab assertions (three claims, not the required two); C4 and C5 are honestly declared non-code-testable with a stated, checkable reason each, and I confirmed both reasons hold: this single-process fixture genuinely has no second real network hop to stage C4, and no CDN/edge process exists to drift for C5.

**Lab safety — pass, no findings.** Grepped the entire lab tree for `socket`, `subprocess`, `os.system`, `requests.`, `urllib`, `http.client`, `paramiko`, `ssl.wrap*`/`ssl.SSLContext` — no hits; the fixture never performs real network I/O, a real handshake, or DNS resolution, matching its own stated non-goals. No real secrets or PII patterns found (`API_KEYS` values are `key-A`/`key-B`/`key-Z`/`key-Q`, plainly synthetic). Both `SECURITY.md` files and the README state local-only scope consistently. Reset is `git checkout --` plus a per-test `reset()`; no persisted state. `vulnerable/`↔`fixed/` pairing is structurally sound and every lesson's closing section disclaims live targets.

## Blocker

**A different, independently-constructed adversarial fake for C3 passes the entire 14-test suite, including every anti-fake test — the module's own "five constructed fakes" review missed a sixth gap in the same check it had just repaired for a different signal.**

Per the task's instruction to build my own fake rather than only re-running the documented ones, I replaced `fixed/app.py`'s version check:

```python
# real fixed code
if tls_version not in _ACCEPTED_TLS_VERSIONS:   # _ACCEPTED_TLS_VERSIONS = {"1.2", "1.3"}
    return False
```

with a plausible alternate "fix" using a lexicographic lower bound instead of exact set membership:

```python
if tls_version < "1.2":
    return False
```

This passes both existing version-related tests (`"1.2"` accepted, `"1.1"` rejected — `"1.1" < "1.2"` is true lexicographically) exactly as the real fix does. I ran the full suite against it:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed
14 passed
```

**All fourteen tests pass, including both hostname anti-fake tests and the newly-added untrusted-CA anti-fake test.** No test in either file sends a `tls_version` value outside `{"1.1", "1.2", "1.3"}`, so nothing distinguishes "exact membership in the accepted set" from "lexicographically at or above the oldest accepted version" — a string like `"9"`, `"2.0"`, or `"A"` sorts above `"1.2"` in Python string comparison (`"9" > "1.2"` is `True`; so is `"A" > "1.2"`, since `'A'` (65) > `'1'` (49) in ASCII) and would be accepted as trustworthy by this fake even though it is not one of the two versions this module's own claim, README, and lessons name as accepted. This is the identical *shape* of gap the module's own second-pass changelog found and fixed for the CA-trust check (a plausible alternate implementation that agrees with the real fix on every input the suite happens to try, and disagrees on inputs it does not) — except this instance is in the version check, and it was not found. I restored the file afterward (`git status --porcelain` clean, confirmed).

I also independently reconstructed one of the module's own documented fakes (the cache-only fix leaving the header override open) to confirm the module's account of its own verification work is not fabricated — it reproduces exactly: 12 of 14 pass, and the two failures are precisely C2's forbidden-outcome test and its anti-fake pair, as `README.md` and `keys/2.2.md` both claim.

**Severity:** this is a real gap in the one thing a Tier-2 lab's anti-fake suite exists to guarantee for a critical claim (C3, hop authentication) — that a plausible-but-wrong alternate implementation cannot pass by agreeing with the real fix only on the inputs the suite happens to try. `04-build.md` (L52) explicitly names hostname-comparison near-misses as the exact class of mistake the anti-fake tests exist to catch, and the module's own second-pass changelog treats an analogous CA-trust gap as worth a dedicated new test — but the identical class of gap in the version check, one line away in the same function, was not looked for.

**To close:** add one test sending an out-of-set `tls_version` that a lexicographic-bound check would wrongly accept (for example `tls_version="9"` or `tls_version="1.30"`, with hostname and CA trust both otherwise valid) and assert `502`; update `README.md`'s fake count and `keys/2.2.md`'s lab section to record a sixth verified fake.

## Minor finding (not a blocker)

**Stale, unreferenced `cache.py` files in both `vulnerable/` and `fixed/`.** `labs/2.2/2.2-request-path/vulnerable/cache.py` and `fixed/cache.py` (a two-function `cache_put`/`cache_get` pair, predating this deepen pass — confirmed via `git log --follow`, both files exist since well before the Tier-2 rewrite) are never imported by `app.py`, never referenced by any test, and never mentioned in `README.md` or any lesson. They appear to be leftover Tier-1 lab code the Tier-2 upgrade should have deleted. Both files are safe (no real secrets, no network I/O — `fixed/cache.py`'s own key shape, `(path, tenant)`, is consistent with the module's real fix, so they are not actively misleading about the *mechanism*), but they are dead weight a future maintainer could mistake for exercised code. Worth a one-line cleanup (`git rm`) alongside the blocker fix above; not itself a reason to withhold a grant.

## Shared `content/standards/pins.yaml` — checked against `origin/deepen/2.3`'s parallel edit

Both branches touch the same `owasp-asvs-5.0.0` pin entry for a related but independently-discovered dead-URL fix. On `deepen/2.2` (this branch): `reviewedAt: "2026-09-22"`, `url: "https://owasp.org/projects/asvs"`, notes ending with 2.2's own dead-URL account. On `origin/deepen/2.3` (commit `e43c153`): `reviewedAt: "2026-09-23"`, the **same** corrected `url`, notes ending with 2.3's own dead-URL account (2.3 independently found and fixed the identical dead URL, for its own citations, one day "later").

**No factual contradiction** — both branches agree on the corrected URL and both agree it is genuinely fixed. But this **will produce a merge conflict** (different `reviewedAt` values on the same field, and two divergent `notes` strings each claiming to be the one that fixed this exact dead link) when either branch merges after the other. Per this task's scope, I am reporting this for the calling session to resolve, not attempting a merge myself — whichever branch merges second will need its notes rewritten to say "already fixed by module 2.3/2.2's pass" rather than repeating a "found and fixed" claim for a URL that will already be corrected on `main`.

## Verdict

- [ ] `quality: developing`
- [ ] `quality: competent`
- [ ] `depth: publishable`
- [ ] `quality: transfer-ready`

**None granted.** The lesson prose (all eight lessons genuinely exceed the 900-word bar with real derivation, not padding), the request-path model, the five teaching claims, the coverage-contract and teaching-claims citations (fully correct, a first for this defect class among the modules I have precedent for), the standards work (all nine ASVS citations and both RFCs verified accurate against live sources, the dead-URL fix genuine), and the metadata honesty are all independently confirmed and genuinely strong — this is not a thin or padded pass, and the module's own second-pass discovery of the `is None`/`is not True` gap is real, precisely as claimed, and well-tested.

But the Blocker above is a real, demonstrated hole in the property the lab's anti-fake suite exists to certify for a critical claim (C3): a plausible alternate "fix" for the version check passes all fourteen tests, including every anti-fake test written for this module, exactly the same class of failure 0.2's review used to withhold every grant for its own module. Per this task's governing instructions, a real blocker withholds every grant regardless of how strong the surrounding work is, and I am not treating "the module already found and fixed one gap in this function" as license to skip looking for a second one in the same function — which is exactly what turned up here.

**Lab-safety review: passes, no findings**, recorded here so a re-review does not have to redo it. Per the governance rule, this does not by itself authorize any grant while the blocker stands — both sides must pass, and the quality side does not.

The fix is narrowly scoped: one new test in `tests/test_tls_hop.py`, plus the `README.md`/`keys/2.2.md` count updates, and (optionally, per the minor finding) removing the two stale `cache.py` files. No lesson's argument needs to change, and no other claim's tests need re-verification — I would expect a re-review limited to the changed lines to close quickly, the same way 4.3's and 0.2's narrowly-scoped blockers did.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real blocker exists. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as this pass set them; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. An authoring agent may not write this line, and neither may an independent review that found a live blocker.

## Addendum, 2026-09-23 — re-review at `2318785`: original blocker closed, but a second, same-shape gap and a miscounted fake remain open

**Independence statement.** I am a separate re-review pass, same day, on the fix commit `2318785` ("Close 2.2's blocker: TLS-version anti-fake gap, stale counts, dead files"). I read the review above in full before touching any file, then verified every claim by construction, not by trusting the diff or the changelog's own description of itself. Every file I edited to construct a fake was restored byte-for-byte afterward (`diff` against a pre-edit copy showed no difference, and `git status --porcelain` was empty each time) before I moved to the next check.

**Original blocker — CLOSED, verified by reconstructing the exact documented fake.** I edited `fixed/app.py`'s version check to `if tls_version < "1.2": return False` (replacing `if tls_version not in _ACCEPTED_TLS_VERSIONS: return False`) and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 14 passed
FAILED tests/test_tls_hop.py::test_anti_fake_version_string_that_sorts_high_is_still_rejected
```

Exactly and only the new test fails, as claimed. Restored, confirmed clean.

**Full pass/fail counts, re-run myself:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
9 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
15 passed
```

Matches the claimed 9/15 fail, 15/15 pass.

**A second, independently-constructed adversarial fake for the same version check also passes the entire 15-test suite, including the test just added to close the first gap.** I replaced the real fix's exact set membership with a plausible "forward-compatible allow-list" mistake — a maintainer's guess that a future point release should also be accepted:

```python
if tls_version not in {"1.2", "1.3", "1.4"}:   # real: not in _ACCEPTED_TLS_VERSIONS ({"1.2", "1.3"})
    return False
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
15 passed
```

**All fifteen tests pass**, including `test_anti_fake_version_string_that_sorts_high_is_still_rejected`. No test in `test_tls_hop.py` sends any version other than `"1.1"`, `"1.2"`, `"1.3"`, or `"1.9"` (confirmed by grep), so nothing distinguishes "exact membership in `{"1.2", "1.3"}`" from "membership in `{"1.2", "1.3", "1.4"}`" — a version this module's own claim, README, and lessons never name as accepted is silently treated as trustworthy. This is the identical *shape* of gap the fix commit just closed for one alternate implementation (lexicographic bound) but not for another (a wider but still-wrong allow-list), in the same function, one line away, immediately after a pass whose entire purpose was to close exactly this class of gap. Restored, confirmed clean (`diff` against pre-edit copy: no difference; `git status --porcelain`: empty).

**A specific recomputed fake count in the fix's own changelog, `README.md`, and `lessons/05-verify.md` is wrong — not by one, as a sibling module's review once caught, but by overclaiming a failure that does not occur.** All three describe the cache-only fix (header override left open) as: *"12 of 15, fails C2's forbidden-outcome test, its malformed-credential case, and its anti-fake test"* (three named failures). I reconstructed this exact fake — `fixed/app.py`'s `_resolve_company` restored to `vulnerable/app.py`'s header-reading behavior, cache key left untouched — and ran the full suite with `-v`:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -v
...
tests/test_cache_key.py::test_forwarded_company_header_cannot_grant_a_different_companys_note FAILED
tests/test_cache_key.py::test_unregistered_api_key_with_a_company_header_is_denied PASSED
tests/test_cache_key.py::test_anti_fake_forwarded_header_with_fresh_never_elsewhere_used_values FAILED
...
2 failed, 13 passed
```

**The malformed-credential case (`test_unregistered_api_key_with_a_company_header_is_denied`) passes against this fake — it does not fail.** Reading the fake's own logic explains why: `_resolve_company` checks `if api_key not in API_KEYS: return None` *before* it ever looks at `x_company`, so an unregistered key (`"key-does-not-exist"`) is rejected on the first branch regardless of any header. The header override this fake leaves open is only reachable through a *registered* key, which is exactly what the forbidden-outcome test and its anti-fake pair exercise — the malformed-credential case tests a different, already-correct code path in both variants. The true count is **13 of 15 pass, 2 failures**, not 12 of 15 with 3 failures, in the fix's own changelog entry (`module.yaml` L236, L251), `labs/2.2/2.2-request-path/README.md`, and `content/modules/2/2.2/lessons/05-verify.md`. This is the same failure mode the task's background names for module 2.3's own review ("6 of 9" that was actually "7 of 9") — a self-reported recomputation that does not match the suite it claims to describe. (For comparison, I also reconstructed the header-only fix — cache left keyed on path alone, `_resolve_company` fixed — and got exactly the claimed 13 of 15, 2 failures, both in `test_other_company_does_not_receive_cached_body` and its anti-fake pair: that count is correct.)

**Several stale test-count references the fix commit's own message claims to have updated were not, in fact, all updated.** Grepping the module and lab tree for lingering `"14 of 14"` / `"6 of 14"` / `"eight"` phrasing after the fix commit found:

- `labs/2.2/2.2-request-path/README.md` L29: `python3 -m pytest ... --impl fixed        # 14 of 14 pass` — still says 14, should say `15 of 15 pass` (the line immediately above it, for `vulnerable`, was correctly updated to `# 6 of 15 pass` in the same commit, making this an inconsistency within one three-line code block, not merely an old reference nobody looked at).
- `content/modules/2/2.2/lessons/05-verify.md` L25 (Mermaid diagram): `-->|"6 of 14 pass"|` — still says 14, should say `6 of 15 pass`.
- `content/modules/2/2.2/lessons/05-verify.md` L28 (same diagram): `-->|"14 of 14 pass"|` — still says 14, should say `15 of 15 pass`.
- `content/modules/2/2.2/lessons/05-verify.md` L35: "the eight that should fail do fail for the stated reason" — should say "nine" (the vulnerable variant now fails 9 of 15, confirmed above).
- `content/assessment/keys/2.2.md` L12: `` `labs/2.2/2.2-request-path/fixed/` must pass all 14. `` — still says 14, should say 15.

The fix commit's message claims "Updated every '14 tests' reference across spec.md, 05-verify.md, items.md, rubric.md, the lab README, and the isolated key" — `items.md` and `rubric.md` were in fact updated correctly (both read "9 of 15 failing" / "15 of 15 passing"), and one paragraph each in `05-verify.md`, the README, `keys/2.2.md`, and `module.yaml`'s changelog were updated with new, mostly-correct prose (save the miscount above) — but the claim that *every* reference was updated is itself not accurate; five instances across two files were missed.

**Confirmed still true, unaffected by the above:** the two stale `vulnerable/cache.py` / `fixed/cache.py` files are genuinely deleted (`find` for `cache.py` under `labs/2.2` and `content/modules/2/2.2` returns nothing) and nothing else in either tree references them (grepped both trees for `cache\.py`; the only hit is `module.yaml`'s own changelog line describing the deletion). Mechanical gates pass: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`; `npm --prefix site run build` (after `npm install --prefer-offline --no-audit --no-fund` in a fresh `site/` checkout) → compiles and generates all 643 static pages successfully.

**Verdict: neither gate is granted.** The second adversarial fake is the same class of hole the original blocker described — a plausible alternate implementation of the version check that agrees with the real fix on every input the suite happens to try and disagrees on inputs it does not — sitting one line away in the same function, discovered immediately after a pass whose stated purpose was closing exactly this class of gap for this check. That alone withholds every grant under this task's governing rule. The miscounted "12 of 15 / three failures" claim is a second, independent problem: the module's own account of its verification work does not match what the suite actually does, in a place a reviewer might otherwise treat as already re-verified because the surrounding numbers were recomputed rather than incremented. The five leftover stale references are minor by comparison but mean the fix commit's own summary of its completeness is not fully accurate either.

**To close:** (1) add a test sending a `tls_version` that is a plausible-but-wrong *addition* to the accepted set (for example `tls_version="1.4"` or `"1.30"`, hostname and CA trust otherwise valid) and assert `502`, isolating "exact membership in the two-element set" from "membership in some superset that also contains 1.2 and 1.3"; (2) correct the cache-only-fix count everywhere it appears (`module.yaml`'s changelog, `README.md`, `lessons/05-verify.md`) to 13 of 15 / 2 failures, and drop the "malformed-credential case" from the list of what it fails; (3) fix the five remaining stale references listed above. None of this requires re-authoring any lesson's argument or re-verifying any other claim's tests.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because real, demonstrated problems remain. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. An independent review that found a live blocker may not write this line.

## Addendum, 2026-09-23 — third-round re-review at `79b8941`: both round-2 findings genuinely closed, but a new, third gap found by independent adversarial construction

**Independence statement.** I am a third, separate re-review pass, same day, on commit `79b8941` ("Close 2.2's second re-review findings: forward-compatible version gap, arithmetic"), now `HEAD` of `deepen/2.2` (fast-forwarded from `origin/deepen/2.2`; the local branch had been stale at `b277893`). I read both addenda above in full before touching any file. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed both by `diff` against a pre-edit copy and by `git status --porcelain` returning empty — before moving to the next check, every time.

**Round 2's first finding — CLOSED, verified by reconstructing the exact fake from scratch.** Before reading the fix's own description of what it does, I built the fake myself: `{"1.2", "1.3", "1.4"}` in place of `_ACCEPTED_TLS_VERSIONS` in `fixed/app.py`'s version check.

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 15 passed
FAILED tests/test_tls_hop.py::test_anti_fake_forward_compatible_allow_list_still_rejects_an_unaccepted_version
```

Fails exactly and only the test added to close this gap. Restored; `diff`/`git status --porcelain` both clean.

**Round 2's second finding (the miscounted cache-only fake) — CLOSED, verified by reading `vulnerable/app.py`'s real check order first, then reconstructing independently.** I read `vulnerable/app.py`'s `_resolve_company` myself (not the fix commit's account of it): it checks `if api_key not in API_KEYS: return None` first, and only then `if x_company: return x_company`. I built a fake `_resolve_company` for `fixed/app.py` in that exact order (key check first, header second) while leaving `_CACHE`'s `(note_id, company)` keying (C1) untouched, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -v
...
test_cache_key.py::test_forwarded_company_header_cannot_grant_a_different_companys_note FAILED
test_cache_key.py::test_unregistered_api_key_with_a_company_header_is_denied PASSED
test_cache_key.py::test_anti_fake_forwarded_header_with_fresh_never_elsewhere_used_values FAILED
2 failed, 14 passed
```

14 of 16, exactly the two failures the fix now claims (C2's forbidden-outcome test and its anti-fake pair); the malformed-credential case passes, exactly as claimed, because the key check runs and fails before the header is ever read. The corrected count (14 of 16, 2 failures — not round 1's own miscounted "12 of 15, 3 failures") is genuinely right this time. Restored; clean.

**A third, independently-constructed adversarial fake — for a different check than either of the first two rounds' findings — passes the entire current 16-test suite.** Per this task's instruction to look for a gap in a different part of the logic, I read all three of `fixed/app.py`'s functions end to end and targeted the hostname comparison in `hop_is_trustworthy`, which the two existing hostname anti-fake tests cover for a *prefix* fake (`presented_hostname.startswith(expected_hostname)`) and a *substring-anywhere* fake (`expected_hostname in presented_hostname`) — but not for a *suffix-without-boundary* fake, a distinct and equally plausible near-miss for "compare the hostnames":

```python
# real fixed code
return presented_hostname == expected_hostname
# adversarial fake
return presented_hostname.endswith(expected_hostname)
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
16 passed
```

**All sixteen tests pass, including both existing hostname anti-fake tests.** Neither existing test catches an `endswith` fake: the prefix test's presented hostname (`"notes-origin.securecollab.internal.attacker.example"`) does not *end with* the expected hostname (it ends with `.attacker.example`), and the substring-anywhere test's presented hostname (`"evil-origin.internal-mirror.example"`) likewise does not end with the expected hostname (`"origin.internal"`) — both were built to defeat a prefix or middle-substring check, not a suffix check, so an `endswith` fake sails through both untouched, and through every other test in the file (all of which either use identical hostnames on both sides or a hostname-mismatch case that also happens not to share a suffix).

I then confirmed this is a real, exploitable behavioral difference from the true fix, not merely an untested-but-harmless corner: I ran the fake directly through the FastAPI `TestClient` with `expected_hostname="origin.internal"`, `presented_hostname="evil-origin.internal"`, `cert_trusted_ca=True`, `tls_version="1.3"` — a classic domain-suffix-without-boundary case (`"evil-origin.internal"` ends with the literal characters `"origin.internal"` with no separating dot or hyphen boundary, so it is not a subdomain of the expected host, it merely shares a text suffix). The fake returns `200 {"relayed": True}`; the real `==` check would return `502` for this exact input, since the strings are not equal. This is the identical *shape* of defect this module's own review has now found three times running in the same three-check function (`hop_is_trustworthy` / the version check) — a plausible alternate comparison that agrees with the real fix on every input the suite happens to try and disagrees on inputs it does not — this time in the hostname check rather than the trust-state or version check. Restored `fixed/app.py`; confirmed via `diff` against the pre-edit copy (no difference) and `git status --porcelain` (empty).

**Other checks performed, all confirmed clean:**

- Reconstructed two more of the five other pre-existing documented fakes myself: the header-only fix (cache reverted to path-only keying, `_resolve_company` left fixed) gives 14 of 16, failing exactly `test_other_company_does_not_receive_cached_body` and its anti-fake pair, as documented; the hostname-substring-anywhere fake (`expected_hostname in presented_hostname`) gives 14 of 16, failing both hostname anti-fake tests, as documented. Combined with the cache-only-fix and `{"1.2","1.3","1.4"}` reconstructions above, four of the seven documented fakes are now independently re-verified against the current 16-test suite by me, in addition to the two rounds before this one.
- Full lab matrix, run myself: `python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q` → `10 failed, 6 passed`; `--impl fixed -q` → `16 passed`. Matches the claimed vulnerable-10/16-fail, fixed-16/16-pass shape exactly. `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`.
- Grepped `spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, the lab `README.md`, and `content/assessment/keys/2.2.md` for stale test-count phrasing (`"14 of 14"`, `"6 of 14"`, `"8 of 14"`, `"15 of 15"`, `"of 14 pass"`, `"of 14 fail"`, `"eight"`). Every live, current-tense claim in these files now correctly says "16" (`items.md`, `rubric.md`, `keys/2.2.md`, README, `05-verify.md`'s Mermaid diagram and prose all read 10-of-16/16-of-16 or count the fakes against "the current 16-test suite"). The only remaining occurrences of "14"/"15"/"12"/"eight"-style numbers are inside `module.yaml`'s own changelog, correctly narrating what past passes found and fixed at the time — historical record, not a live claim, and not a defect.
- Mechanical gates, run myself: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`; `python3 scripts/lint_content.py` (full repo) → `0 error(s), 0 warning(s), 2182 suppressed by baseline`; `npm --prefix site run build` (after `npm install --prefer-offline --no-audit --no-fund` in a fresh `site/` checkout) → compiles and generates all static pages successfully, no errors. Key isolation: no `href="...assessment/keys..."` anywhere in `site/out`.

**Verdict: neither gate is granted, for the third time running.** Round 2's two findings are genuinely closed — I verified both by independent reconstruction, not by trusting the fix commit's own account, and both hold up. But this task's explicit instruction was not to treat "the module already found and fixed gaps in this function twice" as license to stop looking, and a third construction — in a different check (the hostname comparison, not the version check) — found a real, demonstrated, exploitable gap the current 16-test suite does not catch. Per this repo's governing rule, a real blocker withholds every grant regardless of how much of the surrounding work is independently confirmed correct, and regardless of how many prior rounds already closed other findings in the same review cycle.

**To close:** add one test in `tests/test_tls_hop.py` sending a `presented_hostname` that ends with `expected_hostname` but is not equal to it and has no subdomain-boundary character (`.` or the start of string) immediately before the shared suffix — for example `expected_hostname="origin.internal"`, `presented_hostname="evil-origin.internal"`, with `cert_trusted_ca=True` and `tls_version="1.3"` — and assert `502`. This isolates "the presented hostname is exactly the expected hostname" from "the presented hostname merely ends with the expected hostname's characters," the same way the existing prefix and substring-anywhere anti-fake tests isolate the other two plausible near-misses of hostname comparison. Update `README.md`'s and `keys/2.2.md`'s fake counts and lists to record an eighth verified fake once this is added, and re-verify the whole family (prefix, substring-anywhere, suffix-without-boundary) is now covered by three, not two, anti-fake tests.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A third re-review that found a live blocker may not write this line, and finding two prior rounds' issues closed does not change that.

## Addendum, 2026-09-23 — fourth-round re-review at `5961912`: round-3 finding genuinely closed, but a fourth, structurally distinct gap found in the same version check

**Independence statement.** I am a fourth, separate re-review pass, same day, on commit `5961912` ("2.2: close third review round's hostname endswith gap, plus two cross-failures it surfaced"), which is both the tip of `origin/deepen/2.2` and `HEAD` of the branch I reviewed from. I read all three addenda above in full before touching any file. I worked from a worktree already checked out at this exact commit (`/tmp/fix3-2.2`, branch `fix3-2.2-blockers`, tracking `origin/deepen/2.2` with a clean tree) rather than trusting any cached state. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum`/`diff` against a pre-edit copy and by `git status --porcelain` returning empty — before I moved to the next check, every time.

**Round 3's finding — CLOSED, verified by reconstructing the exact fake from scratch before reading the fix's own description.** I read `tests/test_tls_hop.py` end to end first, then wrote `return presented_hostname.endswith(expected_hostname)` in place of `return presented_hostname == expected_hostname` in `fixed/app.py`'s `hop_is_trustworthy`, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 16 passed
FAILED tests/test_tls_hop.py::test_anti_fake_hostname_suffix_without_boundary_is_rejected
```

Fails exactly and only the test added to close this gap. Restored; `diff` against the pre-edit copy showed no difference, `git status --porcelain` was empty.

**Full pass/fail counts, re-run myself, matching every file's own claim exactly:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
11 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
17 passed
```

This matches, word for word and number for number, `labs/2.2/2.2-request-path/README.md` L28-29 ("6 of 17 pass" / "17 of 17 pass"), `content/modules/2/2.2/lessons/05-verify.md`'s Mermaid diagram ("6 of 17 pass" / "17 of 17 pass"), `content/modules/2/2.2/assessment/items.md` L104 and `assessment/rubric.md` L13 ("11 of 17 failing" / "17 of 17 passing"), and `content/assessment/keys/2.2.md` L11-12 ("must fail 11 of 17" / "must pass all 17"). `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`. Every vulnerable failure is a plain assertion on the security property, confirmed by name against the eleven `FAILED` lines above, which match `keys/2.2.md` L11's own enumeration (both `test_cache_key.py` forbidden-outcome/anti-fake pairs for C1 and C2, plus `test_tls_hop.py`'s forbidden-outcome, boundary, and all five anti-fake tests for C3) exactly.

**Coverage-contract and teaching-claims citations — re-derived independently from `items.md`'s own `Claim assessed` tags, cell by cell, found fully correct again.** Map built from `items.md`: C1 → #1, #4, #6, #7, #8; C2 → #2, #4, #7; C3 → #3, #7, #8; C4 → #5, #7; C5 → #6, #7, #8. Checked against `spec.md`'s teaching-claims table (L45-49): every one of these five cells matches exactly, with no addition or omission on either side. No regression from the round-1 finding that this defect class, common elsewhere in this remediation effort, is genuinely clean here.

**Metadata honesty and shared `pins.yaml` — both re-confirmed, no new problem.** `content/modules/2/2.2/module.yaml` L7 still reads `status: draft`; no `reviewer`, `depth`, `quality`, `lastReviewedAt`, or `nextReviewAt` field has been set by any authoring or fix pass. `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry on this branch still reads `reviewedAt: "2026-09-22"` with the corrected `url: "https://owasp.org/projects/asvs"`; `origin/deepen/2.3`'s parallel entry (fetched fresh, not from memory) reads `reviewedAt: "2026-09-23"` with the identical corrected URL and its own independent dead-link narrative. This is the exact same cross-branch note-text conflict the first-round addendum already recorded — same field, same URL, same shape of divergence, one day apart — not a new or worsened problem. I did not touch it, per this task's instruction.

**Mechanical gates, run myself:** `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK` (shown above).

**A fourth, independently-constructed adversarial fake — for the version check specifically, the same check round 3's session confirmed clean and round 2 had already found two prior gaps in — passes the entire current 17-test suite, including both existing version anti-fake tests.** Per this task's explicit instruction to construct my own candidate rather than only replay the documented ones, I re-read all three checks in `hop_is_trustworthy` character by character and asked, for the version check specifically, whether a plausible good-faith author who reads `_ACCEPTED_TLS_VERSIONS = {"1.2", "1.3"}` as "accept protocol versions from 1.2 through 1.3" might implement it as a *numeric range comparison* rather than exact set membership — a natural instinct for anyone used to comparing version numbers arithmetically instead of as opaque strings, and structurally distinct from both of round 2's fakes (a lexicographic string bound, and a hand-written wider string set) and from round 3's fake (a hostname `endswith`, in a different check entirely):

```python
# real fixed code
if tls_version not in _ACCEPTED_TLS_VERSIONS:
    return False
# adversarial fake, in its place
try:
    major_str, minor_str = tls_version.split(".", 1)
    version_num = float(f"{major_str}.{minor_str}")
except ValueError:
    return False
if not (1.2 <= version_num <= 1.3):
    return False
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
17 passed
```

**All seventeen tests pass, including every anti-fake test in the file — both version anti-fakes and all three hostname anti-fakes.** The reason is that every version string any test in `test_tls_hop.py` currently sends — `"1.1"`, `"1.2"`, `"1.3"`, `"1.4"`, `"1.9"` — happens to parse to a `float` whose ordering against the `[1.2, 1.3]` numeric range agrees with membership in the two-element string set `{"1.2", "1.3"}`: `float("1.1")=1.1` (below range, reject, matches), `float("1.2")=1.2` and `float("1.3")=1.3` (in range, accept, matches), `float("1.4")=1.4` and `float("1.9")=1.9` (above range, reject, matches). Nothing in the suite sends a version string that is numerically inside `[1.2, 1.3]` but not a member of `{"1.2", "1.3"}` as a string — which is exactly what a version like `"1.30"` or `"1.20"` is: `float("1.30") == 1.3`, so the fake's range check accepts it, while `"1.30" not in {"1.2", "1.3"}` is `True`, so the real fixed code rejects it. I confirmed this is a real, exploitable behavioral divergence, not a harmless corner, by running both implementations directly through the FastAPI `TestClient` with `expected_hostname` and `presented_hostname` both `"a.securecollab.internal"`, `cert_trusted_ca=True`, `tls_version="1.30"`:

```
$ python3 -c '...'   # fake in place
tls_version=1.30 -> 200
$ python3 -c '...'   # real fixed code, restored
REAL fixed check, tls_version=1.30 -> 502
```

The fake relays a hop presenting `"1.30"` as trustworthy (`200`); the real fix correctly refuses it (`502`) because `"1.30"` was never a version this fixture, this module's `_ACCEPTED_TLS_VERSIONS`, its README, or its lessons name as accepted — it is not TLS 1.2 or TLS 1.3, it is a string that a naive numeric parse conflates with `1.3`. This is the identical *shape* of defect this module's own review has now found four times running in the same three-check function — a plausible alternate implementation that agrees with the real fix on every input the current suite happens to try and disagrees on inputs it does not — and it is the *third* distinct fake found specifically in the version check (after the lexicographic-bound fake and the forward-compatible-allow-list fake, both closed in round 2), showing that closing two gaps in one check is still not evidence the check is exhausted. I restored `fixed/app.py` immediately afterward: `md5sum` before and after matched (`ebdf983ea6753464cffb6dee0a669513`), `diff` against the pre-edit copy showed no difference, and `git status --porcelain` was empty before I moved on.

**Other checks performed, all confirmed clean, no regression from round 3:**

- The trusted-CA check (`if cert_trusted_ca is not True: return False`) has a domain of exactly three values (`True`, `False`, `None`, per the function's `bool | None` signature), and the current suite tests all three with distinct expected outcomes (`True` → 200 via the normal-case test; `False` → 502 via the explicitly-untrusted-CA anti-fake test; `None` → 502 via the unknown-trust-state test). Any function reproducing this truth table over a three-value domain is extensionally identical to `is not True` on every possible input — there is no room left for a fifth fake in this specific check, unlike the hostname and version checks, whose domains are unbounded strings.
- Reconstructed two more of the module's own eight documented fakes myself, independent of the round-3 session's reconstructions: the trust-state `is None` fake (`if cert_trusted_ca is None: return False`) gives `16 passed, 1 failed` (`test_explicitly_untrusted_ca_is_rejected_even_with_matching_hostname_and_version`), and the hostname-prefix fake (`presented_hostname.startswith(expected_hostname)`) gives `16 passed, 1 failed` (`test_anti_fake_hostname_prefix_match_is_rejected`) — both exactly as `README.md` and `keys/2.2.md` claim.
- Grepped `spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, the lab `README.md`, and `content/assessment/keys/2.2.md` for stale test-count phrasing left over from any prior round (`"16 of 16"`, `"15 of 15"`, `"14 of 14"`, `"of 16 pass"`, `"of 16 fail"`). No hits outside `module.yaml`'s own changelog, which correctly narrates past counts as history, not a live claim.

**Verdict: neither gate is granted, for the fourth time running.** Round 3's finding is genuinely closed — I verified this by independent reconstruction, not by trusting the fix commit's own account, and it holds up, as do all counts, citations, and mechanical gates checked above. But a fourth construction, targeted at the one check (the version check) that had already absorbed two prior fixes in round 2, found a real, demonstrated, exploitable gap the current 17-test suite does not catch: a numeric-range parse of the TLS version string, a natural good-faith implementation choice for anyone thinking of "1.2 through 1.3" as a numeric interval rather than an opaque two-element string set. Per this repo's governing rule, a real blocker withholds every grant regardless of how much surrounding work is independently confirmed correct, and regardless of how many prior rounds already closed other findings — including two findings in this exact same check.

**To close:** add one test in `tests/test_tls_hop.py` sending a `tls_version` string that is numerically inside `[1.2, 1.3]` when parsed as a float but is not a member of `_ACCEPTED_TLS_VERSIONS` as a string — for example `tls_version="1.30"` or `tls_version="1.25"`, with hostname and CA trust both otherwise valid — and assert `502`. This isolates "the version string is exactly `\"1.2\"` or `\"1.3\"`" from "the version string parses to a float numerically between 1.2 and 1.3," a fourth and distinct near-miss shape from the two round-2 already covers (a lexicographic string bound, and a hand-written wider string set). Update `README.md`'s and `keys/2.2.md`'s fake counts and lists to record a ninth verified fake once this is added, and note in whichever file tracks the version check's history that three, not two, structurally distinct fakes have now been found and closed for that one line.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A fourth re-review that found a live blocker may not write this line, and finding three prior rounds' issues closed does not change that.

## Addendum, 2026-09-23 — fifth-round re-review at `24c221d`: round-4 finding genuinely closed, but a fifth, structurally distinct gap found in the same version check

**Independence statement.** I am a fifth, separate re-review pass, on commit `24c221d` ("2.2: close fourth review round's numeric-range TLS-version gap, plus the cross-failure it surfaced"), which is `HEAD` of `origin/deepen/2.2`. I checked out this exact commit fresh (`git checkout -B deepen/2.2-review origin/deepen/2.2`) into my own worktree rather than trusting any prior session's cached state, and confirmed the working tree was clean before touching anything. I read all four addenda above in full before touching any file. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum` (`ebdf983ea6753464cffb6dee0a669513` before and after) and by `diff` against a pre-edit copy showing no difference, and by `git status --porcelain` returning empty — before I moved to the next check, every time.

**Round 4's finding — CLOSED, verified by reconstructing the exact fake from scratch before reading the fix's own description.** I read `tests/test_tls_hop.py` end to end first, then wrote a numeric-range parse (`float`, `1.2 <= version_num <= 1.3`) in place of `if tls_version not in _ACCEPTED_TLS_VERSIONS: return False` in `fixed/app.py`'s `hop_is_trustworthy`, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 17 passed
FAILED tests/test_tls_hop.py::test_anti_fake_version_string_that_parses_in_range_is_still_rejected
```

Fails exactly and only the test added to close this gap. Restored; `diff` and `git status --porcelain` both clean.

**Full pass/fail counts, re-run myself, matching every file's own claim exactly:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
12 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
18 passed
```

This matches, word for word and number for number, `labs/2.2/2.2-request-path/README.md` L28-29 ("6 of 18 pass" / "18 of 18 pass"), `content/modules/2/2.2/lessons/05-verify.md`'s Mermaid diagram ("6 of 18 pass" / "18 of 18 pass"), `content/modules/2/2.2/assessment/items.md` L104 and `assessment/rubric.md` L13 ("12 of 18 failing" / "18 of 18 passing"), and `content/assessment/keys/2.2.md` L11 ("must fail 12 of 18" / "must pass all 18"). `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`.

**A fifth, independently-constructed adversarial fake — for the version check specifically, the same check rounds 2 and 4 had already found two prior gaps in — passes the entire current 18-test suite, including all four existing version anti-fake tests.** Per this task's explicit instruction to construct my own candidate and to consider "normalizing/stripping the string before comparing" as one plausible shape, I built exactly that: a good-faith author defensively stripping incidental whitespace (a trailing newline from a header value, a stray leading space) before checking set membership, something many real HTTP-adjacent codebases do reflexively for header-derived strings:

```python
# real fixed code
if tls_version not in _ACCEPTED_TLS_VERSIONS:
    return False
# adversarial fake, in its place
if tls_version.strip() not in _ACCEPTED_TLS_VERSIONS:
    return False
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
18 passed
```

**All eighteen tests pass, including every anti-fake test in the file.** None of the six distinct version strings any test in `test_tls_hop.py` sends (`"1.1"`, `"1.2"`, `"1.3"`, `"1.4"`, `"1.9"`, `"1.30"`) contains any whitespace, so `.strip()` is a no-op on every one of them and the fake's behavior is byte-identical to the real fix on the entire existing suite. I confirmed this is a real, exploitable behavioral divergence, not a harmless corner, by running both implementations directly through the FastAPI `TestClient` with `expected_hostname` and `presented_hostname` both `"a.securecollab.internal"`, `cert_trusted_ca=True`, `tls_version="1.2\n"` (a version string with a trailing newline — the kind of incidental whitespace a real header-parsing or logging pipeline can introduce):

```
fake in place, tls_version='1.2\n' -> 200
```

I then restored the real fixed code and confirmed it correctly returns `502` for the identical input (`"1.2\n" not in {"1.2", "1.3"}` is `True`, since `"1.2\n" != "1.2"`). The fake relays a hop presenting a version string that is not one of the two accepted strings as trustworthy; the real fix correctly refuses it. This is the identical *shape* of defect this module's own review has now found five times running in the same three-check function — a plausible alternate implementation that agrees with the real fix on every input the current suite happens to try and disagrees on inputs it does not — and it is the *fourth* distinct fake found specifically in the version check (after the lexicographic-bound fake, the forward-compatible-allow-list fake, and the numeric-range fake, all closed in rounds 2 and 4), showing that closing three gaps in one check is still not evidence the check is exhausted. I restored `fixed/app.py` immediately afterward: `md5sum` before and after matched (`ebdf983ea6753464cffb6dee0a669513`), `diff` against the pre-edit copy showed no difference, and `git status --porcelain` was empty before I moved on.

**Other checks performed, all genuinely adversarial, all confirmed clean, no regression from round 4:**

- **Hostname check, fourth shape attempted.** Tried case-insensitive comparison (`presented_hostname.casefold() == expected_hostname.casefold()`) and trailing-dot/FQDN normalization (`presented_hostname.rstrip(".") == expected_hostname.rstrip(".")`). Neither passes the full suite as a *plausible-but-wrong* fake in the sense the task asks for, because I could not find an input among the current tests or a newly constructed one where either normalization diverges from `==` on a case this module's own hostnames would plausibly carry (all hostnames used throughout are already lowercase with no trailing dot, and introducing one is testing a different property — DNS FQDN equivalence — that this module's spec and lessons do not claim `hop_is_trustworthy` handles). I did not find a fourth exploitable hostname gap distinct from the three already closed (prefix, substring-anywhere, suffix-without-boundary).
- **Trust-CA check, re-verified the three-value-domain argument myself rather than accepting it.** `hop_is_trustworthy`'s signature is `cert_trusted_ca: bool | None`, and grepping the function body confirms the only comparison is `if cert_trusted_ca is not True`. Enumerated all three possible values by hand: `True` → check passes (proceeds to version/hostname); `False` → check fails (`is not True` is `True`) → rejected; `None` → check fails (`is not True` is `True`) → rejected. I tried constructing a fake using `bool()` coercion (`if not bool(cert_trusted_ca): return False`) on the theory that a good-faith author might coerce rather than compare identity — `bool(True)` is `True` (passes, matches), `bool(False)` is `False` (rejected, matches), `bool(None)` is `False` (rejected, matches) — extensionally identical on this domain, so it is not a distinguishable fake, confirming (not merely accepting) the existing addendum's argument that this check's three-value domain is exhausted by the current three tests.
- **C1 (cache key) and C2 (forwarded identity), re-attempted independently.** Tried a fake that keys the cache on `(note_id, company.lower())` (case-folding company before keying) — no test uses mixed-case company identifiers (`companyA`, `companyB`, `companyZ`, `companyQ` are used consistently), so this does not diverge from the real `(note_id, company)` key on the current suite, and is not a distinguishable fake. Tried a fake for `_resolve_company` that reads `x_company` only when it exactly equals the caller's own bound company (a no-op in practice, since the real fix already ignores `x_company` entirely) — behaviorally identical to the real fix on every input, not a fake at all. Tried sending `X-Company` with a company value containing leading/trailing whitespace (`" companyB"`) to see if any normalization gap exists in the real `_resolve_company` — the real fixed function never reads `x_company` at all, so no whitespace-normalization fake is even meaningful here; this line of attack applies only to the version check, where whitespace-sensitive comparison is actually live. Found no new C1/C2 gap.
- Reconstructed the round-4 numeric-range fake and the round-2 lexicographic-bound fake once each, independent of the round-3/4 sessions' own reconstructions, and got the documented counts (17 of 18 and 15 of 18 respectively, failing exactly the tests each README/keys entry names).
- Grepped `spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, the lab `README.md`, and `content/assessment/keys/2.2.md` for stale test-count phrasing left over from any prior round (`"17 of 17"`, `"16 of 16"`, `"15 of 15"`, `"14 of 14"`, `"of 17 pass"`, `"of 17 fail"`, `"eight fakes"`, `"nine fakes"` used inconsistently). No hits outside `module.yaml`'s own changelog, which correctly narrates past counts as history, not a live claim; every live, current-tense reference reads "18" and "nine fakes" consistently (the ninth being the round-4 numeric-range fake), and no file claims a count that mismatches what I ran myself.
- Coverage-contract and teaching-claims citations re-derived independently from `items.md`'s own `Claim assessed` tags, cell by cell: C1 → #1, #4, #6, #7, #8; C2 → #2, #4, #7; C3 → #3, #7, #8; C4 → #5, #7; C5 → #6, #7, #8 — matches `spec.md`'s teaching-claims table exactly, no addition or omission, no regression from round 4's finding that this defect class is clean here.
- Metadata honesty and shared `pins.yaml`, re-confirmed: `content/modules/2/2.2/module.yaml` still reads `status: draft`, no `reviewer`/`depth`/`quality`/`lastReviewedAt`/`nextReviewAt` set by any authoring or fix pass. `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry on this branch still reads `reviewedAt: "2026-09-22"` with the corrected URL; `origin/deepen/2.3`'s parallel entry (fetched fresh via `git fetch origin`, not from memory) still reads `reviewedAt: "2026-09-23"` with the identical corrected URL and its own independent narrative — the same cross-branch note-text conflict already on record four rounds running, one day apart, not a new or worsened problem. Not touched, per this task's instruction.
- Mechanical gates, run myself: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.

**Verdict: neither gate is granted, for the fifth time running.** Round 4's finding is genuinely closed — I verified this by independent reconstruction, not by trusting the fix commit's own account, and it holds up, as do all counts, citations, and mechanical gates checked above. But a fifth construction, targeted at the one check (the version check) that had already absorbed three prior fixes across rounds 2 and 4, found a real, demonstrated, exploitable gap the current 18-test suite does not catch: a whitespace-stripping normalization of the TLS version string before set-membership comparison, a natural defensive habit for anyone treating `tls_version` as an ordinary header-derived string rather than an opaque, already-canonical token. Per this repo's governing rule, a real blocker withholds every grant regardless of how much surrounding work is independently confirmed correct, and regardless of how many prior rounds already closed other findings — including three findings in this exact same check.

**To close:** add one test in `tests/test_tls_hop.py` sending a `tls_version` string that carries incidental leading/trailing whitespace around an otherwise-non-accepted value (for example `tls_version="1.2 "` is NOT sufficient by itself, since `"1.2 ".strip() == "1.2"` which the *real* fix would also need to reject and currently does — the isolating case is a version whose *stripped* form is still not in `_ACCEPTED_TLS_VERSIONS`, e.g. `tls_version=" 1.9"` or `tls_version="1.30\n"`, with hostname and CA trust both otherwise valid) and assert `502`; additionally, to specifically catch the whitespace-around-an-accepted-value case (`"1.2\n"`, `" 1.3"`), add a companion test asserting `502` for those too, since the real fixed code treats them as distinct strings from `"1.2"`/`"1.3"` and must reject them, while a `.strip()`-based fake would wrongly accept them. This isolates "the version string is exactly one of the two canonical strings, byte for byte" from "the version string, after discarding incidental whitespace, names one of the two accepted versions" — a fifth and distinct near-miss shape from the three round-2/round-4 already cover (a lexicographic string bound, a hand-written wider string set, and a numeric-range parse). Update `README.md`'s and `keys/2.2.md`'s fake counts and lists to record a tenth verified fake once this is added, and note in whichever file tracks the version check's history that four, not three, structurally distinct fakes have now been found and closed for that one line.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A fifth re-review that found a live blocker may not write this line, and finding four prior rounds' issues closed does not change that.

## Addendum, 2026-09-23 — sixth-round re-review at `1e6340f`: round-5 finding genuinely closed, but a sixth, structurally distinct gap found in the same version check

**Independence statement.** I am a sixth, separate re-review pass, on commit `1e6340f` ("2.2: close fifth review round's whitespace-strip TLS-version gap, plus two cross-failures it surfaced"), which is `HEAD` of `origin/deepen/2.2`. My worktree's default branch was stale (pointed at unrelated `main` history); I checked out this exact commit fresh (`git checkout -B deepen/2.2 origin/deepen/2.2`) and confirmed the working tree was clean before touching anything. I read all five addenda above in full before touching any file. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum` (`ebdf983ea6753464cffb6dee0a669513` before and after every edit) and by `git status --porcelain` returning empty — before I moved to the next check, every time.

**Round 5's finding — CLOSED, verified by reconstructing the exact fake from scratch before reading the fix's own description.** I read `tests/test_tls_hop.py` end to end first, then wrote `if tls_version.strip() not in _ACCEPTED_TLS_VERSIONS: return False` in place of the bare membership test in `fixed/app.py`'s `hop_is_trustworthy`, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 18 passed
FAILED tests/test_tls_hop.py::test_anti_fake_version_string_with_incidental_whitespace_is_rejected
```

Fails exactly and only the test added to close this gap. Restored; `md5sum`/`git status --porcelain` both clean.

**Full pass/fail counts, re-run myself, matching every file's own claim exactly:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
13 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
19 passed
```

This matches, word for word and number for number, `labs/2.2/2.2-request-path/README.md` L28-29 ("6 of 19 pass" / "19 of 19 pass"), `content/modules/2/2.2/assessment/items.md` L104 and `assessment/rubric.md` L13 ("13 of 19 failing" / "19 of 19 passing"), and `content/assessment/keys/2.2.md` L11 ("must fail 13 of 19" / "must pass all 19"). `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`. `content/modules/2/2.2/spec.md` states no absolute test count anywhere (checked by grep for `"19"`, `"of 19"`, `"nineteen"` — no hits), so it has nothing to be stale.

**A sixth, independently-constructed adversarial fake — for the version check specifically, the one check that has now absorbed four prior fixes across rounds 2, 4, and 5 — passes the entire current 19-test suite, including all seven existing version anti-fake tests.** Per this task's explicit instruction to try splitting the version string into an `(major, minor)` integer tuple and to check whether that is truly extensionally identical to string-set membership, I built exactly that, guarded with an `isdigit()` check specifically so this fake would *not* be caught by round 5's already-closed whitespace gap (an unguarded `int()` parse, e.g. `int("2\n")`, also strips whitespace and would be caught immediately — I confirmed this first, then hardened the fake past it, the same way a real second-pass author closing round 5's finding might defensively add a digit check without reasoning further about what `str.isdigit()` itself accepts):

```python
# real fixed code
_ACCEPTED_TLS_VERSIONS = {"1.2", "1.3"}
if tls_version not in _ACCEPTED_TLS_VERSIONS:
    return False
# adversarial fake, in its place
_ACCEPTED_TLS_VERSION_TUPLES = {(1, 2), (1, 3)}
parts = tls_version.split(".")
if len(parts) != 2 or not (parts[0].isdigit() and parts[1].isdigit()):
    return False
version_tuple = (int(parts[0]), int(parts[1]))
if version_tuple not in _ACCEPTED_TLS_VERSION_TUPLES:
    return False
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
19 passed
```

**All nineteen tests pass, including every anti-fake test in the file — the whitespace one included, because `"2\n".isdigit()` is `False`, so `"1.2\n"` is correctly rejected by this fake for a different reason than the real fix rejects it (malformed digit shape, not string inequality), landing on the same `502` by coincidence.** None of the seven version strings any test in `test_tls_hop.py` sends (`"1.1"`, `"1.2"`, `"1.3"`, `"1.4"`, `"1.9"`, `"1.30"`, `"1.2\n"`) has a leading zero on either the major or minor component, so `int(parts[i])` never differs from what the digit string spells out for any tested input, and the fake's behavior is indistinguishable from the real fix on the entire existing suite. I confirmed this is a real, exploitable behavioral divergence, not a harmless corner, first by direct probe against the fake and then against the restored real code, both called directly (not only through the FastAPI test client) to isolate the predicate itself:

```
$ python3 -c '...'   # fake in place
fake with 01.2: True
fake with 1.02: True
$ python3 -c '...'   # real fixed code, restored
real with 01.2: False
real with 1.02: False
```

The fake relays a hop presenting `"01.2"` or `"1.02"` as trustworthy (`True` → `200`); the real fix correctly refuses both (`False` → `502`), because neither string is a member of `_ACCEPTED_TLS_VERSIONS = {"1.2", "1.3"}` — `"01.2" != "1.2"` and `"1.02" != "1.2"` as strings, even though `int("01") == int("1") == 1` and `int("02") == int("2") == 2` make their parsed tuples collide with an accepted one. This is the identical *shape* of defect this module's own review has now found six times running in the same three-check function — a plausible alternate implementation that agrees with the real fix on every input the current suite happens to try and disagrees on inputs it does not — and it is the *fifth* distinct fake found specifically in the version check (after the lexicographic-bound fake, the forward-compatible-allow-list fake, the numeric-range-parse fake, and the whitespace-strip fake, closed across rounds 2, 4, and 5), confirming yet again that closing every fake found so far is still not evidence the check is exhausted: an integer-tuple parse is extensionally identical to string-set membership only for inputs with no leading zeros, and nothing in this fixture, this module's spec, or its lessons ever states that constraint. I restored `fixed/app.py` immediately afterward each time I tested a variant: `md5sum` before and after matched (`ebdf983ea6753464cffb6dee0a669513`), and `git status --porcelain` was empty before I moved on.

I also verified the analogous *unguarded* int-tuple parse (a `try/except ValueError` around a bare `int()` split, with no `isdigit()` check) independently — it is caught by the existing whitespace test (`int("2\n") == 2`, same coercion the numeric-range fake already exploited in round 4), confirming it is not a *new* gap by itself; only the `isdigit()`-hardened version, which a real author might write specifically *because* round 5 just closed a whitespace-parsing gap, survives the full suite. This is worth recording explicitly: the isolating input for this sixth fake (a zero-padded version component) is a different near-miss shape from all five already-closed ones, not a rerun of the whitespace case with different guard code.

**Other checks performed, all genuinely adversarial, all confirmed clean, no regression from round 5:**

- **Hostname check, re-attempted with the two shapes round 5 already tried, confirmed the same conclusion by rebuilding the argument myself rather than trusting the addendum's prior wording.** Case-insensitive comparison (`presented_hostname.casefold() == expected_hostname.casefold()`) does pass the full 19-test suite as an empirical matter (I ran it: `19 passed`), because every hostname any test sends is already lowercase. But I do not treat this as a genuine gap distinct from round 5's conclusion: RFC 6125 §6.4.1 specifies DNS-name comparison for certificate validation as case-insensitive by definition, so a case-insensitive `==` is not a plausible-but-wrong near-miss of "compare hostnames" here — it is a defensible, arguably more textbook-correct implementation of the same claim, not a different claim smuggled in under a similar-looking check, unlike the prefix/substring/suffix fakes (which are wrong on their own terms, not merely untested). I also tried IDN/Unicode homograph normalization — comparing after `hostname.encode("idna")` round-tripping, and comparing NFKC-normalized forms (e.g. a Cyrillic "а" (U+0430) standing in for Latin "a") — and found no divergence constructible against this fixture's synthetic hostnames, because `hop_is_trustworthy` never claims to perform IDNA processing at all (it receives `presented_hostname` and `expected_hostname` as already-decoded Python strings, with no punycode or Unicode-confusable handling anywhere in the module's stated scope), so a homograph attack is a gap in an upstream decoding step this module explicitly does not model, not in this predicate. No new hostname gap found.
- **Trusted-CA check, re-verified the three-value-domain argument a third time, from a new angle (truthy/falsy coercion of a non-bool value).** The function's signature is `cert_trusted_ca: bool | None`, so at the type level only `True`, `False`, and `None` are ever passed by a caller respecting the signature; I confirmed the current suite tests all three with distinct expected outcomes, as rounds 4 and 5 already recorded. I then asked whether the type signature is actually *enforced* anywhere in this fixture, since Python does not runtime-check annotations — I sent the relay endpoint a raw JSON payload with `"cert_trusted_ca": 1` (an int, not a bool, which JSON's own type system would happily produce and FastAPI's default `dict` payload type does not reject) directly through the `TestClient`, bypassing the annotation entirely, against the real fixed code: `hop_is_trustworthy(..., cert_trusted_ca=1, ...)` → `1 is not True` evaluates to `True` (since `1 == True` but `1 is not True`, identity, not equality, is what the real code checks) → correctly returns `False`/`502`. This is actually a point *in favor* of the real check's robustness (using `is not True` rather than `not cert_trusted_ca` or `bool(x) is not True` deliberately excludes truthy-but-not-`True` values like `1`), not a gap — I could not construct any JSON-representable value that this check accepts other than the literal boolean `True`. No new CA-check gap found; the type signature's three-value framing is moot for the reason the task anticipated (`bool | None` combined with `is not True` rather than a coercing comparison), and I have now confirmed that by direct construction rather than by accepting the annotation at face value.
- **C1 (cache key), one genuinely new construction: a string-concatenation key in place of the tuple key.** I rewrote `_CACHE` to be keyed on `f"{note_id}:{company}"` (a single string) instead of `(note_id, company)` (a tuple), the classic "I'll just join them with a separator" mistake that can allow a delimiter-collision cache-key bypass in general. This also passes the full 19-test suite (`19 passed`) empirically. I then checked, by construction rather than by stopping at the passing test run, whether this is an *actually exploitable* divergence in this specific fixture: since `company` is always one of exactly four fixed, equal-length, colon-free literal strings resolved server-side from the caller's own API key (never attacker-supplied, per C2's own fix), and every one of those four strings differs in its last character (`companyA`/`companyB`/`companyQ`/`companyZ`), an attacker bound to their own company cannot choose a `note_id` whose concatenation with their own company's fixed suffix reproduces a target string ending in a *different* company's suffix — the trailing 9 characters (`:` plus 8-character company name) of any two colliding strings would need to be textually identical, which requires the two `company` values themselves to be equal. I confirmed this algebraically (`note_id_attacker + ":" + "companyA"` can never equal `note_id_victim + ":" + "companyQ"` for any string `note_id_attacker`, since the two candidate strings' last 9 characters differ and cannot be made equal by varying only the attacker's own note id) rather than only trusting the empirical pass. This is a genuinely new construction attempt (a different mechanism — string-concatenation delimiter collision — from round 5's case-folding attempt), and it also finds no exploitable gap, for a company-set-specific structural reason that would not hold if a fifth company name shared a suffix with another (worth noting as a fixture-specific accident, not a general property of `f"{note_id}:{company}"` as a pattern — a future module reusing this key-construction idiom with attacker-influenced or variable-length identifiers should not read this as an endorsement of the pattern itself).
- **C2 (forwarded identity), reconfirmed no gap via the same reasoning as round 5, no new mechanism found this round beyond the one just described for C1** (both defects share the same `company` value, and the concatenation analysis above covers both cache-key and forwarded-identity angles, since `_resolve_company`'s real fix never reads `x_company` at all and therefore has no string-shaped input left to construct a fake against).
- Reconstructed the round-5 whitespace-strip fake and the round-4 numeric-range fake once each, independent of prior sessions' own reconstructions, and got the documented counts (18 of 19 and 17 of 19 respectively, failing exactly the tests each README/keys entry names).
- Grepped `content/modules/2/2.2/spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, `labs/2.2/2.2-request-path/README.md`, and `content/assessment/keys/2.2.md` for stale test-count phrasing left over from any prior round (`"18 of 18"`, `"17 of 17"`, `"16 of 16"`, `"15 of 15"`, `"14 of 14"`, `"of 1[4-8] pass"`, `"of 1[4-8] fail"`, `"eleven"`, `"twelve"` used inconsistently). No hits outside `content/modules/2/2.2/module.yaml`'s own changelog, which correctly narrates past counts as history (e.g. "12 of 14 pass", "14 of 16", "14 of 17" — all describing what a specific historical fake reconstruction produced at the time, not a live claim about the current suite). Every live, current-tense reference reads "19", "6 of 19", or "13 of 19" consistently.
- Coverage-contract and teaching-claims citations re-derived independently from `items.md`'s own `Claim assessed` tags, cell by cell: C1 → #1, #4, #6, #7, #8; C2 → #2, #4, #7; C3 → #3, #7, #8; C4 → #5, #7; C5 → #6, #7, #8 — matches `spec.md`'s teaching-claims table (L45-49) exactly, no addition or omission, no regression from round 5's finding that this defect class is clean here. Also re-checked the four coverage-contract outcome rows (L59-62) against the same tags: row 1 (C1,C2 → items #1,#2,#4) — all three items' own `Outcome:` text matches the row's outcome text and their claims are a covering subset; row 2 (C3,C4 → item #3) — item #3 is tagged `C3` only, and no item in this table is separately tagged for the C4 half of this specific outcome's claim column; row 3 (C1,C2,C4 → item #5) — item #5 is tagged `C4` only; row 4 (C1,C2,C5 → items #6,#7,#8) — all three items' claims cover a superset including C5. Rows 2 and 3's partial claim-tag coverage (an outcome naming two or three claims while its cited item(s) are tagged with only one of them) is unchanged from the *first* review round's own explicit check of these same four rows, which read this same pattern as "correct" on the basis of matching `Outcome:` text rather than requiring every named claim to carry its own dedicated item tag — I am not treating this as a new or reopened finding, since neither the table's structure nor any cited item has changed across all six rounds, and the original round's judgment on this specific point stands unless a future task explicitly asks this review to revisit that judgment call itself.
- Metadata honesty and shared `pins.yaml`, re-confirmed: `content/modules/2/2.2/module.yaml` still reads `status: draft`, no `reviewer`/`depth`/`quality`/`lastReviewedAt`/`nextReviewAt` set by any authoring or fix pass (checked lines 7, 160-162 directly). `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry on this branch still reads `reviewedAt: "2026-09-22"` with the corrected URL (`https://owasp.org/projects/asvs`); `origin/deepen/2.3`'s parallel entry (fetched fresh via `git fetch origin deepen/2.3`, not from memory or cache) reads `reviewedAt: "2026-09-23"` with the identical corrected URL and its own independent dead-link narrative appended after 2.2's — the same cross-branch note-text conflict already on record five rounds running, one day apart, still just that (no factual disagreement about the URL itself, only duplicated "I fixed this" narration and a differing `reviewedAt` on a shared field that will conflict on merge). Not touched, per this task's instruction.
- Mechanical gates, run myself: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.

**Verdict: neither gate is granted, for the sixth time running.** Round 5's finding is genuinely closed — I verified this by independent reconstruction, not by trusting the fix commit's own account, and it holds up, as do all counts, citations, metadata honesty, the cross-branch `pins.yaml` note, and mechanical gates checked above. But a sixth construction, targeted again at the one check (the version check) that had already absorbed four prior fixes across rounds 2, 4, and 5, found a real, demonstrated, exploitable gap the current 19-test suite does not catch: an `isdigit()`-guarded integer-tuple parse of the two dot-separated components, deliberately hardened past the whitespace gap round 5 just closed, which is extensionally identical to exact string-set membership only for version strings with no leading zeros — a constraint nothing in this fixture, this module's claim, or its lessons ever states. Per this repo's governing rule, a real blocker withholds every grant regardless of how much surrounding work is independently confirmed correct, and regardless of how many prior rounds already closed other findings — including four findings in this exact same check, one of which (round 5's) was closed in the immediately preceding commit.

**To close:** add one test in `tests/test_tls_hop.py` sending a `tls_version` string with a leading zero on either the major or minor component that is otherwise a plausible near-miss of an accepted version — for example `tls_version="01.2"` or `tls_version="1.02"`, with hostname and CA trust both otherwise valid — and assert `502`. This isolates "the version string is exactly one of the two canonical strings `\"1.2\"`/`\"1.3\"`, byte for byte" from "the version string's dot-separated components parse to the same integers as one of the two accepted versions," a sixth and distinct near-miss shape from the four already closed (a lexicographic string bound, a hand-written wider string set, a numeric-range parse, and a whitespace strip). Update `README.md`'s and `keys/2.2.md`'s fake counts and lists to record an eleventh verified fake once this is added, and note in whichever file tracks the version check's history that five, not four, structurally distinct fakes have now been found and closed for that one line.

**Recommendation on the test-strategy question (a judgment call for the calling session or a human to weigh, not something I am implementing).** Four structurally distinct fakes now found in the same two-line check across five rounds (lexicographic bound, wider hand-written set, numeric-range parse, whitespace strip), plus this round's fifth (leading-zero integer-tuple parse), is enough evidence to treat the current strategy — one narrow, hand-picked isolating test added per discovered gap — as demonstrably not converging on an exhausted check, and I recommend against relying on it alone going forward. Each new test closes exactly the one input the reviewer happened to construct, and every round's fake has been *extensionally identical to the real check on the entire existing suite* by construction, which is precisely what a hand-picked isolating test cannot detect in advance — it only ever catches the *next* fake once someone has already thought of it, and five rounds of a skilled adversarial reviewer each finding a sixth one suggests the space of plausible near-miss string-comparison implementations for a two-string-literal set is larger than five or six iterations can exhaust by hand-picked example. A small property-based or systematically-parametrized test — for instance, generating a broad set of near-miss version strings programmatically (every string within edit-distance 1-2 of `"1.2"`/`"1.3"` via character insertion/deletion/substitution, every leading/trailing whitespace and zero-padding variant, every numeric neighbor at small perturbations, every case variant even though case is moot here) and asserting `502` for every one not literally equal to `"1.2"` or `"1.3"`, plus explicit `200` assertions for the two literal accepted strings themselves — would falsify a much larger space of alternate implementations in one test than any single hand-picked case can, including implementations no reviewer has thought of yet (an integer-tuple parse with a *different* zero-padding-tolerant digit check than `isdigit()`, for instance, or a base-conversion quirk not yet tried). I recommend the next fix pass add such a test (or at minimum a table-driven parametrized test enumerating a deliberately large set of near-miss strings in one test function) *in addition to* keeping the existing hand-picked anti-fake tests, which remain valuable as named, readable regression markers for each specific defect class this review has now documented — the two approaches are complementary, not a replacement of one for the other, and dropping the named tests would lose the pedagogical value of `README.md`'s own fake catalogue for learners reading this lab's history.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A sixth re-review that found a live blocker may not write this line, and finding five prior rounds' issues closed does not change that.
