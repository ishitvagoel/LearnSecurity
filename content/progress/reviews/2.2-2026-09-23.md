# 2.2 — independent review, 2026-09-23

**Reviewed commit:** `c26f92b` on `deepen/2.2` ("Finish deepening module 2.2: item bank, real lab bug, citation fixes"), which continues the rate-limited checkpoint at `6c49552` ("WIP: deepen module 2.2 (rate-limited mid-pass, continuing separately)").
**Reviewer:** independent quality reviewer (read-only agent session), also performing the lab-safety check for this pass.
**Independence statement:** I authored no file in this module's scope and had no prior authoring context before this session. Every judgement below comes from reading the files at `c26f92b` and from commands I ran myself. Where I modified `fixed/app.py` to construct an adversarial fake, I restored the original file afterward and confirmed `git status --porcelain` was empty before moving on; the only files I have written in this repository are this artifact and scratch files under `/tmp` outside the repo tree.

## Files inspected

Read in full: `content/modules/2/2.2/spec.md`, `module.yaml`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, `content/assessment/keys/2.2.md`, `labs/2.2/2.2-request-path/{README.md,conftest.py,vulnerable/app.py,vulnerable/SECURITY.md,vulnerable/cache.py,fixed/app.py,fixed/SECURITY.md,fixed/cache.py,requirements.txt,tests/test_cache_key.py,tests/test_tls_hop.py}`, `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry (both on this branch and on `origin/deepen/2.3`), and the precedent artifacts `content/progress/reviews/4.3-2026-09-17.md` and `content/progress/reviews/0.2-2026-09-22.md`. Live-fetched the OWASP ASVS `v5.0.0` tag's full requirements JSON directly (not trusted from `pins.yaml`'s account of it), and live-checked both the old and new ASVS project-page URLs and both cited RFC pages with `curl -L`.

## What I verified as true

**The `is None` vs `is not True` bug — confirmed genuine, precisely as claimed.** `fixed/app.py`'s real `hop_is_trustworthy` reads `if cert_trusted_ca is not True: return False`. I edited a scratch copy to the described fake (`if cert_trusted_ca is None: return False`) and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed
1 failed, 13 passed
FAILED tests/test_tls_hop.py::test_explicitly_untrusted_ca_is_rejected_even_with_matching_hostname_and_version
```

Exactly and only the new test fails — no other test in the 14-test suite depends on this distinction. I restored the file (`cp` from a pre-edit copy) and confirmed `git status --porcelain` was empty.

**Full pass/fail counts, run myself, not trusted from the README or changelog:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable
8 failed, 6 passed
FAILED test_cache_key.py::test_other_company_does_not_receive_cached_body
FAILED test_cache_key.py::test_forwarded_company_header_cannot_grant_a_different_companys_note
FAILED test_cache_key.py::test_anti_fake_cache_key_with_fresh_never_elsewhere_used_company_and_note
FAILED test_cache_key.py::test_anti_fake_forwarded_header_with_fresh_never_elsewhere_used_values
FAILED test_tls_hop.py::test_hostname_mismatch_with_trusted_ca_is_rejected
FAILED test_tls_hop.py::test_tls_1_2_boundary_is_still_accepted_1_1_is_not
FAILED test_tls_hop.py::test_anti_fake_hostname_prefix_match_is_rejected
FAILED test_tls_hop.py::test_anti_fake_hostname_substring_anywhere_is_rejected

$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed
14 passed
```

This matches the claimed "vulnerable 6/14 [pass], fixed 14/14" and items.md's own evidence checklist ("vulnerable/ tests show 8 of 14 failing"). Every vulnerable failure is a plain assertion on the security property (a `404` expected, a `200` returned, or vice versa for the hop check), not a collection or import error — the vulnerable variant fails for the claimed reasons. `bash scripts/run_labs.sh 2.2` independently reports the same shape (`vulnerable failed, fixed passed, RESULT OK`).

**Coverage-contract and teaching-claims citations — re-derived independently from `items.md`'s own tags, cell by cell, and found fully correct.** I built my own map from each item's `Claim assessed` tag: #1→C1, #2→C2, #3→C3, #4→C1,C2, #5→C4, #6→C1,C5, #7→C1–C5, #8→C1,C3,C5. Checked against `spec.md`'s teaching-claims table:

| Claim | spec.md cites | Items actually tagged with this claim | Match |
|---|---|---|---|
| C1 | #1, #4, #6, #7, #8 | #1, #4, #6, #7, #8 | Exact |
| C2 | #2, #4, #7 | #2, #4, #7 | Exact |
| C3 | #3, #7, #8 | #3, #7, #8 | Exact |
| C4 | #5, #7 | #5, #7 | Exact |
| C5 | #6, #7, #8 | #6, #7, #8 | Exact |

Then checked the coverage contract's four outcome rows against each item's own `Outcome:` text (not against module.yaml's slightly longer paraphrase, since `items.md` shortens it, but the meaning and claim scope agree in every case): row 1 (C1,C2 → #1,#2,#4) — all three items carry that exact outcome text; row 2 (C3,C4 → #3) — only item #3 carries that outcome text; row 3 (C1,C2,C4 → #5) — only item #5 carries it; row 4 (C1,C2,C5 → #6,#7,#8) — all three carry it. **All nine cells across both tables are correct.** This is the defect class every prior module's review (1.4, 2.1, 4.3, 0.1, 0.2, 1.1) found broken on first pass; here it is genuinely clean, confirming the second pass's own claim to have fixed it.

**Standards — all nine ASVS 5.0.0 citations verified against the live `v5.0.0` requirements JSON, exact text and exact level, no discrepancies:**

| ID | Level (live) | Module's paraphrase | Verdict |
|---|---|---|---|
| 4.1.3 | L2 | forwarded headers not end-user overridable | Accurate |
| 12.1.1 | L1 | only latest TLS versions (1.2, 1.3) | Accurate |
| 12.2.1 | L1 | TLS for all external connectivity | Accurate |
| 12.2.2 | L1 | publicly trusted certificates | Accurate |
| 12.3.2 | L2 | TLS clients validate certs before communicating | Accurate |
| 14.2.2 | L2 | sensitive data not cached in server components | Accurate |
| 14.3.2 | L2 | anti-caching headers to browsers | Accurate |
| 14.2.5 | L3 | web cache deception, correctly labeled advanced/residual | Accurate |
| 16.3.4 | L2 | log security-control failures incl. backend TLS failures | Accurate |

Every level the module and `pins.yaml`'s note claim matches the live document exactly. RFC 9110 ("HTTP Semantics") and RFC 9846 both resolve 200 and are titled as cited; RFC 9846's live page confirms it obsoletes RFC 8446 (and RFC 5246/5077/6961/7627/8422), exactly as `module.yaml` states.

**Dead-URL fix — confirmed genuine, resolves correctly.** `curl -L` on the old `owasp.org/www-project-application-security-verification-standard/` URL: two `308` redirects then a final `404`. `curl -L` on the corrected `https://owasp.org/projects/asvs`: `200`. The fix is real and correctly targeted.

**Metadata honesty — confirmed clean.** `module.yaml` parses with `status: draft`, `reviewer: pending independent quality and lab-safety review`, `lastReviewedAt: null`, `nextReviewAt: null`. `module.schema.json` validation passes.

**Mechanical gates — run myself:**

```
$ python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline
0 error(s), 0 warning(s)

$ python3 scripts/lint_content.py
0 error(s), 0 warning(s), 2182 suppressed by baseline   # matches the claimed 2215->2182 drop

$ npm --prefix site run build   # after npm install under site/
✓ Compiled successfully
✓ Generating static pages using 3 workers (643/643)
```

Key isolation (the same check `.github/workflows/content.yml` runs): no `href="...assessment/keys..."` anywhere in `site/out`, and `keys/2.2.md`'s first substantive line does not appear verbatim in the export. Both pass.

**Coverage contract — no empty cells; ≥2-claim lab-assertion bar exceeded.** All four `module.yaml` outcomes have a matching, fully-populated row in `spec.md`'s coverage contract (Explanation, Worked example, Practice, Assessment item, Transfer). C1, C2, and C3 each carry genuine, verified lab assertions (three claims, not the required two); C4 and C5 are honestly declared non-code-testable with a stated, checkable reason each, and I confirmed both reasons hold: this single-process fixture genuinely has no second real network hop to stage C4, and no CDN/edge process exists to drift for C5.

**Lab safety — pass, no findings.** Grepped the entire lab tree for `socket`, `subprocess`, `os.system`, `requests.`, `urllib`, `http.client`, `paramiko`, `ssl.wrap*`/`ssl.SSLContext` — no hits; the fixture never performs real network I/O, a real handshake, or DNS resolution, matching its own stated non-goals. No real secrets or PII patterns found (`API_KEYS` values are `key-A`/`key-B`/`key-Z`/`key-Q`, plainly synthetic). Both `SECURITY.md` files and the README state local-only scope consistently. Reset is `git checkout --` plus a per-test `reset()`; no persisted state. `vulnerable/`↔`fixed/` pairing is structurally sound and every lesson's closing section disclaims live targets.

## Blocker

**A different, independently-constructed adversarial fake for C3 passes the entire 14-test suite, including every anti-fake test — the module's own "five constructed fakes" review missed a sixth gap in the same check it had just repaired for a different signal.**

Per the task's instruction to build my own fake rather than only re-running the documented ones, I replaced `fixed/app.py`'s version check:

```python
# real fixed code
if tls_version not in _ACCEPTED_TLS_VERSIONS:   # _ACCEPTED_TLS_VERSIONS = {"1.2", "1.3"}
    return False
```

with a plausible alternate "fix" using a lexicographic lower bound instead of exact set membership:

```python
if tls_version < "1.2":
    return False
```

This passes both existing version-related tests (`"1.2"` accepted, `"1.1"` rejected — `"1.1" < "1.2"` is true lexicographically) exactly as the real fix does. I ran the full suite against it:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed
14 passed
```

**All fourteen tests pass, including both hostname anti-fake tests and the newly-added untrusted-CA anti-fake test.** No test in either file sends a `tls_version` value outside `{"1.1", "1.2", "1.3"}`, so nothing distinguishes "exact membership in the accepted set" from "lexicographically at or above the oldest accepted version" — a string like `"9"`, `"2.0"`, or `"A"` sorts above `"1.2"` in Python string comparison (`"9" > "1.2"` is `True`; so is `"A" > "1.2"`, since `'A'` (65) > `'1'` (49) in ASCII) and would be accepted as trustworthy by this fake even though it is not one of the two versions this module's own claim, README, and lessons name as accepted. This is the identical *shape* of gap the module's own second-pass changelog found and fixed for the CA-trust check (a plausible alternate implementation that agrees with the real fix on every input the suite happens to try, and disagrees on inputs it does not) — except this instance is in the version check, and it was not found. I restored the file afterward (`git status --porcelain` clean, confirmed).

I also independently reconstructed one of the module's own documented fakes (the cache-only fix leaving the header override open) to confirm the module's account of its own verification work is not fabricated — it reproduces exactly: 12 of 14 pass, and the two failures are precisely C2's forbidden-outcome test and its anti-fake pair, as `README.md` and `keys/2.2.md` both claim.

**Severity:** this is a real gap in the one thing a Tier-2 lab's anti-fake suite exists to guarantee for a critical claim (C3, hop authentication) — that a plausible-but-wrong alternate implementation cannot pass by agreeing with the real fix only on the inputs the suite happens to try. `04-build.md` (L52) explicitly names hostname-comparison near-misses as the exact class of mistake the anti-fake tests exist to catch, and the module's own second-pass changelog treats an analogous CA-trust gap as worth a dedicated new test — but the identical class of gap in the version check, one line away in the same function, was not looked for.

**To close:** add one test sending an out-of-set `tls_version` that a lexicographic-bound check would wrongly accept (for example `tls_version="9"` or `tls_version="1.30"`, with hostname and CA trust both otherwise valid) and assert `502`; update `README.md`'s fake count and `keys/2.2.md`'s lab section to record a sixth verified fake.

## Minor finding (not a blocker)

**Stale, unreferenced `cache.py` files in both `vulnerable/` and `fixed/`.** `labs/2.2/2.2-request-path/vulnerable/cache.py` and `fixed/cache.py` (a two-function `cache_put`/`cache_get` pair, predating this deepen pass — confirmed via `git log --follow`, both files exist since well before the Tier-2 rewrite) are never imported by `app.py`, never referenced by any test, and never mentioned in `README.md` or any lesson. They appear to be leftover Tier-1 lab code the Tier-2 upgrade should have deleted. Both files are safe (no real secrets, no network I/O — `fixed/cache.py`'s own key shape, `(path, tenant)`, is consistent with the module's real fix, so they are not actively misleading about the *mechanism*), but they are dead weight a future maintainer could mistake for exercised code. Worth a one-line cleanup (`git rm`) alongside the blocker fix above; not itself a reason to withhold a grant.

## Shared `content/standards/pins.yaml` — checked against `origin/deepen/2.3`'s parallel edit

Both branches touch the same `owasp-asvs-5.0.0` pin entry for a related but independently-discovered dead-URL fix. On `deepen/2.2` (this branch): `reviewedAt: "2026-09-22"`, `url: "https://owasp.org/projects/asvs"`, notes ending with 2.2's own dead-URL account. On `origin/deepen/2.3` (commit `e43c153`): `reviewedAt: "2026-09-23"`, the **same** corrected `url`, notes ending with 2.3's own dead-URL account (2.3 independently found and fixed the identical dead URL, for its own citations, one day "later").

**No factual contradiction** — both branches agree on the corrected URL and both agree it is genuinely fixed. But this **will produce a merge conflict** (different `reviewedAt` values on the same field, and two divergent `notes` strings each claiming to be the one that fixed this exact dead link) when either branch merges after the other. Per this task's scope, I am reporting this for the calling session to resolve, not attempting a merge myself — whichever branch merges second will need its notes rewritten to say "already fixed by module 2.3/2.2's pass" rather than repeating a "found and fixed" claim for a URL that will already be corrected on `main`.

## Verdict

- [ ] `quality: developing`
- [ ] `quality: competent`
- [ ] `depth: publishable`
- [ ] `quality: transfer-ready`

**None granted.** The lesson prose (all eight lessons genuinely exceed the 900-word bar with real derivation, not padding), the request-path model, the five teaching claims, the coverage-contract and teaching-claims citations (fully correct, a first for this defect class among the modules I have precedent for), the standards work (all nine ASVS citations and both RFCs verified accurate against live sources, the dead-URL fix genuine), and the metadata honesty are all independently confirmed and genuinely strong — this is not a thin or padded pass, and the module's own second-pass discovery of the `is None`/`is not True` gap is real, precisely as claimed, and well-tested.

But the Blocker above is a real, demonstrated hole in the property the lab's anti-fake suite exists to certify for a critical claim (C3): a plausible alternate "fix" for the version check passes all fourteen tests, including every anti-fake test written for this module, exactly the same class of failure 0.2's review used to withhold every grant for its own module. Per this task's governing instructions, a real blocker withholds every grant regardless of how strong the surrounding work is, and I am not treating "the module already found and fixed one gap in this function" as license to skip looking for a second one in the same function — which is exactly what turned up here.

**Lab-safety review: passes, no findings**, recorded here so a re-review does not have to redo it. Per the governance rule, this does not by itself authorize any grant while the blocker stands — both sides must pass, and the quality side does not.

The fix is narrowly scoped: one new test in `tests/test_tls_hop.py`, plus the `README.md`/`keys/2.2.md` count updates, and (optionally, per the minor finding) removing the two stale `cache.py` files. No lesson's argument needs to change, and no other claim's tests need re-verification — I would expect a re-review limited to the changed lines to close quickly, the same way 4.3's and 0.2's narrowly-scoped blockers did.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real blocker exists. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as this pass set them; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. An authoring agent may not write this line, and neither may an independent review that found a live blocker.

## Addendum, 2026-09-23 — re-review at `2318785`: original blocker closed, but a second, same-shape gap and a miscounted fake remain open

**Independence statement.** I am a separate re-review pass, same day, on the fix commit `2318785` ("Close 2.2's blocker: TLS-version anti-fake gap, stale counts, dead files"). I read the review above in full before touching any file, then verified every claim by construction, not by trusting the diff or the changelog's own description of itself. Every file I edited to construct a fake was restored byte-for-byte afterward (`diff` against a pre-edit copy showed no difference, and `git status --porcelain` was empty each time) before I moved to the next check.

**Original blocker — CLOSED, verified by reconstructing the exact documented fake.** I edited `fixed/app.py`'s version check to `if tls_version < "1.2": return False` (replacing `if tls_version not in _ACCEPTED_TLS_VERSIONS: return False`) and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 14 passed
FAILED tests/test_tls_hop.py::test_anti_fake_version_string_that_sorts_high_is_still_rejected
```

Exactly and only the new test fails, as claimed. Restored, confirmed clean.

**Full pass/fail counts, re-run myself:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
9 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
15 passed
```

Matches the claimed 9/15 fail, 15/15 pass.

**A second, independently-constructed adversarial fake for the same version check also passes the entire 15-test suite, including the test just added to close the first gap.** I replaced the real fix's exact set membership with a plausible "forward-compatible allow-list" mistake — a maintainer's guess that a future point release should also be accepted:

```python
if tls_version not in {"1.2", "1.3", "1.4"}:   # real: not in _ACCEPTED_TLS_VERSIONS ({"1.2", "1.3"})
    return False
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
15 passed
```

**All fifteen tests pass**, including `test_anti_fake_version_string_that_sorts_high_is_still_rejected`. No test in `test_tls_hop.py` sends any version other than `"1.1"`, `"1.2"`, `"1.3"`, or `"1.9"` (confirmed by grep), so nothing distinguishes "exact membership in `{"1.2", "1.3"}`" from "membership in `{"1.2", "1.3", "1.4"}`" — a version this module's own claim, README, and lessons never name as accepted is silently treated as trustworthy. This is the identical *shape* of gap the fix commit just closed for one alternate implementation (lexicographic bound) but not for another (a wider but still-wrong allow-list), in the same function, one line away, immediately after a pass whose entire purpose was to close exactly this class of gap. Restored, confirmed clean (`diff` against pre-edit copy: no difference; `git status --porcelain`: empty).

**A specific recomputed fake count in the fix's own changelog, `README.md`, and `lessons/05-verify.md` is wrong — not by one, as a sibling module's review once caught, but by overclaiming a failure that does not occur.** All three describe the cache-only fix (header override left open) as: *"12 of 15, fails C2's forbidden-outcome test, its malformed-credential case, and its anti-fake test"* (three named failures). I reconstructed this exact fake — `fixed/app.py`'s `_resolve_company` restored to `vulnerable/app.py`'s header-reading behavior, cache key left untouched — and ran the full suite with `-v`:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -v
...
tests/test_cache_key.py::test_forwarded_company_header_cannot_grant_a_different_companys_note FAILED
tests/test_cache_key.py::test_unregistered_api_key_with_a_company_header_is_denied PASSED
tests/test_cache_key.py::test_anti_fake_forwarded_header_with_fresh_never_elsewhere_used_values FAILED
...
2 failed, 13 passed
```

**The malformed-credential case (`test_unregistered_api_key_with_a_company_header_is_denied`) passes against this fake — it does not fail.** Reading the fake's own logic explains why: `_resolve_company` checks `if api_key not in API_KEYS: return None` *before* it ever looks at `x_company`, so an unregistered key (`"key-does-not-exist"`) is rejected on the first branch regardless of any header. The header override this fake leaves open is only reachable through a *registered* key, which is exactly what the forbidden-outcome test and its anti-fake pair exercise — the malformed-credential case tests a different, already-correct code path in both variants. The true count is **13 of 15 pass, 2 failures**, not 12 of 15 with 3 failures, in the fix's own changelog entry (`module.yaml` L236, L251), `labs/2.2/2.2-request-path/README.md`, and `content/modules/2/2.2/lessons/05-verify.md`. This is the same failure mode the task's background names for module 2.3's own review ("6 of 9" that was actually "7 of 9") — a self-reported recomputation that does not match the suite it claims to describe. (For comparison, I also reconstructed the header-only fix — cache left keyed on path alone, `_resolve_company` fixed — and got exactly the claimed 13 of 15, 2 failures, both in `test_other_company_does_not_receive_cached_body` and its anti-fake pair: that count is correct.)

**Several stale test-count references the fix commit's own message claims to have updated were not, in fact, all updated.** Grepping the module and lab tree for lingering `"14 of 14"` / `"6 of 14"` / `"eight"` phrasing after the fix commit found:

- `labs/2.2/2.2-request-path/README.md` L29: `python3 -m pytest ... --impl fixed        # 14 of 14 pass` — still says 14, should say `15 of 15 pass` (the line immediately above it, for `vulnerable`, was correctly updated to `# 6 of 15 pass` in the same commit, making this an inconsistency within one three-line code block, not merely an old reference nobody looked at).
- `content/modules/2/2.2/lessons/05-verify.md` L25 (Mermaid diagram): `-->|"6 of 14 pass"|` — still says 14, should say `6 of 15 pass`.
- `content/modules/2/2.2/lessons/05-verify.md` L28 (same diagram): `-->|"14 of 14 pass"|` — still says 14, should say `15 of 15 pass`.
- `content/modules/2/2.2/lessons/05-verify.md` L35: "the eight that should fail do fail for the stated reason" — should say "nine" (the vulnerable variant now fails 9 of 15, confirmed above).
- `content/assessment/keys/2.2.md` L12: `` `labs/2.2/2.2-request-path/fixed/` must pass all 14. `` — still says 14, should say 15.

The fix commit's message claims "Updated every '14 tests' reference across spec.md, 05-verify.md, items.md, rubric.md, the lab README, and the isolated key" — `items.md` and `rubric.md` were in fact updated correctly (both read "9 of 15 failing" / "15 of 15 passing"), and one paragraph each in `05-verify.md`, the README, `keys/2.2.md`, and `module.yaml`'s changelog were updated with new, mostly-correct prose (save the miscount above) — but the claim that *every* reference was updated is itself not accurate; five instances across two files were missed.

**Confirmed still true, unaffected by the above:** the two stale `vulnerable/cache.py` / `fixed/cache.py` files are genuinely deleted (`find` for `cache.py` under `labs/2.2` and `content/modules/2/2.2` returns nothing) and nothing else in either tree references them (grepped both trees for `cache\.py`; the only hit is `module.yaml`'s own changelog line describing the deletion). Mechanical gates pass: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`; `npm --prefix site run build` (after `npm install --prefer-offline --no-audit --no-fund` in a fresh `site/` checkout) → compiles and generates all 643 static pages successfully.

**Verdict: neither gate is granted.** The second adversarial fake is the same class of hole the original blocker described — a plausible alternate implementation of the version check that agrees with the real fix on every input the suite happens to try and disagrees on inputs it does not — sitting one line away in the same function, discovered immediately after a pass whose stated purpose was closing exactly this class of gap for this check. That alone withholds every grant under this task's governing rule. The miscounted "12 of 15 / three failures" claim is a second, independent problem: the module's own account of its verification work does not match what the suite actually does, in a place a reviewer might otherwise treat as already re-verified because the surrounding numbers were recomputed rather than incremented. The five leftover stale references are minor by comparison but mean the fix commit's own summary of its completeness is not fully accurate either.

**To close:** (1) add a test sending a `tls_version` that is a plausible-but-wrong *addition* to the accepted set (for example `tls_version="1.4"` or `"1.30"`, hostname and CA trust otherwise valid) and assert `502`, isolating "exact membership in the two-element set" from "membership in some superset that also contains 1.2 and 1.3"; (2) correct the cache-only-fix count everywhere it appears (`module.yaml`'s changelog, `README.md`, `lessons/05-verify.md`) to 13 of 15 / 2 failures, and drop the "malformed-credential case" from the list of what it fails; (3) fix the five remaining stale references listed above. None of this requires re-authoring any lesson's argument or re-verifying any other claim's tests.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because real, demonstrated problems remain. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. An independent review that found a live blocker may not write this line.

## Addendum, 2026-09-23 — third-round re-review at `79b8941`: both round-2 findings genuinely closed, but a new, third gap found by independent adversarial construction

**Independence statement.** I am a third, separate re-review pass, same day, on commit `79b8941` ("Close 2.2's second re-review findings: forward-compatible version gap, arithmetic"), now `HEAD` of `deepen/2.2` (fast-forwarded from `origin/deepen/2.2`; the local branch had been stale at `b277893`). I read both addenda above in full before touching any file. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed both by `diff` against a pre-edit copy and by `git status --porcelain` returning empty — before moving to the next check, every time.

**Round 2's first finding — CLOSED, verified by reconstructing the exact fake from scratch.** Before reading the fix's own description of what it does, I built the fake myself: `{"1.2", "1.3", "1.4"}` in place of `_ACCEPTED_TLS_VERSIONS` in `fixed/app.py`'s version check.

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 15 passed
FAILED tests/test_tls_hop.py::test_anti_fake_forward_compatible_allow_list_still_rejects_an_unaccepted_version
```

Fails exactly and only the test added to close this gap. Restored; `diff`/`git status --porcelain` both clean.

**Round 2's second finding (the miscounted cache-only fake) — CLOSED, verified by reading `vulnerable/app.py`'s real check order first, then reconstructing independently.** I read `vulnerable/app.py`'s `_resolve_company` myself (not the fix commit's account of it): it checks `if api_key not in API_KEYS: return None` first, and only then `if x_company: return x_company`. I built a fake `_resolve_company` for `fixed/app.py` in that exact order (key check first, header second) while leaving `_CACHE`'s `(note_id, company)` keying (C1) untouched, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -v
...
test_cache_key.py::test_forwarded_company_header_cannot_grant_a_different_companys_note FAILED
test_cache_key.py::test_unregistered_api_key_with_a_company_header_is_denied PASSED
test_cache_key.py::test_anti_fake_forwarded_header_with_fresh_never_elsewhere_used_values FAILED
2 failed, 14 passed
```

14 of 16, exactly the two failures the fix now claims (C2's forbidden-outcome test and its anti-fake pair); the malformed-credential case passes, exactly as claimed, because the key check runs and fails before the header is ever read. The corrected count (14 of 16, 2 failures — not round 1's own miscounted "12 of 15, 3 failures") is genuinely right this time. Restored; clean.

**A third, independently-constructed adversarial fake — for a different check than either of the first two rounds' findings — passes the entire current 16-test suite.** Per this task's instruction to look for a gap in a different part of the logic, I read all three of `fixed/app.py`'s functions end to end and targeted the hostname comparison in `hop_is_trustworthy`, which the two existing hostname anti-fake tests cover for a *prefix* fake (`presented_hostname.startswith(expected_hostname)`) and a *substring-anywhere* fake (`expected_hostname in presented_hostname`) — but not for a *suffix-without-boundary* fake, a distinct and equally plausible near-miss for "compare the hostnames":

```python
# real fixed code
return presented_hostname == expected_hostname
# adversarial fake
return presented_hostname.endswith(expected_hostname)
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
16 passed
```

**All sixteen tests pass, including both existing hostname anti-fake tests.** Neither existing test catches an `endswith` fake: the prefix test's presented hostname (`"notes-origin.securecollab.internal.attacker.example"`) does not *end with* the expected hostname (it ends with `.attacker.example`), and the substring-anywhere test's presented hostname (`"evil-origin.internal-mirror.example"`) likewise does not end with the expected hostname (`"origin.internal"`) — both were built to defeat a prefix or middle-substring check, not a suffix check, so an `endswith` fake sails through both untouched, and through every other test in the file (all of which either use identical hostnames on both sides or a hostname-mismatch case that also happens not to share a suffix).

I then confirmed this is a real, exploitable behavioral difference from the true fix, not merely an untested-but-harmless corner: I ran the fake directly through the FastAPI `TestClient` with `expected_hostname="origin.internal"`, `presented_hostname="evil-origin.internal"`, `cert_trusted_ca=True`, `tls_version="1.3"` — a classic domain-suffix-without-boundary case (`"evil-origin.internal"` ends with the literal characters `"origin.internal"` with no separating dot or hyphen boundary, so it is not a subdomain of the expected host, it merely shares a text suffix). The fake returns `200 {"relayed": True}`; the real `==` check would return `502` for this exact input, since the strings are not equal. This is the identical *shape* of defect this module's own review has now found three times running in the same three-check function (`hop_is_trustworthy` / the version check) — a plausible alternate comparison that agrees with the real fix on every input the suite happens to try and disagrees on inputs it does not — this time in the hostname check rather than the trust-state or version check. Restored `fixed/app.py`; confirmed via `diff` against the pre-edit copy (no difference) and `git status --porcelain` (empty).

**Other checks performed, all confirmed clean:**

- Reconstructed two more of the five other pre-existing documented fakes myself: the header-only fix (cache reverted to path-only keying, `_resolve_company` left fixed) gives 14 of 16, failing exactly `test_other_company_does_not_receive_cached_body` and its anti-fake pair, as documented; the hostname-substring-anywhere fake (`expected_hostname in presented_hostname`) gives 14 of 16, failing both hostname anti-fake tests, as documented. Combined with the cache-only-fix and `{"1.2","1.3","1.4"}` reconstructions above, four of the seven documented fakes are now independently re-verified against the current 16-test suite by me, in addition to the two rounds before this one.
- Full lab matrix, run myself: `python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q` → `10 failed, 6 passed`; `--impl fixed -q` → `16 passed`. Matches the claimed vulnerable-10/16-fail, fixed-16/16-pass shape exactly. `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`.
- Grepped `spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, the lab `README.md`, and `content/assessment/keys/2.2.md` for stale test-count phrasing (`"14 of 14"`, `"6 of 14"`, `"8 of 14"`, `"15 of 15"`, `"of 14 pass"`, `"of 14 fail"`, `"eight"`). Every live, current-tense claim in these files now correctly says "16" (`items.md`, `rubric.md`, `keys/2.2.md`, README, `05-verify.md`'s Mermaid diagram and prose all read 10-of-16/16-of-16 or count the fakes against "the current 16-test suite"). The only remaining occurrences of "14"/"15"/"12"/"eight"-style numbers are inside `module.yaml`'s own changelog, correctly narrating what past passes found and fixed at the time — historical record, not a live claim, and not a defect.
- Mechanical gates, run myself: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`; `python3 scripts/lint_content.py` (full repo) → `0 error(s), 0 warning(s), 2182 suppressed by baseline`; `npm --prefix site run build` (after `npm install --prefer-offline --no-audit --no-fund` in a fresh `site/` checkout) → compiles and generates all static pages successfully, no errors. Key isolation: no `href="...assessment/keys..."` anywhere in `site/out`.

**Verdict: neither gate is granted, for the third time running.** Round 2's two findings are genuinely closed — I verified both by independent reconstruction, not by trusting the fix commit's own account, and both hold up. But this task's explicit instruction was not to treat "the module already found and fixed gaps in this function twice" as license to stop looking, and a third construction — in a different check (the hostname comparison, not the version check) — found a real, demonstrated, exploitable gap the current 16-test suite does not catch. Per this repo's governing rule, a real blocker withholds every grant regardless of how much of the surrounding work is independently confirmed correct, and regardless of how many prior rounds already closed other findings in the same review cycle.

**To close:** add one test in `tests/test_tls_hop.py` sending a `presented_hostname` that ends with `expected_hostname` but is not equal to it and has no subdomain-boundary character (`.` or the start of string) immediately before the shared suffix — for example `expected_hostname="origin.internal"`, `presented_hostname="evil-origin.internal"`, with `cert_trusted_ca=True` and `tls_version="1.3"` — and assert `502`. This isolates "the presented hostname is exactly the expected hostname" from "the presented hostname merely ends with the expected hostname's characters," the same way the existing prefix and substring-anywhere anti-fake tests isolate the other two plausible near-misses of hostname comparison. Update `README.md`'s and `keys/2.2.md`'s fake counts and lists to record an eighth verified fake once this is added, and re-verify the whole family (prefix, substring-anywhere, suffix-without-boundary) is now covered by three, not two, anti-fake tests.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A third re-review that found a live blocker may not write this line, and finding two prior rounds' issues closed does not change that.

## Addendum, 2026-09-23 — fourth-round re-review at `5961912`: round-3 finding genuinely closed, but a fourth, structurally distinct gap found in the same version check

**Independence statement.** I am a fourth, separate re-review pass, same day, on commit `5961912` ("2.2: close third review round's hostname endswith gap, plus two cross-failures it surfaced"), which is both the tip of `origin/deepen/2.2` and `HEAD` of the branch I reviewed from. I read all three addenda above in full before touching any file. I worked from a worktree already checked out at this exact commit (`/tmp/fix3-2.2`, branch `fix3-2.2-blockers`, tracking `origin/deepen/2.2` with a clean tree) rather than trusting any cached state. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum`/`diff` against a pre-edit copy and by `git status --porcelain` returning empty — before I moved to the next check, every time.

**Round 3's finding — CLOSED, verified by reconstructing the exact fake from scratch before reading the fix's own description.** I read `tests/test_tls_hop.py` end to end first, then wrote `return presented_hostname.endswith(expected_hostname)` in place of `return presented_hostname == expected_hostname` in `fixed/app.py`'s `hop_is_trustworthy`, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 16 passed
FAILED tests/test_tls_hop.py::test_anti_fake_hostname_suffix_without_boundary_is_rejected
```

Fails exactly and only the test added to close this gap. Restored; `diff` against the pre-edit copy showed no difference, `git status --porcelain` was empty.

**Full pass/fail counts, re-run myself, matching every file's own claim exactly:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
11 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
17 passed
```

This matches, word for word and number for number, `labs/2.2/2.2-request-path/README.md` L28-29 ("6 of 17 pass" / "17 of 17 pass"), `content/modules/2/2.2/lessons/05-verify.md`'s Mermaid diagram ("6 of 17 pass" / "17 of 17 pass"), `content/modules/2/2.2/assessment/items.md` L104 and `assessment/rubric.md` L13 ("11 of 17 failing" / "17 of 17 passing"), and `content/assessment/keys/2.2.md` L11-12 ("must fail 11 of 17" / "must pass all 17"). `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`. Every vulnerable failure is a plain assertion on the security property, confirmed by name against the eleven `FAILED` lines above, which match `keys/2.2.md` L11's own enumeration (both `test_cache_key.py` forbidden-outcome/anti-fake pairs for C1 and C2, plus `test_tls_hop.py`'s forbidden-outcome, boundary, and all five anti-fake tests for C3) exactly.

**Coverage-contract and teaching-claims citations — re-derived independently from `items.md`'s own `Claim assessed` tags, cell by cell, found fully correct again.** Map built from `items.md`: C1 → #1, #4, #6, #7, #8; C2 → #2, #4, #7; C3 → #3, #7, #8; C4 → #5, #7; C5 → #6, #7, #8. Checked against `spec.md`'s teaching-claims table (L45-49): every one of these five cells matches exactly, with no addition or omission on either side. No regression from the round-1 finding that this defect class, common elsewhere in this remediation effort, is genuinely clean here.

**Metadata honesty and shared `pins.yaml` — both re-confirmed, no new problem.** `content/modules/2/2.2/module.yaml` L7 still reads `status: draft`; no `reviewer`, `depth`, `quality`, `lastReviewedAt`, or `nextReviewAt` field has been set by any authoring or fix pass. `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry on this branch still reads `reviewedAt: "2026-09-22"` with the corrected `url: "https://owasp.org/projects/asvs"`; `origin/deepen/2.3`'s parallel entry (fetched fresh, not from memory) reads `reviewedAt: "2026-09-23"` with the identical corrected URL and its own independent dead-link narrative. This is the exact same cross-branch note-text conflict the first-round addendum already recorded — same field, same URL, same shape of divergence, one day apart — not a new or worsened problem. I did not touch it, per this task's instruction.

**Mechanical gates, run myself:** `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK` (shown above).

**A fourth, independently-constructed adversarial fake — for the version check specifically, the same check round 3's session confirmed clean and round 2 had already found two prior gaps in — passes the entire current 17-test suite, including both existing version anti-fake tests.** Per this task's explicit instruction to construct my own candidate rather than only replay the documented ones, I re-read all three checks in `hop_is_trustworthy` character by character and asked, for the version check specifically, whether a plausible good-faith author who reads `_ACCEPTED_TLS_VERSIONS = {"1.2", "1.3"}` as "accept protocol versions from 1.2 through 1.3" might implement it as a *numeric range comparison* rather than exact set membership — a natural instinct for anyone used to comparing version numbers arithmetically instead of as opaque strings, and structurally distinct from both of round 2's fakes (a lexicographic string bound, and a hand-written wider string set) and from round 3's fake (a hostname `endswith`, in a different check entirely):

```python
# real fixed code
if tls_version not in _ACCEPTED_TLS_VERSIONS:
    return False
# adversarial fake, in its place
try:
    major_str, minor_str = tls_version.split(".", 1)
    version_num = float(f"{major_str}.{minor_str}")
except ValueError:
    return False
if not (1.2 <= version_num <= 1.3):
    return False
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
17 passed
```

**All seventeen tests pass, including every anti-fake test in the file — both version anti-fakes and all three hostname anti-fakes.** The reason is that every version string any test in `test_tls_hop.py` currently sends — `"1.1"`, `"1.2"`, `"1.3"`, `"1.4"`, `"1.9"` — happens to parse to a `float` whose ordering against the `[1.2, 1.3]` numeric range agrees with membership in the two-element string set `{"1.2", "1.3"}`: `float("1.1")=1.1` (below range, reject, matches), `float("1.2")=1.2` and `float("1.3")=1.3` (in range, accept, matches), `float("1.4")=1.4` and `float("1.9")=1.9` (above range, reject, matches). Nothing in the suite sends a version string that is numerically inside `[1.2, 1.3]` but not a member of `{"1.2", "1.3"}` as a string — which is exactly what a version like `"1.30"` or `"1.20"` is: `float("1.30") == 1.3`, so the fake's range check accepts it, while `"1.30" not in {"1.2", "1.3"}` is `True`, so the real fixed code rejects it. I confirmed this is a real, exploitable behavioral divergence, not a harmless corner, by running both implementations directly through the FastAPI `TestClient` with `expected_hostname` and `presented_hostname` both `"a.securecollab.internal"`, `cert_trusted_ca=True`, `tls_version="1.30"`:

```
$ python3 -c '...'   # fake in place
tls_version=1.30 -> 200
$ python3 -c '...'   # real fixed code, restored
REAL fixed check, tls_version=1.30 -> 502
```

The fake relays a hop presenting `"1.30"` as trustworthy (`200`); the real fix correctly refuses it (`502`) because `"1.30"` was never a version this fixture, this module's `_ACCEPTED_TLS_VERSIONS`, its README, or its lessons name as accepted — it is not TLS 1.2 or TLS 1.3, it is a string that a naive numeric parse conflates with `1.3`. This is the identical *shape* of defect this module's own review has now found four times running in the same three-check function — a plausible alternate implementation that agrees with the real fix on every input the current suite happens to try and disagrees on inputs it does not — and it is the *third* distinct fake found specifically in the version check (after the lexicographic-bound fake and the forward-compatible-allow-list fake, both closed in round 2), showing that closing two gaps in one check is still not evidence the check is exhausted. I restored `fixed/app.py` immediately afterward: `md5sum` before and after matched (`ebdf983ea6753464cffb6dee0a669513`), `diff` against the pre-edit copy showed no difference, and `git status --porcelain` was empty before I moved on.

**Other checks performed, all confirmed clean, no regression from round 3:**

- The trusted-CA check (`if cert_trusted_ca is not True: return False`) has a domain of exactly three values (`True`, `False`, `None`, per the function's `bool | None` signature), and the current suite tests all three with distinct expected outcomes (`True` → 200 via the normal-case test; `False` → 502 via the explicitly-untrusted-CA anti-fake test; `None` → 502 via the unknown-trust-state test). Any function reproducing this truth table over a three-value domain is extensionally identical to `is not True` on every possible input — there is no room left for a fifth fake in this specific check, unlike the hostname and version checks, whose domains are unbounded strings.
- Reconstructed two more of the module's own eight documented fakes myself, independent of the round-3 session's reconstructions: the trust-state `is None` fake (`if cert_trusted_ca is None: return False`) gives `16 passed, 1 failed` (`test_explicitly_untrusted_ca_is_rejected_even_with_matching_hostname_and_version`), and the hostname-prefix fake (`presented_hostname.startswith(expected_hostname)`) gives `16 passed, 1 failed` (`test_anti_fake_hostname_prefix_match_is_rejected`) — both exactly as `README.md` and `keys/2.2.md` claim.
- Grepped `spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, the lab `README.md`, and `content/assessment/keys/2.2.md` for stale test-count phrasing left over from any prior round (`"16 of 16"`, `"15 of 15"`, `"14 of 14"`, `"of 16 pass"`, `"of 16 fail"`). No hits outside `module.yaml`'s own changelog, which correctly narrates past counts as history, not a live claim.

**Verdict: neither gate is granted, for the fourth time running.** Round 3's finding is genuinely closed — I verified this by independent reconstruction, not by trusting the fix commit's own account, and it holds up, as do all counts, citations, and mechanical gates checked above. But a fourth construction, targeted at the one check (the version check) that had already absorbed two prior fixes in round 2, found a real, demonstrated, exploitable gap the current 17-test suite does not catch: a numeric-range parse of the TLS version string, a natural good-faith implementation choice for anyone thinking of "1.2 through 1.3" as a numeric interval rather than an opaque two-element string set. Per this repo's governing rule, a real blocker withholds every grant regardless of how much surrounding work is independently confirmed correct, and regardless of how many prior rounds already closed other findings — including two findings in this exact same check.

**To close:** add one test in `tests/test_tls_hop.py` sending a `tls_version` string that is numerically inside `[1.2, 1.3]` when parsed as a float but is not a member of `_ACCEPTED_TLS_VERSIONS` as a string — for example `tls_version="1.30"` or `tls_version="1.25"`, with hostname and CA trust both otherwise valid — and assert `502`. This isolates "the version string is exactly `\"1.2\"` or `\"1.3\"`" from "the version string parses to a float numerically between 1.2 and 1.3," a fourth and distinct near-miss shape from the two round-2 already covers (a lexicographic string bound, and a hand-written wider string set). Update `README.md`'s and `keys/2.2.md`'s fake counts and lists to record a ninth verified fake once this is added, and note in whichever file tracks the version check's history that three, not two, structurally distinct fakes have now been found and closed for that one line.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A fourth re-review that found a live blocker may not write this line, and finding three prior rounds' issues closed does not change that.

## Addendum, 2026-09-23 — fifth-round re-review at `24c221d`: round-4 finding genuinely closed, but a fifth, structurally distinct gap found in the same version check

**Independence statement.** I am a fifth, separate re-review pass, on commit `24c221d` ("2.2: close fourth review round's numeric-range TLS-version gap, plus the cross-failure it surfaced"), which is `HEAD` of `origin/deepen/2.2`. I checked out this exact commit fresh (`git checkout -B deepen/2.2-review origin/deepen/2.2`) into my own worktree rather than trusting any prior session's cached state, and confirmed the working tree was clean before touching anything. I read all four addenda above in full before touching any file. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum` (`ebdf983ea6753464cffb6dee0a669513` before and after) and by `diff` against a pre-edit copy showing no difference, and by `git status --porcelain` returning empty — before I moved to the next check, every time.

**Round 4's finding — CLOSED, verified by reconstructing the exact fake from scratch before reading the fix's own description.** I read `tests/test_tls_hop.py` end to end first, then wrote a numeric-range parse (`float`, `1.2 <= version_num <= 1.3`) in place of `if tls_version not in _ACCEPTED_TLS_VERSIONS: return False` in `fixed/app.py`'s `hop_is_trustworthy`, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 17 passed
FAILED tests/test_tls_hop.py::test_anti_fake_version_string_that_parses_in_range_is_still_rejected
```

Fails exactly and only the test added to close this gap. Restored; `diff` and `git status --porcelain` both clean.

**Full pass/fail counts, re-run myself, matching every file's own claim exactly:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
12 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
18 passed
```

This matches, word for word and number for number, `labs/2.2/2.2-request-path/README.md` L28-29 ("6 of 18 pass" / "18 of 18 pass"), `content/modules/2/2.2/lessons/05-verify.md`'s Mermaid diagram ("6 of 18 pass" / "18 of 18 pass"), `content/modules/2/2.2/assessment/items.md` L104 and `assessment/rubric.md` L13 ("12 of 18 failing" / "18 of 18 passing"), and `content/assessment/keys/2.2.md` L11 ("must fail 12 of 18" / "must pass all 18"). `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`.

**A fifth, independently-constructed adversarial fake — for the version check specifically, the same check rounds 2 and 4 had already found two prior gaps in — passes the entire current 18-test suite, including all four existing version anti-fake tests.** Per this task's explicit instruction to construct my own candidate and to consider "normalizing/stripping the string before comparing" as one plausible shape, I built exactly that: a good-faith author defensively stripping incidental whitespace (a trailing newline from a header value, a stray leading space) before checking set membership, something many real HTTP-adjacent codebases do reflexively for header-derived strings:

```python
# real fixed code
if tls_version not in _ACCEPTED_TLS_VERSIONS:
    return False
# adversarial fake, in its place
if tls_version.strip() not in _ACCEPTED_TLS_VERSIONS:
    return False
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
18 passed
```

**All eighteen tests pass, including every anti-fake test in the file.** None of the six distinct version strings any test in `test_tls_hop.py` sends (`"1.1"`, `"1.2"`, `"1.3"`, `"1.4"`, `"1.9"`, `"1.30"`) contains any whitespace, so `.strip()` is a no-op on every one of them and the fake's behavior is byte-identical to the real fix on the entire existing suite. I confirmed this is a real, exploitable behavioral divergence, not a harmless corner, by running both implementations directly through the FastAPI `TestClient` with `expected_hostname` and `presented_hostname` both `"a.securecollab.internal"`, `cert_trusted_ca=True`, `tls_version="1.2\n"` (a version string with a trailing newline — the kind of incidental whitespace a real header-parsing or logging pipeline can introduce):

```
fake in place, tls_version='1.2\n' -> 200
```

I then restored the real fixed code and confirmed it correctly returns `502` for the identical input (`"1.2\n" not in {"1.2", "1.3"}` is `True`, since `"1.2\n" != "1.2"`). The fake relays a hop presenting a version string that is not one of the two accepted strings as trustworthy; the real fix correctly refuses it. This is the identical *shape* of defect this module's own review has now found five times running in the same three-check function — a plausible alternate implementation that agrees with the real fix on every input the current suite happens to try and disagrees on inputs it does not — and it is the *fourth* distinct fake found specifically in the version check (after the lexicographic-bound fake, the forward-compatible-allow-list fake, and the numeric-range fake, all closed in rounds 2 and 4), showing that closing three gaps in one check is still not evidence the check is exhausted. I restored `fixed/app.py` immediately afterward: `md5sum` before and after matched (`ebdf983ea6753464cffb6dee0a669513`), `diff` against the pre-edit copy showed no difference, and `git status --porcelain` was empty before I moved on.

**Other checks performed, all genuinely adversarial, all confirmed clean, no regression from round 4:**

- **Hostname check, fourth shape attempted.** Tried case-insensitive comparison (`presented_hostname.casefold() == expected_hostname.casefold()`) and trailing-dot/FQDN normalization (`presented_hostname.rstrip(".") == expected_hostname.rstrip(".")`). Neither passes the full suite as a *plausible-but-wrong* fake in the sense the task asks for, because I could not find an input among the current tests or a newly constructed one where either normalization diverges from `==` on a case this module's own hostnames would plausibly carry (all hostnames used throughout are already lowercase with no trailing dot, and introducing one is testing a different property — DNS FQDN equivalence — that this module's spec and lessons do not claim `hop_is_trustworthy` handles). I did not find a fourth exploitable hostname gap distinct from the three already closed (prefix, substring-anywhere, suffix-without-boundary).
- **Trust-CA check, re-verified the three-value-domain argument myself rather than accepting it.** `hop_is_trustworthy`'s signature is `cert_trusted_ca: bool | None`, and grepping the function body confirms the only comparison is `if cert_trusted_ca is not True`. Enumerated all three possible values by hand: `True` → check passes (proceeds to version/hostname); `False` → check fails (`is not True` is `True`) → rejected; `None` → check fails (`is not True` is `True`) → rejected. I tried constructing a fake using `bool()` coercion (`if not bool(cert_trusted_ca): return False`) on the theory that a good-faith author might coerce rather than compare identity — `bool(True)` is `True` (passes, matches), `bool(False)` is `False` (rejected, matches), `bool(None)` is `False` (rejected, matches) — extensionally identical on this domain, so it is not a distinguishable fake, confirming (not merely accepting) the existing addendum's argument that this check's three-value domain is exhausted by the current three tests.
- **C1 (cache key) and C2 (forwarded identity), re-attempted independently.** Tried a fake that keys the cache on `(note_id, company.lower())` (case-folding company before keying) — no test uses mixed-case company identifiers (`companyA`, `companyB`, `companyZ`, `companyQ` are used consistently), so this does not diverge from the real `(note_id, company)` key on the current suite, and is not a distinguishable fake. Tried a fake for `_resolve_company` that reads `x_company` only when it exactly equals the caller's own bound company (a no-op in practice, since the real fix already ignores `x_company` entirely) — behaviorally identical to the real fix on every input, not a fake at all. Tried sending `X-Company` with a company value containing leading/trailing whitespace (`" companyB"`) to see if any normalization gap exists in the real `_resolve_company` — the real fixed function never reads `x_company` at all, so no whitespace-normalization fake is even meaningful here; this line of attack applies only to the version check, where whitespace-sensitive comparison is actually live. Found no new C1/C2 gap.
- Reconstructed the round-4 numeric-range fake and the round-2 lexicographic-bound fake once each, independent of the round-3/4 sessions' own reconstructions, and got the documented counts (17 of 18 and 15 of 18 respectively, failing exactly the tests each README/keys entry names).
- Grepped `spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, the lab `README.md`, and `content/assessment/keys/2.2.md` for stale test-count phrasing left over from any prior round (`"17 of 17"`, `"16 of 16"`, `"15 of 15"`, `"14 of 14"`, `"of 17 pass"`, `"of 17 fail"`, `"eight fakes"`, `"nine fakes"` used inconsistently). No hits outside `module.yaml`'s own changelog, which correctly narrates past counts as history, not a live claim; every live, current-tense reference reads "18" and "nine fakes" consistently (the ninth being the round-4 numeric-range fake), and no file claims a count that mismatches what I ran myself.
- Coverage-contract and teaching-claims citations re-derived independently from `items.md`'s own `Claim assessed` tags, cell by cell: C1 → #1, #4, #6, #7, #8; C2 → #2, #4, #7; C3 → #3, #7, #8; C4 → #5, #7; C5 → #6, #7, #8 — matches `spec.md`'s teaching-claims table exactly, no addition or omission, no regression from round 4's finding that this defect class is clean here.
- Metadata honesty and shared `pins.yaml`, re-confirmed: `content/modules/2/2.2/module.yaml` still reads `status: draft`, no `reviewer`/`depth`/`quality`/`lastReviewedAt`/`nextReviewAt` set by any authoring or fix pass. `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry on this branch still reads `reviewedAt: "2026-09-22"` with the corrected URL; `origin/deepen/2.3`'s parallel entry (fetched fresh via `git fetch origin`, not from memory) still reads `reviewedAt: "2026-09-23"` with the identical corrected URL and its own independent narrative — the same cross-branch note-text conflict already on record four rounds running, one day apart, not a new or worsened problem. Not touched, per this task's instruction.
- Mechanical gates, run myself: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.

**Verdict: neither gate is granted, for the fifth time running.** Round 4's finding is genuinely closed — I verified this by independent reconstruction, not by trusting the fix commit's own account, and it holds up, as do all counts, citations, and mechanical gates checked above. But a fifth construction, targeted at the one check (the version check) that had already absorbed three prior fixes across rounds 2 and 4, found a real, demonstrated, exploitable gap the current 18-test suite does not catch: a whitespace-stripping normalization of the TLS version string before set-membership comparison, a natural defensive habit for anyone treating `tls_version` as an ordinary header-derived string rather than an opaque, already-canonical token. Per this repo's governing rule, a real blocker withholds every grant regardless of how much surrounding work is independently confirmed correct, and regardless of how many prior rounds already closed other findings — including three findings in this exact same check.

**To close:** add one test in `tests/test_tls_hop.py` sending a `tls_version` string that carries incidental leading/trailing whitespace around an otherwise-non-accepted value (for example `tls_version="1.2 "` is NOT sufficient by itself, since `"1.2 ".strip() == "1.2"` which the *real* fix would also need to reject and currently does — the isolating case is a version whose *stripped* form is still not in `_ACCEPTED_TLS_VERSIONS`, e.g. `tls_version=" 1.9"` or `tls_version="1.30\n"`, with hostname and CA trust both otherwise valid) and assert `502`; additionally, to specifically catch the whitespace-around-an-accepted-value case (`"1.2\n"`, `" 1.3"`), add a companion test asserting `502` for those too, since the real fixed code treats them as distinct strings from `"1.2"`/`"1.3"` and must reject them, while a `.strip()`-based fake would wrongly accept them. This isolates "the version string is exactly one of the two canonical strings, byte for byte" from "the version string, after discarding incidental whitespace, names one of the two accepted versions" — a fifth and distinct near-miss shape from the three round-2/round-4 already cover (a lexicographic string bound, a hand-written wider string set, and a numeric-range parse). Update `README.md`'s and `keys/2.2.md`'s fake counts and lists to record a tenth verified fake once this is added, and note in whichever file tracks the version check's history that four, not three, structurally distinct fakes have now been found and closed for that one line.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A fifth re-review that found a live blocker may not write this line, and finding four prior rounds' issues closed does not change that.

## Addendum, 2026-09-23 — sixth-round re-review at `1e6340f`: round-5 finding genuinely closed, but a sixth, structurally distinct gap found in the same version check

**Independence statement.** I am a sixth, separate re-review pass, on commit `1e6340f` ("2.2: close fifth review round's whitespace-strip TLS-version gap, plus two cross-failures it surfaced"), which is `HEAD` of `origin/deepen/2.2`. My worktree's default branch was stale (pointed at unrelated `main` history); I checked out this exact commit fresh (`git checkout -B deepen/2.2 origin/deepen/2.2`) and confirmed the working tree was clean before touching anything. I read all five addenda above in full before touching any file. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum` (`ebdf983ea6753464cffb6dee0a669513` before and after every edit) and by `git status --porcelain` returning empty — before I moved to the next check, every time.

**Round 5's finding — CLOSED, verified by reconstructing the exact fake from scratch before reading the fix's own description.** I read `tests/test_tls_hop.py` end to end first, then wrote `if tls_version.strip() not in _ACCEPTED_TLS_VERSIONS: return False` in place of the bare membership test in `fixed/app.py`'s `hop_is_trustworthy`, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 18 passed
FAILED tests/test_tls_hop.py::test_anti_fake_version_string_with_incidental_whitespace_is_rejected
```

Fails exactly and only the test added to close this gap. Restored; `md5sum`/`git status --porcelain` both clean.

**Full pass/fail counts, re-run myself, matching every file's own claim exactly:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
13 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
19 passed
```

This matches, word for word and number for number, `labs/2.2/2.2-request-path/README.md` L28-29 ("6 of 19 pass" / "19 of 19 pass"), `content/modules/2/2.2/assessment/items.md` L104 and `assessment/rubric.md` L13 ("13 of 19 failing" / "19 of 19 passing"), and `content/assessment/keys/2.2.md` L11 ("must fail 13 of 19" / "must pass all 19"). `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`. `content/modules/2/2.2/spec.md` states no absolute test count anywhere (checked by grep for `"19"`, `"of 19"`, `"nineteen"` — no hits), so it has nothing to be stale.

**A sixth, independently-constructed adversarial fake — for the version check specifically, the one check that has now absorbed four prior fixes across rounds 2, 4, and 5 — passes the entire current 19-test suite, including all seven existing version anti-fake tests.** Per this task's explicit instruction to try splitting the version string into an `(major, minor)` integer tuple and to check whether that is truly extensionally identical to string-set membership, I built exactly that, guarded with an `isdigit()` check specifically so this fake would *not* be caught by round 5's already-closed whitespace gap (an unguarded `int()` parse, e.g. `int("2\n")`, also strips whitespace and would be caught immediately — I confirmed this first, then hardened the fake past it, the same way a real second-pass author closing round 5's finding might defensively add a digit check without reasoning further about what `str.isdigit()` itself accepts):

```python
# real fixed code
_ACCEPTED_TLS_VERSIONS = {"1.2", "1.3"}
if tls_version not in _ACCEPTED_TLS_VERSIONS:
    return False
# adversarial fake, in its place
_ACCEPTED_TLS_VERSION_TUPLES = {(1, 2), (1, 3)}
parts = tls_version.split(".")
if len(parts) != 2 or not (parts[0].isdigit() and parts[1].isdigit()):
    return False
version_tuple = (int(parts[0]), int(parts[1]))
if version_tuple not in _ACCEPTED_TLS_VERSION_TUPLES:
    return False
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
19 passed
```

**All nineteen tests pass, including every anti-fake test in the file — the whitespace one included, because `"2\n".isdigit()` is `False`, so `"1.2\n"` is correctly rejected by this fake for a different reason than the real fix rejects it (malformed digit shape, not string inequality), landing on the same `502` by coincidence.** None of the seven version strings any test in `test_tls_hop.py` sends (`"1.1"`, `"1.2"`, `"1.3"`, `"1.4"`, `"1.9"`, `"1.30"`, `"1.2\n"`) has a leading zero on either the major or minor component, so `int(parts[i])` never differs from what the digit string spells out for any tested input, and the fake's behavior is indistinguishable from the real fix on the entire existing suite. I confirmed this is a real, exploitable behavioral divergence, not a harmless corner, first by direct probe against the fake and then against the restored real code, both called directly (not only through the FastAPI test client) to isolate the predicate itself:

```
$ python3 -c '...'   # fake in place
fake with 01.2: True
fake with 1.02: True
$ python3 -c '...'   # real fixed code, restored
real with 01.2: False
real with 1.02: False
```

The fake relays a hop presenting `"01.2"` or `"1.02"` as trustworthy (`True` → `200`); the real fix correctly refuses both (`False` → `502`), because neither string is a member of `_ACCEPTED_TLS_VERSIONS = {"1.2", "1.3"}` — `"01.2" != "1.2"` and `"1.02" != "1.2"` as strings, even though `int("01") == int("1") == 1` and `int("02") == int("2") == 2` make their parsed tuples collide with an accepted one. This is the identical *shape* of defect this module's own review has now found six times running in the same three-check function — a plausible alternate implementation that agrees with the real fix on every input the current suite happens to try and disagrees on inputs it does not — and it is the *fifth* distinct fake found specifically in the version check (after the lexicographic-bound fake, the forward-compatible-allow-list fake, the numeric-range-parse fake, and the whitespace-strip fake, closed across rounds 2, 4, and 5), confirming yet again that closing every fake found so far is still not evidence the check is exhausted: an integer-tuple parse is extensionally identical to string-set membership only for inputs with no leading zeros, and nothing in this fixture, this module's spec, or its lessons ever states that constraint. I restored `fixed/app.py` immediately afterward each time I tested a variant: `md5sum` before and after matched (`ebdf983ea6753464cffb6dee0a669513`), and `git status --porcelain` was empty before I moved on.

I also verified the analogous *unguarded* int-tuple parse (a `try/except ValueError` around a bare `int()` split, with no `isdigit()` check) independently — it is caught by the existing whitespace test (`int("2\n") == 2`, same coercion the numeric-range fake already exploited in round 4), confirming it is not a *new* gap by itself; only the `isdigit()`-hardened version, which a real author might write specifically *because* round 5 just closed a whitespace-parsing gap, survives the full suite. This is worth recording explicitly: the isolating input for this sixth fake (a zero-padded version component) is a different near-miss shape from all five already-closed ones, not a rerun of the whitespace case with different guard code.

**Other checks performed, all genuinely adversarial, all confirmed clean, no regression from round 5:**

- **Hostname check, re-attempted with the two shapes round 5 already tried, confirmed the same conclusion by rebuilding the argument myself rather than trusting the addendum's prior wording.** Case-insensitive comparison (`presented_hostname.casefold() == expected_hostname.casefold()`) does pass the full 19-test suite as an empirical matter (I ran it: `19 passed`), because every hostname any test sends is already lowercase. But I do not treat this as a genuine gap distinct from round 5's conclusion: RFC 6125 §6.4.1 specifies DNS-name comparison for certificate validation as case-insensitive by definition, so a case-insensitive `==` is not a plausible-but-wrong near-miss of "compare hostnames" here — it is a defensible, arguably more textbook-correct implementation of the same claim, not a different claim smuggled in under a similar-looking check, unlike the prefix/substring/suffix fakes (which are wrong on their own terms, not merely untested). I also tried IDN/Unicode homograph normalization — comparing after `hostname.encode("idna")` round-tripping, and comparing NFKC-normalized forms (e.g. a Cyrillic "а" (U+0430) standing in for Latin "a") — and found no divergence constructible against this fixture's synthetic hostnames, because `hop_is_trustworthy` never claims to perform IDNA processing at all (it receives `presented_hostname` and `expected_hostname` as already-decoded Python strings, with no punycode or Unicode-confusable handling anywhere in the module's stated scope), so a homograph attack is a gap in an upstream decoding step this module explicitly does not model, not in this predicate. No new hostname gap found.
- **Trusted-CA check, re-verified the three-value-domain argument a third time, from a new angle (truthy/falsy coercion of a non-bool value).** The function's signature is `cert_trusted_ca: bool | None`, so at the type level only `True`, `False`, and `None` are ever passed by a caller respecting the signature; I confirmed the current suite tests all three with distinct expected outcomes, as rounds 4 and 5 already recorded. I then asked whether the type signature is actually *enforced* anywhere in this fixture, since Python does not runtime-check annotations — I sent the relay endpoint a raw JSON payload with `"cert_trusted_ca": 1` (an int, not a bool, which JSON's own type system would happily produce and FastAPI's default `dict` payload type does not reject) directly through the `TestClient`, bypassing the annotation entirely, against the real fixed code: `hop_is_trustworthy(..., cert_trusted_ca=1, ...)` → `1 is not True` evaluates to `True` (since `1 == True` but `1 is not True`, identity, not equality, is what the real code checks) → correctly returns `False`/`502`. This is actually a point *in favor* of the real check's robustness (using `is not True` rather than `not cert_trusted_ca` or `bool(x) is not True` deliberately excludes truthy-but-not-`True` values like `1`), not a gap — I could not construct any JSON-representable value that this check accepts other than the literal boolean `True`. No new CA-check gap found; the type signature's three-value framing is moot for the reason the task anticipated (`bool | None` combined with `is not True` rather than a coercing comparison), and I have now confirmed that by direct construction rather than by accepting the annotation at face value.
- **C1 (cache key), one genuinely new construction: a string-concatenation key in place of the tuple key.** I rewrote `_CACHE` to be keyed on `f"{note_id}:{company}"` (a single string) instead of `(note_id, company)` (a tuple), the classic "I'll just join them with a separator" mistake that can allow a delimiter-collision cache-key bypass in general. This also passes the full 19-test suite (`19 passed`) empirically. I then checked, by construction rather than by stopping at the passing test run, whether this is an *actually exploitable* divergence in this specific fixture: since `company` is always one of exactly four fixed, equal-length, colon-free literal strings resolved server-side from the caller's own API key (never attacker-supplied, per C2's own fix), and every one of those four strings differs in its last character (`companyA`/`companyB`/`companyQ`/`companyZ`), an attacker bound to their own company cannot choose a `note_id` whose concatenation with their own company's fixed suffix reproduces a target string ending in a *different* company's suffix — the trailing 9 characters (`:` plus 8-character company name) of any two colliding strings would need to be textually identical, which requires the two `company` values themselves to be equal. I confirmed this algebraically (`note_id_attacker + ":" + "companyA"` can never equal `note_id_victim + ":" + "companyQ"` for any string `note_id_attacker`, since the two candidate strings' last 9 characters differ and cannot be made equal by varying only the attacker's own note id) rather than only trusting the empirical pass. This is a genuinely new construction attempt (a different mechanism — string-concatenation delimiter collision — from round 5's case-folding attempt), and it also finds no exploitable gap, for a company-set-specific structural reason that would not hold if a fifth company name shared a suffix with another (worth noting as a fixture-specific accident, not a general property of `f"{note_id}:{company}"` as a pattern — a future module reusing this key-construction idiom with attacker-influenced or variable-length identifiers should not read this as an endorsement of the pattern itself).
- **C2 (forwarded identity), reconfirmed no gap via the same reasoning as round 5, no new mechanism found this round beyond the one just described for C1** (both defects share the same `company` value, and the concatenation analysis above covers both cache-key and forwarded-identity angles, since `_resolve_company`'s real fix never reads `x_company` at all and therefore has no string-shaped input left to construct a fake against).
- Reconstructed the round-5 whitespace-strip fake and the round-4 numeric-range fake once each, independent of prior sessions' own reconstructions, and got the documented counts (18 of 19 and 17 of 19 respectively, failing exactly the tests each README/keys entry names).
- Grepped `content/modules/2/2.2/spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, `labs/2.2/2.2-request-path/README.md`, and `content/assessment/keys/2.2.md` for stale test-count phrasing left over from any prior round (`"18 of 18"`, `"17 of 17"`, `"16 of 16"`, `"15 of 15"`, `"14 of 14"`, `"of 1[4-8] pass"`, `"of 1[4-8] fail"`, `"eleven"`, `"twelve"` used inconsistently). No hits outside `content/modules/2/2.2/module.yaml`'s own changelog, which correctly narrates past counts as history (e.g. "12 of 14 pass", "14 of 16", "14 of 17" — all describing what a specific historical fake reconstruction produced at the time, not a live claim about the current suite). Every live, current-tense reference reads "19", "6 of 19", or "13 of 19" consistently.
- Coverage-contract and teaching-claims citations re-derived independently from `items.md`'s own `Claim assessed` tags, cell by cell: C1 → #1, #4, #6, #7, #8; C2 → #2, #4, #7; C3 → #3, #7, #8; C4 → #5, #7; C5 → #6, #7, #8 — matches `spec.md`'s teaching-claims table (L45-49) exactly, no addition or omission, no regression from round 5's finding that this defect class is clean here. Also re-checked the four coverage-contract outcome rows (L59-62) against the same tags: row 1 (C1,C2 → items #1,#2,#4) — all three items' own `Outcome:` text matches the row's outcome text and their claims are a covering subset; row 2 (C3,C4 → item #3) — item #3 is tagged `C3` only, and no item in this table is separately tagged for the C4 half of this specific outcome's claim column; row 3 (C1,C2,C4 → item #5) — item #5 is tagged `C4` only; row 4 (C1,C2,C5 → items #6,#7,#8) — all three items' claims cover a superset including C5. Rows 2 and 3's partial claim-tag coverage (an outcome naming two or three claims while its cited item(s) are tagged with only one of them) is unchanged from the *first* review round's own explicit check of these same four rows, which read this same pattern as "correct" on the basis of matching `Outcome:` text rather than requiring every named claim to carry its own dedicated item tag — I am not treating this as a new or reopened finding, since neither the table's structure nor any cited item has changed across all six rounds, and the original round's judgment on this specific point stands unless a future task explicitly asks this review to revisit that judgment call itself.
- Metadata honesty and shared `pins.yaml`, re-confirmed: `content/modules/2/2.2/module.yaml` still reads `status: draft`, no `reviewer`/`depth`/`quality`/`lastReviewedAt`/`nextReviewAt` set by any authoring or fix pass (checked lines 7, 160-162 directly). `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry on this branch still reads `reviewedAt: "2026-09-22"` with the corrected URL (`https://owasp.org/projects/asvs`); `origin/deepen/2.3`'s parallel entry (fetched fresh via `git fetch origin deepen/2.3`, not from memory or cache) reads `reviewedAt: "2026-09-23"` with the identical corrected URL and its own independent dead-link narrative appended after 2.2's — the same cross-branch note-text conflict already on record five rounds running, one day apart, still just that (no factual disagreement about the URL itself, only duplicated "I fixed this" narration and a differing `reviewedAt` on a shared field that will conflict on merge). Not touched, per this task's instruction.
- Mechanical gates, run myself: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.

**Verdict: neither gate is granted, for the sixth time running.** Round 5's finding is genuinely closed — I verified this by independent reconstruction, not by trusting the fix commit's own account, and it holds up, as do all counts, citations, metadata honesty, the cross-branch `pins.yaml` note, and mechanical gates checked above. But a sixth construction, targeted again at the one check (the version check) that had already absorbed four prior fixes across rounds 2, 4, and 5, found a real, demonstrated, exploitable gap the current 19-test suite does not catch: an `isdigit()`-guarded integer-tuple parse of the two dot-separated components, deliberately hardened past the whitespace gap round 5 just closed, which is extensionally identical to exact string-set membership only for version strings with no leading zeros — a constraint nothing in this fixture, this module's claim, or its lessons ever states. Per this repo's governing rule, a real blocker withholds every grant regardless of how much surrounding work is independently confirmed correct, and regardless of how many prior rounds already closed other findings — including four findings in this exact same check, one of which (round 5's) was closed in the immediately preceding commit.

**To close:** add one test in `tests/test_tls_hop.py` sending a `tls_version` string with a leading zero on either the major or minor component that is otherwise a plausible near-miss of an accepted version — for example `tls_version="01.2"` or `tls_version="1.02"`, with hostname and CA trust both otherwise valid — and assert `502`. This isolates "the version string is exactly one of the two canonical strings `\"1.2\"`/`\"1.3\"`, byte for byte" from "the version string's dot-separated components parse to the same integers as one of the two accepted versions," a sixth and distinct near-miss shape from the four already closed (a lexicographic string bound, a hand-written wider string set, a numeric-range parse, and a whitespace strip). Update `README.md`'s and `keys/2.2.md`'s fake counts and lists to record an eleventh verified fake once this is added, and note in whichever file tracks the version check's history that five, not four, structurally distinct fakes have now been found and closed for that one line.

**Recommendation on the test-strategy question (a judgment call for the calling session or a human to weigh, not something I am implementing).** Four structurally distinct fakes now found in the same two-line check across five rounds (lexicographic bound, wider hand-written set, numeric-range parse, whitespace strip), plus this round's fifth (leading-zero integer-tuple parse), is enough evidence to treat the current strategy — one narrow, hand-picked isolating test added per discovered gap — as demonstrably not converging on an exhausted check, and I recommend against relying on it alone going forward. Each new test closes exactly the one input the reviewer happened to construct, and every round's fake has been *extensionally identical to the real check on the entire existing suite* by construction, which is precisely what a hand-picked isolating test cannot detect in advance — it only ever catches the *next* fake once someone has already thought of it, and five rounds of a skilled adversarial reviewer each finding a sixth one suggests the space of plausible near-miss string-comparison implementations for a two-string-literal set is larger than five or six iterations can exhaust by hand-picked example. A small property-based or systematically-parametrized test — for instance, generating a broad set of near-miss version strings programmatically (every string within edit-distance 1-2 of `"1.2"`/`"1.3"` via character insertion/deletion/substitution, every leading/trailing whitespace and zero-padding variant, every numeric neighbor at small perturbations, every case variant even though case is moot here) and asserting `502` for every one not literally equal to `"1.2"` or `"1.3"`, plus explicit `200` assertions for the two literal accepted strings themselves — would falsify a much larger space of alternate implementations in one test than any single hand-picked case can, including implementations no reviewer has thought of yet (an integer-tuple parse with a *different* zero-padding-tolerant digit check than `isdigit()`, for instance, or a base-conversion quirk not yet tried). I recommend the next fix pass add such a test (or at minimum a table-driven parametrized test enumerating a deliberately large set of near-miss strings in one test function) *in addition to* keeping the existing hand-picked anti-fake tests, which remain valuable as named, readable regression markers for each specific defect class this review has now documented — the two approaches are complementary, not a replacement of one for the other, and dropping the named tests would lose the pedagogical value of `README.md`'s own fake catalogue for learners reading this lab's history.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A sixth re-review that found a live blocker may not write this line, and finding five prior rounds' issues closed does not change that.

## Addendum, 2026-09-23 — seventh-round re-review at `195baed`: sixth-round finding genuinely closed by the sweep extension, but the sweep itself has a gap, and a seventh, structurally distinct fake exploits it

**Independence statement.** I am a seventh, separate re-review pass, on commit `195baed` ("Merge sixth review's addendum ... with the sweep-test close"), `HEAD` of `origin/deepen/2.2`. My worktree's own branch was unrelated stale history (an agent-scratch branch tracking `main`), so I created a fresh local branch (`review-2.2-work`) directly from `origin/deepen/2.2` at this exact commit and confirmed the working tree was clean before touching anything. I read all six addenda above in full before touching any file. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum` (`ebdf983ea6753464cffb6dee0a669513` before and after every edit, matching every prior round's own recorded value) and by `git status --porcelain` returning empty — before I moved to the next check, every time. I ran two throwaway scratch Python files inside the lab directory to drive these probes and deleted both (`git status --porcelain` clean) before finishing.

**Preliminary task: reconstructed the sixth review's own fake and confirmed it is now closed exactly as the changelog and prior addendum describe.** Wrote the `.isdigit()`-guarded `(major, minor)` integer-tuple parse in place of the real membership check in `fixed/app.py`:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
2 failed, 33 passed
FAILED tests/test_tls_hop.py::test_anti_fake_version_near_miss_sweep_is_rejected[01.2]
FAILED tests/test_tls_hop.py::test_anti_fake_version_near_miss_sweep_is_rejected[1.02]
```

Fails exactly the two sweep cases named in `module.yaml`'s changelog, none of the eight dedicated tests — matching the closure claim precisely. Restored; `md5sum`/`git status --porcelain` both clean. This finding is genuinely closed and I am not re-opening it.

**Full pass/fail counts, re-run myself, matching every file's own claim exactly:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
29 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
35 passed
```

Matches `labs/2.2/2.2-request-path/README.md` L28-29 ("6 of 35 pass" / "35 of 35 pass"), `content/modules/2/2.2/lessons/05-verify.md`'s Mermaid diagram, `assessment/items.md` L104 and `assessment/rubric.md` L13 ("29 of 35 failing" / "35 of 35 passing"), and `content/assessment/keys/2.2.md` L11-12 ("must fail 29 of 35" / "must pass all 35"). `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`.

**Task item (a): the sweep exercises the real code through the real path.** `_relay()` in `test_tls_hop.py` calls `client.post("/internal/relay", json={...})`, and `client` (from `conftest.py`) is a `fastapi.testclient.TestClient` wrapping `mod.create_app()` for whichever `--impl` was selected — a real HTTP POST through FastAPI's routing and JSON body-parsing into the real `relay()` handler, which calls the real `hop_is_trustworthy()`. No shortcut; the parametrized sweep test calls the same `_relay()` helper every other test in the file uses. Confirmed by reading `conftest.py` and the top of `test_tls_hop.py` directly, not by trusting this description.

**Task item (b): all sixteen sweep cases are distinct, well-formed literals, escape sequences included.** Read `_VERSION_NEAR_MISSES` directly: `" 1.2"`, `"1.2 "`, `"\t1.2"`, `"1.3\t"`, `"1.2\r\n"`, `"01.2"`, `"1.02"`, `"1.20"`, `"1.2.0"`, `"+1.3"`, `"1.2e0"`, `"1.23"`, `"1.234"`, `"TLSv1.2"`, `"v1.3"`, `"1.2 or 1.3"` — sixteen entries, all pairwise distinct, `\t` and `\r\n` are genuine Python escape sequences (confirmed by printing `repr()` of each and by the pytest parametrize IDs themselves, which render literally as `\t1.2`, `1.3\t`, `1.2\r\n` in the failing-test output above and in the passing-run's collected test IDs) — no malformed literal, no accidental duplicate, no case that silently degrades to a no-op string.

**Task item (c) — the actual finding: the sweep is ASCII-only, and a Unicode-normalizing implementation of the version check is a plausible, well-motivated alternate implementation that passes the entire current 35-test suite while genuinely diverging from the real fix.** I tried several of the inputs the task suggested directly against the real fixed code first (`"1.2\x00"`, `"1.3."`, `" "`, `""` all correctly rejected — the real code is exact-string `==`/`in`, so it always handles these correctly; the open question was never whether the *real* code is wrong on these, but whether the *test suite* would catch a plausible *fake* that treats one of them, or something structurally like them, as accepted). Reasoning from there: every one of the sixteen sweep cases, and all eleven fakes this module's six review rounds have found and closed, varies the version string only within ASCII (whitespace, digits, punctuation, letters) — none tests a *non-ASCII* representation of a digit. A defensive habit at least as plausible as the already-closed whitespace-strip fake — and arguably a more textbook-recommended one, since Unicode Technical Standard #39 and common security guidance recommend NFKC-normalizing untrusted strings before comparison specifically to avoid confusable-character bugs — is to Unicode-normalize the incoming string before checking membership:

```python
# real fixed code
if tls_version not in _ACCEPTED_TLS_VERSIONS:
    return False
# adversarial fake, in its place
import unicodedata
normalized_version = unicodedata.normalize("NFKC", tls_version)
if normalized_version not in _ACCEPTED_TLS_VERSIONS:
    return False
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
35 passed
```

**All thirty-five tests pass, including every dedicated anti-fake test and all sixteen sweep cases.** None of the sweep's sixteen near-miss strings contains a non-ASCII character, so NFKC normalization is a no-op on every one of them (and on every other string any test in the file sends), and the fake's behavior is indistinguishable from the real fix on the entire existing suite. I then confirmed this is a real, exploitable divergence — not a harmless corner — first by calling `hop_is_trustworthy` directly with the fake in place and `tls_version="１.２"` (U+FF11 FULLWIDTH DIGIT ONE, U+FF0E FULLWIDTH FULL STOP, U+FF12 FULLWIDTH DIGIT TWO — a string that renders visually as "1.2" but is not ASCII "1.2"): the fake returns `True`. I restored the real fixed code and called it with the identical input: it returns `False`, because `"１.２" != "1.2"` as Python strings, and `unicodedata.normalize("NFKC", "１.２") == "1.2"` is what the fake exploits. I then re-confirmed this end-to-end through the actual `POST /internal/relay` path via `TestClient` (JSON transports the raw code points without loss), with the fake in place and hostname/CA-trust both otherwise valid: the endpoint returns `200 {"relayed": True}`. Restoring the real fixed code and re-running the identical request returns `502`. Both `md5sum` (`ebdf983ea6753464cffb6dee0a669513`) and `git status --porcelain` confirmed clean before and after every one of these edits.

This is the identical *shape* of defect this module's own review has now found seven times running in the same function — a plausible alternate implementation that agrees with the real fix on every input the current suite happens to try and disagrees on inputs it does not — and it is a structurally distinct near-miss shape from all eleven already-closed fakes (none of which involves a non-ASCII code point) and from all sixteen of the sweep's own cases (all of which are ASCII). It is also not limited to the version check: I tried the analogous NFKC-normalizing fake on the *hostname* comparison in the same function (`unicodedata.normalize("NFKC", presented_hostname) == unicodedata.normalize("NFKC", expected_hostname)` in place of the bare `==`) and it, too, passes the full 35-test suite — confirming this is a gap in the sweep's coverage of the general "string equality via an alternate, plausible normalization" class, not an accident specific to one of the two checks. I restored `fixed/app.py` to its original state after each construction; `md5sum` and `git status --porcelain` both confirmed clean.

**Other checks performed, all genuinely adversarial, all confirmed clean, no regression from round 6:**

- **C1/C2, one further attempt.** `company` values in `_resolve_company` and `_CACHE`'s key always come from a fixed, hardcoded four-entry dictionary (`API_KEYS`), never from any attacker-influenced string comparison — there is no `==`/`in`/normalization predicate on `company` anywhere in the fixed code for a Unicode- or whitespace-style fake to target, unlike `tls_version` and the hostnames, which are genuinely compared strings. I could not construct a plausible normalization-style fake for C1 or C2 for this structural reason, consistent with all six prior rounds finding nothing new here.
- **Hostname check, no additional non-Unicode gap found.** Beyond the Unicode-normalization gap described above (which is the same class as the version-check finding and not counted as a second, independent hostname-specific finding), I did not find a further ASCII-only hostname gap distinct from the three already closed (prefix, substring-anywhere, suffix-without-boundary) or the case-fold/IDNA angles round 6 already tried and correctly declined to treat as fakes.
- **CA-trust check, not re-litigated.** The three-value-domain argument (`bool | None`, exhausted by `True`/`False`/`None`, `is not True` deliberately excludes truthy-but-not-`True` coercions) has now been independently re-derived by three separate rounds (4, 5, 6) from different angles; I read the function body once more to confirm it is unchanged and did not attempt a fourth reconstruction, per the task's own guidance not to re-litigate this without a genuinely new angle.
- Grepped `content/modules/2/2.2/spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, `labs/2.2/2.2-request-path/README.md`, and `content/assessment/keys/2.2.md` for stale test-count phrasing from any prior round (`"34 of 34"`, `"19 of 19"`, `"18 of 18"`, `"of 3[0-4] pass"`, `"of 3[0-4] fail"`, `"ten fakes"`, `"nine fakes"`). No hits outside `module.yaml`'s own changelog narrating history correctly. Every live, current-tense reference reads "35", "6 of 35"/"29 of 35", and "eleven fakes" consistently. (Full excerpts recorded above under "All counts consistent" in this session's own working notes.)
- Coverage-contract and teaching-claims citations re-derived independently from `items.md`'s own `Claim assessed` tags, cell by cell: C1 → #1, #4, #6, #7, #8; C2 → #2, #4, #7; C3 → #3, #7, #8; C4 → #5, #7; C5 → #6, #7, #8 — matches `spec.md`'s teaching-claims table (L45-49) exactly, no addition or omission, no regression from any prior round.
- Metadata honesty and shared `pins.yaml`, re-confirmed: `content/modules/2/2.2/module.yaml` still reads `status: draft` (L7), `reviewer: pending independent quality and lab-safety review` (L160), `lastReviewedAt: null`, `nextReviewAt: null` (L161-162) — no authoring or fix pass has touched these across any of the seven rounds. `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry on this branch still reads `reviewedAt: "2026-09-22"` with the corrected `url: "https://owasp.org/projects/asvs"`; `origin/deepen/2.3`'s parallel entry (fetched fresh via `git fetch origin deepen/2.3`, not from cache) reads `reviewedAt: "2026-09-23"` with the identical corrected URL and its own independent dead-link narrative — the same benign cross-branch note-text conflict on record for all seven rounds now, one day apart, still just that: no factual disagreement about the URL, only duplicated "I fixed this" narration and a differing `reviewedAt` on a shared field that will conflict on merge. Not touched, per this task's instruction.
- Mechanical gates, run myself: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK` (shown above).

**Verdict: neither gate is granted, for the seventh time running.** Round 6's finding is genuinely closed — verified by independent reconstruction, not by trusting the changelog's own account — and it holds up, as do all counts, citations, metadata honesty, the cross-branch `pins.yaml` note, and mechanical gates checked above. But the task's specific instruction this round — ask whether the sweep itself has a design gap, rather than construct an eighth hand-picked ASCII near-miss — surfaced a real, demonstrated, exploitable one: the sweep, and every one of the eleven fakes found before it, has only ever varied the version (and hostname) strings within ASCII. A Unicode-normalizing comparison — at least as plausible a good-faith implementation choice as the whitespace-strip fake already closed in round 5, and arguably better-motivated by standard security guidance on string comparison — passes the entire current 35-test suite, including the sixteen-case sweep meant to generalize past hand-picked cases, and genuinely disagrees with the real fix on a fullwidth-digit input that reaches the check unchanged through the real JSON/HTTP path. Per this repo's governing rule, a real blocker withholds every grant regardless of how much surrounding work is independently confirmed correct, and regardless of how many prior rounds — six, now — already closed other findings in this exact same function.

**To close:** add at least one non-ASCII case to `_VERSION_NEAR_MISSES` (for example the fullwidth string `"１.２"`, or a mixed-width variant such as `"1．2"`) and assert `502`, and add the analogous case to a hostname anti-fake test (a fullwidth or otherwise NFKC-confusable `presented_hostname` that normalizes to the expected one) and assert `502`. This isolates "the string is exactly the canonical ASCII literal, code point for code point" from "the string normalizes to the canonical literal under NFKC or a similar Unicode normalization," a seventh and distinct near-miss shape from the eleven already closed, and closes it for both checks the sweep's design gap affects, not only the one this round happened to construct first. Given six consecutive rounds have each found the sweep, or the checks it covers, incompletely closed, this may also be worth flagging to the human decision already recorded in `module.yaml`'s changelog as evidence that even a sixteen-case systematic sweep restricted to one representational class (ASCII) does not substitute for a scope statement about what representational classes the check is and is not required to handle — a documentation gap as much as a test gap, since neither `spec.md`, the lessons, nor the lab's `README.md` currently states whether non-ASCII/Unicode-confusable input is in or out of scope for `hop_is_trustworthy`, unlike the sweep's own docstring, which correctly names every ASCII-near-miss class it does cover.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A seventh re-review that found a live blocker may not write this line, and finding the sixth round's own issue closed does not change that.

## Addendum, 2026-09-23 — eighth-round re-review at `350962f`: round-7 finding genuinely closed, but a new, structurally distinct Unicode-adjacent gap found in the same two checks

**Independence statement.** I am an eighth, separate re-review pass, on commit `350962f` ("2.2: close seventh review's Unicode-normalization gap in both the version and hostname checks"), which is `HEAD` of `origin/deepen/2.2`. My worktree's own branch was unrelated stale history (an agent-scratch branch tracking `main`), so I hard-reset my own dedicated worktree branch to `origin/deepen/2.2` at this exact commit and confirmed the working tree was clean before touching anything. I read all seven addenda above in full before touching any file. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum` (`ebdf983ea6753464cffb6dee0a669513` before and after every edit) and by `diff` against a pre-edit copy, and by `git status --porcelain` returning empty — before I moved to the next check, every time. Scratch probe scripts lived only under `/tmp`, outside the repository tree, and were deleted before I finished.

**Round 7's finding — CLOSED, verified by reconstructing the exact fake from scratch before reading the fix's own description, against both checks it affects.** I wrote `unicodedata.normalize("NFKC", tls_version) not in _ACCEPTED_TLS_VERSIONS` in place of the bare membership test, and `unicodedata.normalize("NFKC", presented_hostname) == unicodedata.normalize("NFKC", expected_hostname)` in place of the bare `==`, in `fixed/app.py`'s `hop_is_trustworthy`, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
2 failed, 35 passed
FAILED tests/test_tls_hop.py::test_anti_fake_version_near_miss_sweep_is_rejected[１.２]
FAILED tests/test_tls_hop.py::test_anti_fake_hostname_unicode_confusable_is_rejected
```

Fails exactly and only the sweep case and dedicated test added to close this gap — nothing else regresses. Restored; `diff` against the pre-edit copy showed no difference, `git status --porcelain` was empty.

**Full pass/fail counts, re-run myself, matching every file's own claim exactly:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
31 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
37 passed
```

This matches, word for word and number for number, `labs/2.2/2.2-request-path/README.md` L28-29 ("6 of 37 pass" / "37 of 37 pass"), `content/modules/2/2.2/lessons/05-verify.md`'s Mermaid diagram ("6 of 37 pass" / "37 of 37 pass"), `content/modules/2/2.2/assessment/items.md` L104 and `assessment/rubric.md` L13 ("31 of 37 failing" / "37 of 37 passing"), and `content/assessment/keys/2.2.md` L11-12 ("must fail 31 of 37 tests" / "must pass all 37"). `content/modules/2/2.2/spec.md` states no bare absolute test count outside its own claim table (checked by grep for `"of 37"`, `"37 tests"`, `"thirty-seven"` — no hits), consistent with round 6's finding that it has nothing to go stale. Every one of the 31 vulnerable failures is a plain assertion on the security property, cross-checked by name against `keys/2.2.md` L11's own enumeration (`test_cache_key.py`'s two forbidden-outcome/anti-fake pairs for C1 and C2, plus `test_tls_hop.py`'s forbidden-outcome, boundary, and nine anti-fake tests — four dedicated hostname anti-fakes, four dedicated version-string anti-fakes, and the seventeen-case sweep — for C3): I ran the vulnerable variant myself and confirmed the `FAILED` list contains exactly those and no others, and that `test_unknown_trust_state_fails_closed` and `test_explicitly_untrusted_ca_is_rejected_even_with_matching_hostname_and_version` correctly do **not** appear in it, because `vulnerable/app.py`'s `hop_is_trustworthy` (`return cert_trusted_ca is True`) happens to fail closed on both `None` and `False` even though it checks nothing else — `keys/2.2.md`'s enumeration is honest about this, not merely convenient. `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`.

**Coverage-contract and teaching-claims citations — re-derived independently from `items.md`'s own `Claim assessed` tags, found unchanged and still fully correct.** `git diff 38b36a0 350962f --stat` shows this fix commit touched `items.md` by exactly 2 lines (a count update) and did not touch any `Claim assessed` tag. Re-read the tags directly: C1 → #1, #4, #6, #7, #8; C2 → #2, #4, #7; C3 → #3, #7, #8; C4 → #5, #7; C5 → #6, #7, #8 — identical to every one of the seven prior rounds' own derivations, matching `spec.md`'s teaching-claims table with no addition or omission.

**Sweep and dedicated-test infrastructure sanity-check (this round's assigned task item), mostly clean, one cosmetic inaccuracy found.**

- All seventeen `_VERSION_NEAR_MISSES` entries are pairwise distinct, well-formed literals (confirmed by reading the list directly and by pytest's own parametrize IDs in the collected-test and failed-test output above, which render each escape sequence literally: `\t1.2`, `1.3\t`, `1.2\r\n`, `１.２`).
- `test_anti_fake_hostname_unicode_confusable_is_rejected` genuinely exercises the real `/internal/relay` path: it calls the same `_relay()` helper (`client.post("/internal/relay", json={...})`) every other test in the file uses, and `client` (from `conftest.py`) is a real `TestClient` wrapping `create_app()` for the selected `--impl` — no shortcut, no bare function call.
- **One cosmetic, non-functional inaccuracy:** the comment at `test_tls_hop.py` L257-258 describes the sweep's fullwidth entry as rendering `"1.2"` where "each character is a FULLWIDTH Unicode code point, U+FF11/U+FF0E/U+FF12" — but I dumped the actual code points of that literal directly (`[c for c in line if ord(c) > 127]` on the source file) and the period between the two fullwidth digits is a plain ASCII `.` (U+002E), not the fullwidth full stop U+FF0E the comment names; only the two digits (U+FF11, U+FF12) are actually fullwidth. `unicodedata.normalize("NFKC", "１.２") == "1.2"` still holds regardless (ASCII `.` normalizes to itself), so the test's *behavior* is unaffected and the finding it demonstrates is real — this is a documentation-comment imprecision only, about which one of three characters is fullwidth, not a test bug, a wrong assertion, or a wrong expected status code. I am recording it because the task asked me to sanity-check the infrastructure for exactly this class of slip; I do not think it rises to blocker severity on its own and would not withhold a grant for it alone.

**A new, independently-constructed adversarial fake — for the same two checks round 7 just closed, using a Unicode angle round 7 did not try — passes the entire current 37-test suite.** Per this task's explicit instruction to look beyond NFKC specifically (NFKD, cross-script confusables, zero-width characters, `casefold()`), I first ruled out three of those four empirically before finding the one that works:

- **NFKD** (decomposed form) in place of NFKC on both checks: still fails the same two tests NFKC-closing fixed, because NFKD also folds `１`/`２`/fullwidth letters to their ASCII decompositions — not a new gap. (`2 failed, 35 passed`, restored, clean.)
- **Cross-script confusables** (e.g. Cyrillic а U+0430 for Latin a U+0061): I did not construct this as a fake, because `content/modules/2/2.2/spec.md`'s new "Known residuals" bullet (L70) already and explicitly disclaims exactly this — "homograph attacks using visually similar but code-point-distinct scripts in a genuinely registered hostname" — as out of scope, and reads as accurate rather than overclaiming: the module's claim is narrowly "the check itself must not normalize away a genuine mismatch," not general IDN/homograph security, and no plausible standard-library normalization (NFKC/NFKD/casefold) actually folds Cyrillic а onto Latin a, so treating this as an open gap would be holding the module to a claim it does not make and correctly says it does not make. This is not a new finding; the spec's own residual note already covers it honestly.
- **`casefold()` alone or combined with NFKC** on the hostname: round 6's addendum already examined case-insensitive hostname comparison and declined to treat it as a fake, correctly, since DNS-name comparison is case-insensitive by definition (RFC 6125 §6.4.1) and every hostname in this fixture is already lowercase, so no divergence is constructible. I re-confirmed this holds for the current 37-test suite and did not re-litigate it further, per the task's own guidance.
- **A zero-width character (U+200B ZERO WIDTH SPACE) surviving a "strip invisible/format characters" cleaning step — this is the new gap.** Python's `str.strip()` does not remove U+200B (`"​".isspace()` is `False`), so round 5's already-closed whitespace-strip fake does not reach it, and NFKC/NFKD normalization does not fold or remove it either (`unicodedata.normalize("NFKC", "1.2​")` and the NFKD form both return `'1.2​'` unchanged — I confirmed this directly). But stripping Unicode category **Cf** ("Format", which U+200B belongs to, alongside other zero-width and bidi-control characters) before comparing untrusted strings is itself a distinct, standard, security-guidance-recommended defensive habit — the same class of mitigation recommended against "trojan source"/invisible-character smuggling attacks, and at least as plausible a good-faith implementation choice as the already-closed NFKC fake, arguably more so in a security review context, since it is specifically motivated by the same untrusted-string-hygiene concern the module's own docstring cites in support of the NFKC fake it just closed. I wrote this in place of both bare comparisons in `fixed/app.py`:

```python
import unicodedata

def _strip_invisible(s: str) -> str:
    return "".join(ch for ch in s if unicodedata.category(ch) != "Cf")

# in hop_is_trustworthy, in place of the real fixed code:
if _strip_invisible(tls_version) not in _ACCEPTED_TLS_VERSIONS:
    return False
return _strip_invisible(presented_hostname) == _strip_invisible(expected_hostname)
```

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
37 passed
```

**All thirty-seven tests pass, including every anti-fake test in the file — the fullwidth sweep case and the Unicode-confusable hostname test both included**, because fullwidth digits (`１`, `２`) and fullwidth letters are Unicode category **Nd** and **Lo**/**Ll** respectively, not **Cf** — `_strip_invisible` leaves them untouched, so those two inputs still correctly fail membership/equality and the fake agrees with the real fix on them by coincidence, exactly the way the NFKC fake agreed with every pre-Unicode ASCII test by coincidence before round 7 found it. None of the thirty-seven existing test inputs (the sixteen ASCII sweep cases, the fullwidth sweep case, the four dedicated hostname anti-fakes including the fullwidth one, or any other test) contains a U+200B or any other Cf-category character, so this fake is behaviorally indistinguishable from the real fix on the entire current suite.

I confirmed this is a real, exploitable divergence — not a harmless corner — first by calling `hop_is_trustworthy` directly with the fake in place: `tls_version="1.2​"` (a trailing zero-width space) returns `True`, and a hostname with a zero-width space spliced into the middle of an otherwise-correct string (`"a.sec​urecollab.internal"` against expected `"a.securecollab.internal"`) also returns `True`. I then drove both through the real `POST /internal/relay` path via `TestClient` (JSON transports the raw code points losslessly) with the fake in place, hostname and CA trust otherwise valid: `tls_version="1.2​"` → `200 {"relayed": true}`. I restored `fixed/app.py` to the real code and re-ran the identical request: `502 {"detail": "upstream hop not trustworthy"}`, because `"1.2​" != "1.2"` and `"1.2​" not in {"1.2", "1.3"}` under plain string equality/membership, with no normalization at all. Both `md5sum` (`ebdf983ea6753464cffb6dee0a669513`) and `diff` against the pre-edit copy confirmed the restore was byte-for-byte exact; `git status --porcelain` was empty before I moved on.

This is the identical *shape* of defect this module's own review has now found eight times running across its two string-comparison checks — a plausible alternate implementation that agrees with the real fix on every input the current suite happens to try and disagrees on inputs it does not — and it is structurally distinct from all twelve fakes already closed (the five ASCII-only version-check fakes, the three ASCII-only hostname-check fakes, and the NFKC fake round 7 just closed for both checks): NFKC and NFKD normalization, confirmed above, do not touch U+200B at all, so a fix that closes the NFKC/NFKD gap does nothing against this one, and vice versa — an implementation could plausibly do either, both, or neither, and each choice needs its own isolating test. It also reaches both checks with the same one-line-per-check change, exactly the way round 7's NFKC fake did, for the same reason: both checks compare untrusted strings, and a "clean untrusted strings before comparing" helper applied once naturally covers both call sites.

**Other checks performed, all genuinely adversarial, all confirmed clean, no regression from round 7:**

- **C1 (cache key) and C2 (forwarded identity), one further fresh attempt each.** I looked for a Unicode-normalization-adjacent angle specific to these two claims, since the task asked for at least one genuinely fresh attempt: whether a `note_id` (from the URL path) built from Unicode-confusable or Cf-stripped characters could cause two textually-different note IDs to collide under some plausible normalizing cache-key implementation, defeating the cache-key claim (C1) the way the version/hostname checks were just defeated. I could not construct this, for a structural reason distinct from — but analogous to — round 6's and round 7's own conclusion: `company` (the other half of the `(note_id, company)` key) never passes through any string-comparison predicate at all in the fixed code — it comes only from a fixed four-entry dictionary lookup on the caller's own credential (`API_KEYS[api_key]`) — so there is no `==`/`in`/normalization call site on `company` for a Unicode fake to target, and a `note_id` collision within the *same* company's own two note IDs is not a forbidden outcome this module claims to prevent (C1 and C2 are both about cross-*company* leakage, not about one company's own note IDs colliding with each other). No new C1/C2 gap found; same conclusion as all seven prior rounds, reached via a genuinely different angle (Unicode/normalization rather than case-folding or string-concatenation).
- **CA-trust check, not re-litigated.** The three-value-domain argument (`bool | None`, exhausted by `True`/`False`/`None`, `is not True` deliberately excludes truthy-but-not-`True` coercions) has now been independently re-derived by four separate rounds (4, 5, 6, and implicitly re-confirmed by 7's structural argument that Unicode fakes have no string predicate to target here either); I read the function body once more to confirm it is unchanged (`if cert_trusted_ca is not True: return False`, still the first check) and did not attempt a fifth reconstruction, per the task's own guidance not to re-litigate this without a genuinely new angle, and I could not think of one beyond what round 6 already tried (`bool()` coercion) and round 7 already noted (no string predicate exists here for a Unicode-style fake to target).
- Reconstructed the round-7 NFKC fake (above) and additionally reconstructed one further pre-existing documented fake independent of prior sessions' reconstructions — the hostname-prefix fake (`presented_hostname.startswith(expected_hostname)`) — and got the documented count (`36 passed, 1 failed`, failing exactly `test_anti_fake_hostname_prefix_match_is_rejected`).
- Grepped `content/modules/2/2.2/spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, `labs/2.2/2.2-request-path/README.md`, and `content/assessment/keys/2.2.md` for stale test-count phrasing from any prior round (`"35 of 35"`, `"34 of 34"`, `"19 of 19"`, `"18 of 18"`, `"of 3[0-6] pass"`, `"of 3[0-6] fail"`, `"eleven fakes"`, `"ten fakes"`). No hits outside `content/modules/2/2.2/module.yaml`'s own changelog, which correctly narrates past counts as history (its final entry describes the round-7 fix producing "37 tests", matching the live count, not a stale one).
- Metadata honesty and shared `pins.yaml`, re-confirmed: `content/modules/2/2.2/module.yaml` still reads `status: draft` (L7), `reviewer: pending independent quality and lab-safety review` (L160), `lastReviewedAt: null`, `nextReviewAt: null` (L161-162) — no authoring or fix pass has touched these across any of the eight rounds. `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry on this branch still reads `reviewedAt: "2026-09-22"` with the corrected `url: "https://owasp.org/projects/asvs"`; `origin/deepen/2.3`'s parallel entry (fetched fresh via `git fetch origin deepen/2.3`, not from cache) reads `reviewedAt: "2026-09-23"` with the identical corrected URL and its own independent dead-link narrative appended after 2.2's — the same benign cross-branch note-text conflict on record for all eight rounds now, still just that: no factual disagreement about the URL, only duplicated "I fixed this" narration and a differing `reviewedAt` on a shared field that will conflict on merge. Not touched, per this task's instruction.
- Mechanical gates, run myself: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.

**Verdict: neither gate is granted, for the eighth time running.** Round 7's finding is genuinely closed — I verified this by independent reconstruction, not by trusting the fix commit's own account, and it holds up, as do all counts, citations, the residual-scope note's honesty, metadata honesty, the cross-branch `pins.yaml` note, and mechanical gates checked above. But an eighth construction, targeted specifically at the Unicode-adjacent angles this task named as unexplored (having ruled out NFKD, cross-script confusables, and `casefold()` as either not-a-gap or already-honestly-disclaimed), found a real, demonstrated, exploitable gap the current 37-test suite does not catch: stripping Unicode category **Cf** (zero-width and format characters) before comparing, a defensive habit motivated by the identical class of security guidance (untrusted-string hygiene before comparison) that justified the NFKC fake round 7 just closed, and one that NFKC/NFKD normalization does not itself catch. Per this repo's governing rule, a real blocker withholds every grant regardless of how much surrounding work is independently confirmed correct, and regardless of how many prior rounds — seven, now — already closed other findings in these same two checks.

**To close:** add a Cf-category/zero-width case to both the version sweep and a dedicated hostname anti-fake test — for example `tls_version="1.2​"` (a trailing U+200B ZERO WIDTH SPACE) to `_VERSION_NEAR_MISSES`, and a hostname anti-fake test with `presented_hostname` equal to the expected hostname but with a U+200B spliced into the middle (e.g. `"a.sec​urecollab.internal"` against `"a.securecollab.internal"`) — both asserting `502`. This isolates "the string is exactly the canonical literal, code point for code point" from "the string, after removing zero-width/format characters, names the canonical literal," an eighth and distinct near-miss shape from the twelve already closed (five version-check fakes, three hostname-check fakes, and the NFKC fake covering both). Given that round 7 already generalized the version check's docstring to note "Unicode normalization included" as one instance of a broader "plausible normalization or partial match" class, and this round found a second, non-overlapping instance of exactly that broader class within one round of the first being closed, this may be worth escalating (as round 6 already flagged for the ASCII-only near-miss space) to a documented decision about whether hand-picked-per-discovery near-miss tests can ever be treated as convergent for an unbounded-string equality check, versus adopting a structural fix instead of another test — for example, having `hop_is_trustworthy` reject any `tls_version` or hostname containing non-ASCII or non-printable characters outright before the membership/equality check even runs (a normalization-proof allow-list on the *input's character set*, not on the comparison), which would close this entire class of gap (NFKC, NFKD, Cf-stripping, and any future normalization-adjacent variant not yet constructed) in one structural change rather than one test per discovered variant. I am naming this as a recommendation for the calling session or a human to weigh, not implementing it myself, per this task's read-only scope.

Also worth noting for whoever picks this up: while the cosmetic U+FF0E-vs-ASCII-period comment inaccuracy noted above under the sweep sanity-check does not by itself justify withholding a grant, it would cost nothing to fix in the same pass as the structural fix above, since both touch the same comment block.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. An eighth re-review that found a live blocker may not write this line, and finding the seventh round's own issue closed does not change that.

## Addendum, 2026-09-24 — ninth-round re-review at `9e4dd8a`: round-8 finding genuinely closed and the Unicode scope cap respected, but a new blocker found in C1 (cache key), a claim eight rounds never adversarially touched

**Independence statement.** I am a ninth, separate re-review pass, on commit `9e4dd8a` ("2.2: close eighth review's Cf-stripping gap, and record a scope decision capping further Unicode-normalization hunting"), which is `HEAD` of `origin/deepen/2.2`. My worktree's own branch was unrelated history, so I reset my dedicated worktree branch to `origin/deepen/2.2` at this exact commit (`git checkout -B deepen/2.2 origin/deepen/2.2`) and confirmed the working tree was clean before touching anything. I read all eight addenda above in full, and `content/modules/2/2.2/spec.md`'s "Known residuals" section and `module.yaml`'s full changelog, before touching any file. Per this task's explicit scope cap, I did not spend adversarial effort constructing a ninth Unicode-normalization-adjacent variant of `hop_is_trustworthy`'s hostname/version checks; I verified round 8's own finding is closed (required by this task) and then gave genuinely fresh effort to C1 and C2, which is where it turned up something. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum` (`ebdf983ea6753464cffb6dee0a669513` before and after every edit) and by `diff` against a pre-edit copy showing no difference, and by `git status --porcelain` returning empty — before I moved to the next check, every time. Scratch backup copies lived only under `/tmp`, outside the repository tree.

**Round 8's finding — CLOSED, verified by reconstructing the exact combined fake from scratch.** I wrote a `_strip_invisible` helper (`"".join(ch for ch in s if unicodedata.category(ch) != "Cf")`) and applied it to both `hop_is_trustworthy`'s version-membership check and its hostname equality check in `fixed/app.py`, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
2 failed, 37 passed
FAILED tests/test_tls_hop.py::test_anti_fake_version_near_miss_sweep_is_rejected[1.2​]
FAILED tests/test_tls_hop.py::test_anti_fake_hostname_zero_width_character_is_rejected
```

Fails exactly and only the two tests added to close this gap — nothing else regresses. Restored; `diff` against the pre-edit copy showed no difference, `git status --porcelain` was empty, `md5sum` matched before and after (`ebdf983ea6753464cffb6dee0a669513`).

**Full pass/fail counts, re-run myself, matching every file's own claim exactly:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
33 failed, 6 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
39 passed
```

This matches `labs/2.2/2.2-request-path/README.md` L28-29 ("6 of 39 pass" / "39 of 39 pass"), `content/modules/2/2.2/lessons/05-verify.md`'s Mermaid diagram ("6 of 39 pass" / "39 of 39 pass"), `content/modules/2/2.2/assessment/items.md` L104 and `assessment/rubric.md` L13 ("33 of 39 failing" / "39 of 39 passing"), and `content/assessment/keys/2.2.md` L11-12 ("must fail 33 of 39" / "must pass all 39"). `bash scripts/run_labs.sh 2.2` independently reports `vulnerable failed, fixed passed, RESULT OK`.

**Scope cap respected.** `spec.md`'s "Known residuals" bullet (L70) clearly names the two closed Unicode-normalization-adjacent classes (NFKC/NFKD canonical normalization, category-Cf stripping) and explicitly caps further hand-picked variants (a third normalization form, a different invisible-character category, homoglyphs, IDNA/Punycode) as an accepted non-goal, in language a learner or a future reviewer can actually parse — it reads clearly, not just to another adversarial reviewer. I did not construct a ninth such variant, per this task's instruction, and I did not find one incidentally either.

**C1 and C2 — genuinely fresh adversarial effort, three angles tried, one of which is a real, demonstrated, exploitable gap.**

I read `_resolve_company`, `get_note`, `put_note`, and the `_CACHE`/`_ORIGIN_STORE` keying in `fixed/app.py` end to end before constructing anything, specifically looking past `hop_is_trustworthy` (the function every one of the eight prior rounds targeted) to the two claims none of them had adversarially touched.

1. **TOCTOU / ordering in the cache get-then-set, checked structurally rather than assumed away.** `get_note`'s cache read and cache write are two separate dict operations (`_CACHE.get((note_id, company))`, later `_CACHE[(note_id, company)] = text`) with no lock between them, which is the textbook shape of a TOCTOU bug in a real concurrent server. I confirmed this is not exploitable *in this fixture* for a structural reason, not merely by assumption: every endpoint is a synchronous `def` (not `async def`), `TestClient` issues one HTTP call at a time and blocks for the response, and both `_CACHE` and `_ORIGIN_STORE` are reloaded fresh per test via `conftest.py`'s `_load()` (a fresh module exec, not a shared import) — there is no mechanism in this test harness that can interleave two requests' get/set pairs, and even under a hypothetical interleaving, two different companies' operations always target different dict keys (`(note_id, companyA)` vs. `(note_id, companyB)`), so there is no shared mutable slot for a race to corrupt. No test can be constructed to exercise a real race here; this is not a gap, and I did not write a test for it.

2. **`note_id` smuggling a delimiter to defeat a plausible alternate string-concatenation key, constructed and run for real.** I replaced the tuple key `(note_id, company)` with a concatenated string key `note_id + ":" + company` (a very plausible alternate "fix" — many real caches key on a colon-joined string rather than a tuple) and ran the full suite:

   ```
   $ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
   39 passed
   ```

   All 39 pass. I then tried to construct an actual collision rather than stop at "it passed": since `company` is always one of exactly four fixed, equal-length, mutually non-suffix literals (`companyA`, `companyB`, `companyZ`, `companyQ` — each ends in a distinct character), the last 9 characters of `note_id + ":" + company` always uniquely identify which company produced the string and where the delimiter fell, regardless of what an attacker puts in `note_id` (which cannot contain `/` as a FastAPI path segment, but can contain `:`). I could not construct a `(note_id_1, companyA)` vs. `(note_id_2, companyB)` pair that concatenates to the same string, and confirmed this by direct construction (`note_id="n1:companyB"` with the caller's own `companyA` credential does not collide with any real `companyB` entry). This alternate keying scheme is extensionally identical to the tuple key for this fixture's closed four-company domain — a real finding of "no gap," not an oversight, and I am recording the construction so a future round does not need to re-derive it.

3. **`API_KEYS` lookup, two plausible-but-wrong alternates, both constructed and both already caught.** `API_KEYS.get(api_key, x_company)` in place of the real `if api_key not in API_KEYS: return None; return API_KEYS[api_key]` — this returns the caller-supplied `x_company` value instead of `None` for an unregistered key — fails `test_unregistered_api_key_with_a_company_header_is_denied` immediately (401 expected, 404 returned), confirming the existing malformed-credential test already forecloses this exact class of `.get()`-with-a-wrong-default bug. Case-insensitive key lookup (`api_key.lower()` against a lower-cased `API_KEYS`) introduces no company-crossing gap, because none of the four key literals collide under case-folding and every one still resolves to its own, correct company — this loosens credential *matching*, not the C1/C2 authorization property this module claims, so I did not pursue it as a fake.

**A real, demonstrated, exploitable gap in C1 — the cache is never actually verified to serve a hit at all, so a "fix" that silently disables caching entirely passes the whole 39-test suite.** I checked every assertion in `tests/test_cache_key.py` for whether any of them inspects the response's own `"source"` field (`"cache"` vs. `"origin"`, the exact field `fixed/app.py`'s own `get_note` returns and that `lessons/03-break.md`'s sequence diagrams narrate the vulnerability *through*, at L27: `O->>B: 200 {body: "alice-n1", source: cache}`). None do — I grepped the whole `tests/` directory for the string `source` and found zero matches outside `test_tls_hop.py`'s unrelated "trojan source" comment text. Every C1/C2 assertion in the suite checks only the response body and status code, never whether the value actually came from a cache hit.

I constructed this directly: removed the cache-population line entirely from `fixed/app.py`'s `get_note` (`_CACHE[(note_id, company)] = text`, replaced with a comment and nothing else — the cache dict is now permanently empty; every request, cached or not, falls through to `_ORIGIN_STORE`), and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
39 passed
```

**All thirty-nine tests pass, including both of C1's dedicated tests and its anti-fake pair.** The reason is structural, not an accident of which values the tests happen to use: `_ORIGIN_STORE` is already correctly scoped by `(note_id, company)` on its own (this is the "boundary case" `test_cross_company_lookup_is_scoped_even_before_any_cache_entry_exists` explicitly names as a property the origin store holds independently of the cache), so every one of C1's assertions — "company B does not receive company A's cached body," the anti-fake pair using fresh note/company values — is *also* satisfied whenever the cache never fires at all and every read goes straight to the correctly-scoped origin store. A test suite built entirely from "does the wrong company ever receive a body" assertions cannot distinguish "the cache is keyed correctly" from "the cache never serves anything, ever" — both produce identical externally observable behavior for every case this suite tests, because the origin store's own scoping is a hidden safety net the tests never isolate the cache mechanism from.

I confirmed this is not a one-line accident specific to my exact edit by trying a second, independently-plausible variant: a write-side key/read-side key type mismatch (`_CACHE[note_id] = text` on write — a plain string — against the unchanged tuple-keyed read `_CACHE.get((note_id, company))`), which has the identical effect (the cache is written but can never be read back, since no read ever looks up a plain-string key) and produces the same result:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
39 passed
```

Again all 39 pass. I restored `fixed/app.py` after each of these two constructions and confirmed byte-for-byte: `diff` against the pre-edit copy showed no difference, `md5sum` matched (`ebdf983ea6753464cffb6dee0a669513`), and `git status --porcelain` was empty before I moved to the next check.

**Severity and scope.** This is not a Unicode-adjacent variant of `hop_is_trustworthy` — it is a structurally distinct gap in C1, the module's flagship, title-bearing claim ("shared cache returns tenant A's body to tenant B"), in a part of the code eight consecutive rounds never adversarially touched because all eight targeted the version/hostname checks. `04-build.md` (L22, L30) teaches the correct fix explicitly as "the key itself grows a field, rather than gaining a check that runs before the lookup" — i.e., caching is meant to keep working, correctly scoped, not to be silently disabled — so a "fix" that disables caching entirely is not the mechanism this module teaches, yet the test suite cannot tell the two apart. Per this repo's governing rule, a real blocker withholds every grant regardless of how many prior rounds already closed other findings in a different function, and regardless of how strong the surrounding lesson prose and citation work are (both of which I re-confirm below are still genuinely strong).

**To close:** add an assertion on the response's own `"source"` field to isolate "the cache actually served this response" from "the response happened to be correct" — at minimum, assert `second.json()["source"] == "cache"` in `test_same_company_reads_its_own_note_after_caching` (the normal case already primes and re-reads the same note; it currently checks only the body), and add a dedicated test that primes the cache for one company, asserts the priming read is `"source": "origin"` and the following read is `"source": "cache"`, so a cache-disabling "fix" (or a write/read key-type mismatch) fails on the missing cache hit rather than passing by accident because the origin store's own scoping happens to cover every case the suite currently checks. This is a different repair shape from any of the thirteen version/hostname fakes closed so far — it targets whether the *mechanism* this module is named for participates at all, not whether a string-comparison predicate is exact.

**Other checks performed, all confirmed clean, no regression from round 8:**

- Coverage-contract and teaching-claims citations, re-derived independently from `items.md`'s own `Claim assessed` tags: C1 → #1, #4, #6, #7, #8; C2 → #2, #4, #7; C3 → #3, #7, #8; C4 → #5, #7; C5 → #6, #7, #8 — matches `spec.md`'s teaching-claims table (L45-49) exactly, no addition or omission, no regression from any prior round's finding that this defect class is clean here.
- Metadata honesty: `content/modules/2/2.2/module.yaml` still reads `status: draft` (L7), `reviewer: pending independent quality and lab-safety review` (L160), `lastReviewedAt: null`, `nextReviewAt: null` (L161-162) — no authoring or fix pass has touched these across any of the nine rounds.
- Shared `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry, checked against `origin/deepen/2.3` fetched fresh: this branch still reads `reviewedAt: "2026-09-22"`, `origin/deepen/2.3` still reads `reviewedAt: "2026-09-23"`, both with the identical corrected `url: "https://owasp.org/projects/asvs"` and each branch's own independent dead-link narrative — the same benign cross-branch conflict on record for all nine rounds now: no factual disagreement, only a `reviewedAt` field and duplicated "I fixed this" narration that will need resolving at merge time. Not touched, per this task's instruction.
- Mechanical gates, run myself: `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.
- Stale-count grep across `spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, the lab `README.md`, and `content/assessment/keys/2.2.md` for any live (non-changelog) reference to a prior test count (`"of 37"`, `"of 35"`, `"of 34"`, `"of 19"` through `"of 14"`, and the corresponding number words): every current-tense reference reads "39" consistently; the only non-"39" occurrences are inside `keys/2.2.md`'s own historical bullets narrating what earlier rounds found at the time, correctly framed in the past tense, and `module.yaml`'s changelog, which is explicitly historical record.
- Broader content sanity pass (spec.md, all eight lessons, items.md, rubric.md, keys/2.2.md), read for quality issues independent of the lab's test suite: the seven-step loop is genuinely covered end to end (property in `01`, model/diagram in `02`, break traces in `03`, build repair in `04`, verify counts and fake-enumeration in `05`, detection/purge design in `06`, transfer task in `07`, seeded code review in `08`); all eight lessons exceed the 900-word bar with real derivation rather than padding, each carries an authorized-scope statement and a titled cross-reference; the eight assessment items are well-formed, each names a `Claim assessed`/`Outcome` pair, requires actual tracing or diagnosis rather than recall (item #4 is itself a plausible-but-wrong cache-key fake presented to the learner as a diagnosis exercise, which is good pedagogy and, worth noting, is a *different* fake — trusting `x_company` for the cache key while resolving `company` correctly for the origin store — from the cache-disabling gap found above); the "Known residuals" section (L64-70) reads clearly to a learner, not only to a reviewer, naming what is and is not covered in plain terms. I did not find a content-quality defect independent of the lab; the one defect found this round is in the lab's own test coverage, not in the prose.

**Verdict: neither gate is granted, for the ninth time running — but for the first time in a claim other than `hop_is_trustworthy`'s version/hostname checks.** Round 8's finding is genuinely closed, the scope cap on further Unicode-adjacent hunting is real and was respected, and the module's lesson prose, coverage-contract citations, standards work, and metadata honesty are all independently re-confirmed strong. But a fresh adversarial pass aimed specifically at C1 and C2 — as this task asked, given eight rounds' total absence of scrutiny there — found a real, demonstrated gap: the test suite for C1 never verifies that a cache hit actually occurs, so a fix that silently and completely disables the cache passes every test written for the property this module is named after. This is not a ninth Unicode-normalization variant and does not fall under the scope cap; it is squarely the kind of "genuinely new angle" on C1 this task asked for.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A ninth re-review that found a live blocker may not write this line, even though it is the first blocker found outside `hop_is_trustworthy` and the eighth round's own finding is confirmed closed.

## Addendum, 2026-09-24 — tenth-round re-review at `1eb79ed`: round-9 finding genuinely closed, but a new, structurally distinct C2 gap found by the same kind of scrutiny that found it

**Independence statement.** I am a tenth, separate re-review pass, on commit `1eb79ed` ("2.2: close ninth review's C1 cache-mechanism gap, the first blocker outside hop_is_trustworthy"), the current tip of `origin/deepen/2.2`. I read all nine prior rounds of this file in full before touching any file. Every file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum` against the known-good digest and by `git status --porcelain` returning empty — before I moved to the next check, every time.

**Round 9's finding — CLOSED, verified by reconstructing the exact fake before reading anything else.** I removed only the cache-population line (`_CACHE[(note_id, company)] = text`) from `fixed/app.py`'s `get_note`, leaving `_CACHE` permanently empty, and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
2 failed, 38 passed
FAILED tests/test_cache_key.py::test_same_company_reads_its_own_note_after_caching
FAILED tests/test_cache_key.py::test_anti_fake_cache_actually_serves_the_second_read
```

Exactly and only the strengthened normal-case test and the new dedicated anti-fake test fail, precisely as `module.yaml`'s changelog and this file's round-9 addendum both claim. Restored (`cp` from a pre-edit copy); `md5sum` on the restored file reads `ebdf983ea6753464cffb6dee0a669513`, matching every prior round's own recorded digest; `git status --porcelain` empty.

**Full pass/fail counts, run myself, not trusted from any doc:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
33 failed, 7 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
40 passed
```

Matches every current-tense claim in `README.md`, `items.md`, `rubric.md`, `keys/2.2.md`, and `lessons/05-verify.md` (33 of 40 failing, 40 of 40 passing).

**C2 — asked, per this task, to apply the exact scrutiny that just found the C1 gap: does any test verify identity resolution actually goes through the credential path, or only that the header is ignored and a handful of literal keys resolve correctly? A real, demonstrated, exploitable gap exists, and it is worse in kind than the original vulnerability.**

`fixed/app.py`'s real `_resolve_company` is:

```python
def _resolve_company(api_key, x_company):
    if api_key not in API_KEYS:
        return None
    return API_KEYS[api_key]
```

Every test in `test_cache_key.py` (I read all nine, listed at the top of this file's own "Files inspected" and reconfirmed by re-reading them fresh) uses only the four registered literals `key-A`/`key-B`/`key-Z`/`key-Q` as valid credentials, plus the one literal `"key-does-not-exist"` as the sole invalid one (confirmed by `grep -n "key-" tests/test_cache_key.py`: no other credential string appears anywhere in the file). No test ever sends a credential that is *unregistered* but shares the *shape* of a registered one.

I replaced `_resolve_company` with a fake that never consults `API_KEYS` at all — it derives a company purely from the credential string's shape:

```python
def _resolve_company(api_key, x_company):
    if not api_key or not api_key.startswith("key-") or len(api_key) != 5:
        return None
    return "company" + api_key[-1].upper()
```

For every credential any test in the suite sends, this produces the identical result to the real lookup (`key-A` → `companyA`, `key-B` → `companyB`, `key-Z` → `companyZ`, `key-Q` → `companyQ`, and `key-does-not-exist` → `None`, since it fails the `len == 5` check). Ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
40 passed
```

**All forty tests pass, including both C2 forbidden-outcome/anti-fake tests, the malformed-credential test, and every C1 test (including round 9's own new `"source"`-field assertions).** I then confirmed this is a real, exploitable, demonstrated divergence from the real fix, not merely an untested-but-harmless corner — a credential that was *never issued by the origin at all* is accepted and minted a company identity:

```
$ python3 -c "
import sys; sys.path.insert(0, 'fixed')
from app import create_app, reset
from fastapi.testclient import TestClient
reset()
client = TestClient(create_app())
r1 = client.put('/notes/secret', json={'body': 'companyD confidential data'}, headers={'Authorization': 'Bearer key-D'})
print('PUT with never-issued key-D:', r1.status_code, r1.json())
r2 = client.get('/notes/secret', headers={'Authorization': 'Bearer key-D'})
print('GET with never-issued key-D:', r2.status_code, r2.json())
"
PUT with never-issued key-D: 200 {'stored': True}
GET with never-issued key-D: 200 {'body': 'companyD confidential data', 'source': 'origin'}
```

`"key-D"` is not a key in `API_KEYS` — it was never issued, never registered, never handed to any caller by the origin. Against the real fix, `_resolve_company("key-D", None)` returns `None` and both calls 401. Against this fake, any string of the shape `"key-" + <one character>` is treated as a fully valid, authenticated caller and minted a brand-new company identity on the spot, with full read/write access to that company's notes. This is not merely "the header channel reopened" (C2's originally-named forbidden outcome) — it is a strictly worse failure: an attacker does not even need to hold anyone's real credential, only to guess a five-character string of the right shape. `module.yaml`'s own repair description (`fixed/app.py` L1-10) states the intended property as "no code path in this function can return a value that did not come from `API_KEYS[api_key]`" — this fake violates that description in its most literal sense, in code that "passes" the entire suite meant to certify it.

I restored the file immediately: `cp` from the pre-edit copy, `md5sum` confirms `ebdf983ea6753464cffb6dee0a669513`, `git status --porcelain` empty.

**Severity and scope.** This is not an eleventh Unicode-adjacent variant of `hop_is_trustworthy`, and it is not a repeat of round 9's C1 finding — it is a third, independent function (`_resolve_company`) and a different failure shape (authentication-by-shape-guessing rather than a disabled mechanism or a string-comparison near-miss) from anything found in rounds 1 through 9. Per this task's own framing, round 9's insight was "the suite only ever checked the response body/status, never *how* the correct answer was produced" — the analogous question for C2 is "does any test verify company resolution actually authenticates against the origin's own issued-credential table, or only that a header is ignored and a fixed set of literals happen to map correctly?" The answer is the latter, and every C1/C2 forbidden-outcome and anti-fake test — checking only a final status code or body, exactly as this task's background predicted might be the shape of a remaining gap — is blind to it.

**To close:** add a test sending a credential string that is *shaped like* a valid key but was never registered (for example `"key-D"`, `"key-M"`, or any single-character suffix outside `{A, B, Z, Q}`) with no `X-Company` header, and assert `401` — isolating "a credential the origin actually issued and can verify" from "a string that merely looks like one." This is a different repair shape from any of the fourteen fakes closed so far: it targets whether company resolution authenticates against a real credential store at all, not whether a header is read or a comparison is exact.

**Other checks performed, all confirmed clean:**

- **C4 and C5 — read `lessons/02-model.md` and `lessons/06-operate.md` fresh, checking specifically for a shortcut behind the "not directly code-testable" claim.** Both do genuine, checkable design work rather than hand-waving around the absence of a lab assertion. `02-model.md` builds a five-row hop table with an explicit "what is authenticated" / "what is not" column for every hop including DNS (honestly marked out of scope rather than deleted), a Mermaid flow with per-arrow authority annotations, three named rejected diagram shapes, and a worked re-derivation of one annotation directly from the fixture's own `Header(...)` call — this is a real, falsifiable design artifact a reviewer could check a diagram against, not an assertion that TLS-is-fine. `06-operate.md` builds two named signals with an explicit schema (what each field must and must not carry, with a stated reason: "the evidence that proves a leak happened is, definitionally, the same data whose exposure the leak consists of"), a state diagram distinguishing the two regression shapes, a worked example of two actual log lines read for what they omit, and an explicit argument for why a purge drill and a certificate-failure drill are not interchangeable. Neither page claims more than it does: both explicitly say what they are not doing (no real DNSSEC chain, no deployed CDN, no real certificate rotation) in a closing section, and `spec.md`'s coverage-contract row states the specific, checkable reason C4/C5 cannot be lab-tested (no second real network hop; no CDN/edge process to drift) rather than a vague "out of scope." I found no honesty problem here — the claim that these are design/model claims rather than lab-tested ones is accurate, and the design work behind it is real.
- **Full fresh read of all eight `lessons/*.md`, the lab `README.md`, `spec.md`, `items.md`, `rubric.md`, and `content/assessment/keys/2.2.md`.** All eight lessons still genuinely derive their claims (rejected alternatives, worked examples re-deriving a line of code, "use it somewhere new" transfer prompts, an honest "what this page is not doing" close) rather than reading as fragments or template fills; none of the seven-step loop's steps are skipped or thin.
- **Readability judgment, asked for explicitly and answered either way: the accumulated fake-enumeration prose in `README.md` and the parallel "Anti-fake tests are not decoration" section of `lessons/05-verify.md` has become unreadable review-log sediment, not learner-serving content, and I would flag this even though it does not block a grant on its own.** Both sections have grown, across ten rounds, into dense forensic narration — "The eleventh fake, found in a sixth independent review round...", "The twelfth fake, found in a seventh independent review round...", cross-referencing which sweep case a given fake fails and why, in prose clearly written to prove exhaustiveness to the *next reviewer*, not to teach the *next learner* what an anti-fake test is for. A learner reading `05-verify.md` for the first time does not need, and is not well served by, a blow-by-blow reconstruction of which review round found which of fourteen fakes and how each interacts with a nineteen-case parametrized sweep; the pedagogical point ("an anti-fake test isolates one specific plausible-wrong mechanism from the real one, and a suite that only checks outcomes can miss a mechanism entirely") is fully made by the *first two or three* fakes in each section, and everything after that is audit trail, not instruction. This is exactly the "scar tissue" this task's framing named. I am not blocking on it — it is a real defect distinct from correctness, worth trimming before this module ships, most naturally by moving the full fake-by-fake forensic account to `content/assessment/keys/2.2.md` (which is examiner-only and already carries an equivalent history) and leaving `README.md`/`05-verify.md` with a short, representative sample plus a pointer, rather than growing the learner-facing count by one paragraph every round indefinitely.
- **Coverage-contract citations, cross-checked against `items.md`'s own tags.** The teaching-claims table (`spec.md` L45-49) is exact: C1→#1,#4,#6,#7,#8; C2→#2,#4,#7; C3→#3,#7,#8; C4→#5,#7; C5→#6,#7,#8, matching `items.md`'s `Claim assessed` tags cell for cell, confirmed independently. The separate coverage-contract table (L57-62) lists broader `Claim` columns per outcome (e.g. row 2: "C3, C4") than the single assessment item cited for that row's specific outcome text actually tags (item #3: C3 only) — I traced this and it is not a citation error: the `Claim` column describes which claims the *outcome itself* touches (as `module.yaml` defines the outcome), while the cited item matches by *outcome text*, the same methodology the very first review round in this file used and which I am not overturning; C4's own item citation (`items.md` #5, #7) lives correctly under the teaching-claims table and under the coverage-contract's row 3, not row 2. Worth noting for a future reviewer, not a defect.
- **Metadata honesty.** `content/modules/2/2.2/module.yaml` still reads `status: draft`, `reviewer: pending independent quality and lab-safety review`, `lastReviewedAt: null`, `nextReviewAt: null` — unchanged by the round-9 fix pass, as required.
- **Mechanical gates, run myself:** `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.
- **Stale-count grep** across `spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, the lab `README.md`, and `content/assessment/keys/2.2.md` for any live reference to a prior count (`"of 39"` down through `"of 14"`): every occurrence found outside `module.yaml`'s changelog is inside `keys/2.2.md`'s own historical bullets, explicitly narrating what an earlier round found at the time it found it (correctly past-tensed) — no live claim anywhere still cites a stale count; every current-tense claim reads "40".
- **Shared `content/standards/pins.yaml`'s known cross-branch conflict with `origin/deepen/2.3`, re-fetched fresh.** Still just that: this branch's `owasp-asvs-5.0.0` entry reads `reviewedAt: "2026-09-22"`; `origin/deepen/2.3`'s reads `reviewedAt: "2026-09-23"` with 2.3's own dead-URL narrative appended to the shared `notes` field. Both agree on the corrected `url` (`https://owasp.org/projects/asvs`); no factual disagreement, only a field value and duplicated narration that will need reconciling at merge time, exactly as round 9 (and every round before it) recorded. Not touched.

**Verdict: neither gate is granted, for the tenth time running.** Round 9's finding is genuinely closed, the scope cap on further Unicode-adjacent hunting was respected (I constructed no such variant), C4 and C5's "not directly code-testable" claims hold up under a fresh read for rigor rather than presence, and the module's lesson prose, standards work, coverage-contract citations, and metadata honesty are all independently re-confirmed strong — including a readability judgment on the accumulated fake-enumeration prose, answered as: it has become review-log sediment that would need trimming before this module ships, though not a blocker on its own. But a fresh adversarial pass on C2, applying the exact question round 9's own finding raised ("does the suite check the mechanism or only the outcome?") to a different claim, found a real, demonstrated, exploitable gap: no test in this suite ever sends a credential that is shaped like a valid key but was never actually issued by the origin, so a `_resolve_company` that authenticates by pattern-matching a credential's shape — never consulting `API_KEYS` at all — passes the entire forty-test suite, including every C1 and C2 forbidden-outcome and anti-fake test. Per this repository's governing rule, this withholds every grant regardless of how strong the surrounding, independently-reconfirmed work is.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated, exploitable gap was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A tenth re-review that found a live blocker may not write this line.

## Addendum, 2026-09-24 — eleventh-round re-review at `c27cb65`: round-10 finding genuinely closed, the trim is accurate, no new adversarial code fake found, but a stale test count survives in learner-facing prose

**Independence statement.** I am an eleventh, separate re-review pass, on commit `c27cb65` ("2.2: close tenth review's C2 credential-shape gap, and trim the fake-enumeration prose per its readability finding"), the current tip of `origin/deepen/2.2`. I read all ten prior rounds of this file in full before touching any file. The only file I edited to construct a fake was restored byte-for-byte afterward — confirmed by `md5sum` against every prior round's own recorded digest (`ebdf983ea6753464cffb6dee0a669513`) and by `git status --porcelain` returning empty — before I moved to any other check. The only files I have written in this repository this pass are this artifact and scratch files under `/tmp` outside the repo tree.

**Round 10's finding — CLOSED, verified by reconstructing the exact fake before reading anything else.** I replaced `fixed/app.py`'s `_resolve_company` with the documented shape-matching fake (`if not api_key or not api_key.startswith("key-") or len(api_key) != 5: return None; return "company" + api_key[-1].upper()`) and ran the full suite:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
1 failed, 40 passed
FAILED tests/test_cache_key.py::test_anti_fake_credential_shaped_like_a_valid_key_is_still_denied
```

Exactly and only the new dedicated test fails, precisely as `module.yaml`'s changelog and round 10's own addendum claim. Restored (`cp` from a pre-edit copy); `md5sum` on the restored file reads `ebdf983ea6753464cffb6dee0a669513`, matching every prior round's own recorded digest; `git status --porcelain` empty.

**Full pass/fail counts, run myself, not trusted from any doc:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
33 failed, 8 passed
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
41 passed
```

Matches every current-tense claim in `README.md`, `items.md`, `rubric.md`, `keys/2.2.md`, and (with one exception, below) `lessons/05-verify.md` (33 of 41 failing, 41 of 41 passing).

**The readability trim — asked to evaluate fresh, as a first-time learner, whether it actually works.** I read `labs/2.2/2.2-request-path/README.md`'s "Anti-fake tests are not decoration" paragraph and `lessons/05-verify.md`'s "Anti-fake tests are not decoration" section with no prior context, then checked both against `content/assessment/keys/2.2.md`'s full forensic account. **The trim succeeds on its own terms.** Both sections now state a clear headline count ("fifteen constructed fakes"), work through two or three representative shapes concretely enough that a first-time reader can see the general pattern (an approximate string match; a mechanism disabled behind a hidden safety net elsewhere; an identity check that verifies shape instead of provenance), name the general lesson in one sentence each ("a check that stops one signal short of the real one... can satisfy an entire suite built only from outcome assertions, until a test is written that asks not just 'was the answer right' but 'did it get there the way this module teaches'"), and point to `keys/2.2.md` for the complete round-by-round account rather than reproducing it. I checked the specific counts and mechanism descriptions the trim keeps against `keys/2.2.md`'s own bullets and found no inaccuracy: "fifteen" matches `keys/2.2.md`'s own final tally exactly (its last bullet lists fifteen named fakes against the current 41-test suite, distinct from the "seventeen" figure used elsewhere in this file's history, which additionally counts two fakes constructed only to demonstrate the sweep's generality and never found by any review round — `README.md` and `05-verify.md` correctly use "fifteen," the reviewer-found count, not "seventeen"). No factual content was lost that a learner needs; the trim is a genuine, accurate improvement, not merely a shorter but wrong version of the same claim.

**But the fresh read this exercise required surfaced a gap in a different sentence of the same lesson page, one none of the ten prior rounds' stale-count greps caught.** `lessons/05-verify.md`'s "What these checks do not prove" section (line 70, in the web-cache-deception residual bullet) reads: *"Treating this module's Level 2 cache-key property as if it already covered Level 3 web cache deception would overstate what **fourteen** passing tests actually checked."* The suite has 41 tests, not 14, and has not had 14 since the very first version of this lab (`git log --follow` on this file confirms the line has read "fourteen passing tests" verbatim since commit `c26f92b`, the deepen pass that originally built the 14-test suite, and was never touched by any of the ten subsequent count-correction commits that followed as the suite grew to 15, 16, 17, 18, 19, 34, 35, 37, 39, 40, and now 41 tests). Every prior round's stale-count grep (see round 9's and round 10's own "Stale-count grep" bullets, and round 2's original discovery of five missed numeric references) searched for numeral patterns like `"of 14"`, `"6 of 14"`, `"14 of 14"` — patterns this line does not match, because it spells the number out as a word ("fourteen") with no "of" before or after it. That is exactly why ten rounds of otherwise-thorough numeral grepping missed it. I confirmed no other spelled-out stale number survives: `grep -n "fourteen\|thirteen\|\\b14\\b"` across `spec.md` and all eight `lessons/*.md` returns only this one live-tense hit outside `module.yaml`'s own historical changelog (`items.md`, `rubric.md`, and the README already correctly say "forty-one"/"41").

**This is exactly the "lessons have not kept pace with the lab" gap this task asked me to look for, in miniature.** The sentence's own point is about not overstating verification coverage for a residual — and in making that point, it dramatically *understates* how much verification the current suite actually performs (14 vs. 41, roughly a third of the true count), inside a lesson section whose entire subject is teaching a learner to read verification claims precisely rather than by vibes. A learner who reads this section and separately reads the README's or `items.md`'s correct "41" would have no way to know which number describes their own lab directory; a learner who reads only this section would come away believing the suite is far smaller and thinner than it is.

**Severity.** This is not a code defect and not an adversarial fake against `fixed/app.py` — it is a factual inaccuracy in learner-facing prose, of the same class round 2's miscounted cache-only-fix failure count and round 2's five missed stale references were, and this repository's own governing rule treats that class of finding as real and blocking, independent of whatever code-level gap does or does not accompany it in a given round.

**To close:** change `lessons/05-verify.md` line 70's "fourteen passing tests" to "forty-one passing tests" (or "the current forty-one-test suite," matching this page's own phrasing elsewhere on the same page, e.g. line 9's "every test in `labs/2.2/2.2-request-path/tests` passes"). No other file needs a change; I found no second instance of this spelled-out-number pattern anywhere else in this module's tree.

**Fresh adversarial effort on the least-scrutinized area (per this task's track 2), no new code gap found.** Per this task's framing of what had and had not been tried across ten rounds, I targeted areas distinct from every documented fake: (1) re-read all nine tests in `test_cache_key.py` end to end and checked each one's assertion against its own docstring's claim — all nine test exactly what they say they test, with no gap between the claimed isolation and the actual assertion (confirmed by re-deriving, by hand, which of the two structural bugs and which anti-fake signal each case exercises, and finding it matches the file's own comments in every case); (2) checked `conftest.py`'s `client` fixture for cross-test state leakage — it re-imports and re-execs `app.py` as a fresh module object per test via `importlib.util.module_from_spec`/`exec_module`, so `_ORIGIN_STORE` and `_CACHE` cannot leak between tests even in a shared pytest-xdist-free run; confirmed no shared mutable module-level state outside what `reset()` clears; (3) looked for a FastAPI path/header parsing quirk in `note_id` or `x_company` that the suite's own assertions might not anticipate (type coercion, whitespace, case-folding, duplicate headers) — `note_id` is an untyped path parameter (`str`), so FastAPI performs no coercion beyond URL-decoding, and every test uses a plain ASCII literal; `x_company` is an untyped optional header (`str | None`), and — more importantly — `fixed/app.py`'s `_resolve_company` never reads it at all (round 10's own repair), so no parsing quirk on that parameter can affect authorization in the fixed variant regardless of what FastAPI does with it; (4) looked for a C1/C2 cross-cancellation fake — a single change to both `_CACHE`'s key and `_resolve_company` that is wrong in each function individually but happens to compose correctly on every test in the suite. I could not construct one: `_resolve_company`'s only inputs are `api_key` and (unread) `x_company`, and `_CACHE`'s key is `(note_id, company)` where `company` is always `_resolve_company`'s own return value — any fake that makes `_resolve_company` return a wrong-but-suite-passing value for some input would, by construction, poison `_CACHE`'s key with that same wrong value and be caught by whichever single-claim test already isolates that input (I re-checked this reasoning against the two real fakes already found in these two functions, the header-override fake and the credential-shape fake, and both are caught by a single-claim test exactly because the cache key inherits the wrong company value rather than independently re-deriving it — there is no second, independent source of "company" for a cross-cancellation to hide behind). None of this constitutes a new documented finding; I list it so a twelfth round does not have to re-tread the same ground.

**Other checks performed, all confirmed clean:**

- **Mechanical gates, run myself:** `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.
- **Coverage-contract citations, cross-checked against `items.md`'s own tags, cell by cell.** `spec.md`'s teaching-claims table (C1→#1,#4,#6,#7,#8; C2→#2,#4,#7; C3→#3,#7,#8; C4→#5,#7; C5→#6,#7,#8) still matches `items.md`'s own `Claim assessed` tags exactly. No change from round 10's own re-derivation.
- **Metadata honesty.** `content/modules/2/2.2/module.yaml` still reads `status: draft`, `reviewer: pending independent quality and lab-safety review`, `lastReviewedAt: null`, `nextReviewAt: null` — unchanged by the round-10 fix pass, as required.
- **Shared `content/standards/pins.yaml`'s known cross-branch conflict with `origin/deepen/2.3`, re-fetched fresh.** Still just that: this branch's `owasp-asvs-5.0.0` entry reads `reviewedAt: "2026-09-22"`; `origin/deepen/2.3`'s reads `reviewedAt: "2026-09-23"` with 2.3's own dead-URL narrative appended to the shared `notes` field. Both agree on the corrected `url` (`https://owasp.org/projects/asvs`); no factual disagreement, only a field value and duplicated narration that will need reconciling at merge time, exactly as every round before this one recorded. Not touched.
- **The known Unicode-normalization scope cap (`spec.md`'s "Known residuals" section) was respected — I constructed no further such variant**, per this task's explicit instruction not to spend effort there.

**Verdict: neither gate is granted, for the eleventh time running.** Round 10's finding is genuinely closed, the readability trim is accurate and genuinely serves a first-time learner without losing anything `keys/2.2.md` needs to keep, no new adversarial code fake survives against the current 41-test suite despite a fresh, independent search in areas none of the prior ten rounds specifically targeted (per-test docstring-to-assertion fidelity, fixture state isolation, FastAPI parameter coercion, and a C1/C2 cross-cancellation construction), and every mechanical gate, citation cross-check, and metadata field is unchanged and clean. But the same fresh-read discipline this task asked me to apply to the trim surfaced a real, demonstrated factual inaccuracy one sentence away from it, in the same lesson page: `lessons/05-verify.md` line 70 still says "fourteen passing tests" when the suite has forty-one, a stale reference that survived ten rounds of numeral-pattern grepping only because it is spelled out as a word rather than written as a digit. Per this repository's governing rule, a real, demonstrated inaccuracy in learner-facing content withholds every grant regardless of how strong the surrounding, independently-reconfirmed work is, and regardless of how narrow the fix.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated inaccuracy was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. An eleventh re-review that found a live blocker may not write this line.

## Addendum, 2026-09-24 — twelfth-round re-review at `56127ba`: round-11 finding genuinely closed, no new adversarial code fake found from two genuinely fresh angles, but a second stale count — this time a bare digit rather than a spelled-out word — survives one sentence away from the first

**Independence statement.** I am a twelfth, separate re-review pass, on commit `56127ba` ("2.2: fix a stale spelled-out test count that survived ten rounds of numeral-only greps"), the current tip of `origin/deepen/2.2`. My own worktree's branch was unrelated history (a fresh scratch worktree tracking `main`), so I created a dedicated local branch directly from `origin/deepen/2.2` at this exact commit and confirmed the working tree was clean before touching anything. I read all eleven addenda above in full before touching any file. I constructed no adversarial fake against `fixed/app.py` this round (my two fresh-angle checks below were answered by reading and by running the existing suite, not by editing and restoring the implementation), so there is no restore-and-verify step to report for this round; the only files I have written in this repository this pass are this artifact and scratch grep output kept only in my own shell history, never written to disk in the repo tree.

**Round 11's finding — CLOSED, verified directly.** `lessons/05-verify.md` line 70 now reads "...would overstate what forty-one passing tests actually checked" — confirmed by reading the line myself, not by trusting the changelog's account of it.

**Full pass/fail counts, run myself, not trusted from any doc:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
33 failed, 8 passed, 2 warnings
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
41 passed, 2 warnings
```

Matches every current-tense claim in `README.md`, `items.md`, `rubric.md`, `keys/2.2.md`, and (with one exception, below) `lessons/05-verify.md` (33 of 41 failing, 41 of 41 passing). I additionally listed the eight tests that pass on `vulnerable/` by name (`-v`) and checked each against `05-verify.md` L35's own explanation of why the vulnerable variant should pass exactly these eight and no others: `test_same_company_reads_its_own_note_after_caching` (shared normal case), `test_unregistered_api_key_with_a_company_header_is_denied` and `test_cross_company_lookup_is_scoped_even_before_any_cache_entry_exists` (cases that do not depend on any of the three bugs), `test_anti_fake_credential_shaped_like_a_valid_key_is_still_denied` (named explicitly as the credential-shape anti-fake test), `test_anti_fake_cache_actually_serves_the_second_read` (named explicitly as the cache-actually-fires anti-fake test), and `test_matching_hostname_trusted_ca_current_version_is_trustworthy`, `test_unknown_trust_state_fails_closed`, `test_explicitly_untrusted_ca_is_rejected_even_with_matching_hostname_and_version` (the C3 normal, malformed, and anti-fake-pair cases, which pass on `vulnerable/app.py`'s own `return cert_trusted_ca is True` by the same coincidence — it fails closed on `None` and `False` while checking nothing else — every prior round from the seventh onward has already recorded). All eight named tests match all eight actual passes, one for one, with no unexplained pass and no unexplained failure. `vulnerable/app.py` has not drifted from what the docs assume about it.

**Mechanical gates, run myself:** `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.

**Coverage-contract and teaching-claims citations, re-derived independently from `items.md`'s own `Claim assessed` tags, cell by cell, found unchanged and still fully correct.** C1 → #1, #4, #6, #7, #8; C2 → #2, #4, #7; C3 → #3, #7, #8; C4 → #5, #7; C5 → #6, #7, #8 — matches `spec.md`'s teaching-claims table (L45-49) exactly, no addition or omission, no regression from any prior round. The coverage-contract outcome rows (L59-62) show the same partial-claim-tag pattern every round since the first has read as correct (an outcome's `Claim` column naming more claims than its cited item's own tag, resolved by matching `Outcome:` text rather than claim-tag superset) — not re-litigated, per the standing judgment.

**Metadata honesty and shared `pins.yaml`, both re-confirmed, no new problem.** `content/modules/2/2.2/module.yaml` still reads `status: draft` (L7), `reviewer: pending independent quality and lab-safety review` (L160), `lastReviewedAt: null`, `nextReviewAt: null` (L161-162) — unchanged by the round-11 fix pass, as required. `content/standards/pins.yaml`'s `owasp-asvs-5.0.0` entry on this branch still reads `reviewedAt: "2026-09-22"` with the corrected `url: "https://owasp.org/projects/asvs"`; `origin/deepen/2.3`'s parallel entry (fetched fresh via `git fetch origin deepen/2.3`, not from cache) reads `reviewedAt: "2026-09-23"` with the identical corrected URL and its own independent dead-link narrative appended to the shared `notes` field — the same benign cross-branch conflict on record for all twelve rounds now: no factual disagreement about the URL, only a differing `reviewedAt` and duplicated "I fixed this" narration that will need reconciling at merge time. Not touched, per this task's instruction.

**My own full stale-count sweep, both numeral and spelled-out, across the whole scope named by this task.** Grepped `spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, the lab `README.md`, and `content/assessment/keys/2.2.md` for (a) every numeral "N of M pass/fail" pattern for every prior suite size (14 through 40), and (b) every spelled-out number word from "seven" through "twenty" adjacent to test/pass/fail/fake vocabulary. Both sweeps came back clean: every live, current-tense hit reads "41" or "forty-one" consistently; the only non-"41" hits are inside `module.yaml`'s changelog and `keys/2.2.md`'s own historical bullets, both correctly narrating past rounds' findings in the past tense. Round 11's own fix holds, and no third instance of either pattern shape exists anywhere in this module's tree.

**A different, independently-constructed defect from the same discipline round 11 established — a bare digit, not a word, stale since the very first commit of this lab, that neither sweep pattern above (nor any of the ten numeral sweeps before it) could ever have caught, because the number itself is also, coincidentally, a currently-correct count elsewhere in the same file.**

`lessons/05-verify.md` line 49, in the "Worked example: reading a failure for the reason it names" section, reads: *"A reviewer who reads only "8 failed" without reading which eight, and why, has not verified anything — they have counted."* The vulnerable variant currently fails 33 of 41 tests, not 8 — I ran it myself, above. `8` is not a stale count in the abstract sense the last two rounds' findings were (a number that is simply wrong on its face); it is currently the *correct* count for something else entirely in this exact file — the number of tests that **pass** on `vulnerable/` (confirmed above, and stated correctly one paragraph earlier, at line 35: *"A run that reports "8 passed" for `vulnerable`..."*). This is exactly why no numeral-pattern grep in any of the eleven prior rounds' sweeps (`"of 14"`, `"of 41"`, `"6 of 14"`, etc.) and no spelled-out-word grep in round 11's own sweep could have caught it: the digit `8` legitimately, correctly appears in this file today, one paragraph above the stale occurrence, describing a different quantity. A grep for "any occurrence of a number that isn't 41" would have produced a false negative on this exact line, because 8 is a real, live, correct number in this file — just not the number this particular sentence needs.

I confirmed this by direct git history, not by assuming the number was never updated: this line has read `"8 failed"` verbatim since commit `c26f92b`, the original deepen pass that first built this lab's suite at 14 tests (when the vulnerable variant genuinely did fail 8 of 14, per the very first review round's own confirmed count: `"8 failed, 6 passed"`). None of the ten subsequent count-correcting commits that grew the suite to 15, 16, 17, 18, 19, 34, 35, 37, 39, 40, and now 41 tests — and none of round 11's own fix, which touched the very next paragraph-adjacent occurrence of a stale number in this same file — ever touched this sentence. `git log -p` on this exact line shows it unchanged in every one of those commits.

**This is the identical class of defect round 11 itself just found and blocked on, one sentence away, in the same lesson section, and it survived precisely because round 11's own closing fix looked for spelled-out words while this line uses a digit that hides in plain sight behind a coincidentally-correct neighbor.** The sentence's own point — that a reviewer who reads only a bare count without reading which tests and why "has not verified anything, they have counted" — is undermined by the sentence itself citing a number that is not, in fact, what counting the current suite would produce. A learner who runs `pytest --impl vulnerable` themselves and sees `33 failed` has no way to reconcile that with this sentence's `"8 failed"` without already knowing the module's own history well enough to guess which number is stale.

I checked for a further such instance systematically before concluding this is the only one: grepped both `lessons/05-verify.md` and every other file in scope for every standalone quoted `"N passed"` / `"N failed"` pattern (not just the numeral-adjacency patterns every prior round's sweep used) — there are exactly two such quoted-count phrases in the entire module tree, both on `05-verify.md` (L35 `"8 passed"`, correct; L49 `"8 failed"`, stale), and no third occurrence of this specific rhetorical device anywhere else.

**Severity.** Not a code defect, not an adversarial fake against `fixed/app.py` or `vulnerable/app.py` — a factual inaccuracy in learner-facing prose, the same class round 11's own finding was, in the same file, in the same "Verify" lesson whose entire subject is teaching a learner to read verification claims precisely rather than by vibes. This repository's governing rule treats this class of finding as real and blocking independent of whatever code-level gap does or does not accompany it in a given round, exactly as round 11 concluded for its own, adjacent finding.

**To close:** change `lessons/05-verify.md` line 49's `"8 failed"` to `"33 failed"` (matching this page's own correct usage of the current vulnerable-fail count elsewhere, e.g. line 35's "the thirty-three that should fail do fail for the stated reason," and line 77's "the thirty-three tests that fail"). No other file needs a change; my sweep above found no second instance of either the numeral or spelled-out pattern, and no second instance of this specific quoted-count rhetorical device.

**Two genuinely fresh angles pursued this round, per this task's framing, neither producing a new code gap — recorded so a thirteenth round does not have to re-derive either.**

1. **Does `conftest.py`'s `client` fixture reliably reset state before every single one of the 41 tests, including ones added across eleven rounds by eleven different sessions who never saw each other's code?** Read `conftest.py` in full: the `client` fixture carries no explicit `scope=` argument, so it is function-scoped by pytest's own default — it re-runs `_load()` (a fresh `importlib.util.module_from_spec`/`exec_module`, a brand-new module object, not a cached import) and then `mod.reset()` (clearing `_ORIGIN_STORE` and `_CACHE` explicitly) for every single test that requests it. I then checked whether every test *does* request it, rather than assuming the fixture's existence implies universal use: grepped both test files' `def test_` signatures directly — all nine tests in `test_cache_key.py` and all fifteen named tests plus the nineteen-case parametrized sweep in `test_tls_hop.py` (41 test invocations in total, matching the collected count) take `client` as their sole fixture parameter, with no test calling `create_app()` or touching `_CACHE`/`_ORIGIN_STORE` directly, and no second, differently-scoped fixture defined anywhere in `conftest.py` that any test could request instead. There is no code path by which one test's state could reach another's, regardless of test collection order, and this holds structurally (by construction, not by having run the suite once and observed no leakage) because each test's own module object is discarded after that test's `client` fixture teardown. No gap found.
2. **Does `vulnerable/app.py` still fail every test the module's docs claim, for the reason claimed, or has it quietly drifted from what eleven rounds of `fixed/app.py` and test changes now assume about it?** Read `vulnerable/app.py` fresh, independent of any prior round's account of it, and confirmed by hand that its three defects are exactly what `README.md` and the module's changelog describe (`_resolve_company` reads `x_company` when present; `_CACHE` is keyed on `note_id` alone; `hop_is_trustworthy` returns only `cert_trusted_ca is True`) with no other divergence from `fixed/app.py` beyond those three lines and the docstring. The eight tests that pass on `vulnerable/` (listed above) are exactly and only the eight `05-verify.md` L35 names, for exactly the reasons it gives — I verified this by tracing each one against `vulnerable/app.py`'s actual code rather than trusting the lesson's own explanation of itself. No drift found.

**Other checks performed, all confirmed clean, no regression from round 11:**

- Full fresh read of all eight `lessons/*.md`, `spec.md`, `items.md`, `rubric.md`, `keys/2.2.md`, and the lab `README.md`, asked specifically whether this module reads as a lab-testing exercise that happens to have a curriculum module attached, rather than the reverse. Six of the eight lessons (`01-property.md`, `02-model.md`, `03-break.md`, `04-build.md`, `06-operate.md`, `07-transfer.md`, `08-review.md`) carry no fake-count or test-count narration at all beyond a single, appropriately-scoped reference to the lab's existence — the eleven rounds' forensic weight is confined to `05-verify.md` (already trimmed once, in round 10) and the two files built to hold it (`README.md`'s own trimmed section and `keys/2.2.md`'s complete, examiner-only account). The seven-step loop still reads as a coherent teaching arc for a first-time learner: each lesson derives its claim before asserting it, defines terms in dual form, names rejected alternatives, and closes with an honest "what this page is not doing" statement. I did not find the accumulated lab scrutiny leaking into any lesson beyond the two sections already identified and partially addressed by round 10's trim — and this round's own finding is, itself, confined to one of those same two sections, not a new leak into previously-clean prose.
- Re-attempted, briefly, a construction distinct from every prior round's angle: whether `test_tls_hop.py`'s `_relay` helper or `test_cache_key.py`'s request-building code contains its own bug that could make a test pass on `fixed/` for the wrong reason (a test-side defect, not an implementation-side one) — read `_relay` (`test_tls_hop.py` L15-24) and the request-construction code in three representative `test_cache_key.py` cases end to end; both call the real endpoints through `client.post`/`client.put`/`client.get` with literal, hand-written JSON/header values and assert against the real HTTP response, with no helper-level shortcut, mock, or monkeypatch anywhere in either file (confirmed by grepping both files for `mock`, `patch`, `monkeypatch` — no hits). No test-side gap found.

**Verdict: neither gate is granted, for the twelfth time running.** Round 11's finding is genuinely closed, the module's lesson prose, coverage-contract citations, standards work, metadata honesty, and the cross-branch `pins.yaml` note are all independently re-confirmed strong, `conftest.py`'s reset discipline holds by construction across all 41 tests with no test able to bypass it, and `vulnerable/app.py` has not drifted from what eleven rounds of documentation now assume about it. But the exact discipline this task asked me to apply — running the module's own stale-count sweep myself, in full — surfaced a second, structurally different stale count one sentence away from the first: a bare digit that survived every prior sweep specifically because it coincides with a different, currently-correct number in the same file. Per this repository's governing rule, a real, demonstrated inaccuracy in learner-facing content withholds every grant regardless of how strong the surrounding, independently-reconfirmed work is, regardless of how narrow the fix, and regardless of how many consecutive rounds have already closed other findings.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated inaccuracy was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A twelfth re-review that found a live blocker may not write this line.

## Addendum, 2026-09-24 — thirteenth-round re-review at `ba5fa8e`: round-12 finding genuinely closed, full checklist and a fresh code-level angle both come back clean, but a new blocker found — this time in `module.yaml` itself, not in the lab or the lesson prose

**Reviewed commit:** `ba5fa8e` on `deepen/2.2` ("2.2: fix a second stale test count in a worked-example paragraph").
**Independence statement:** I authored no file in this module's scope and had no prior authoring context before this session. Every judgement below comes from reading the files at `ba5fa8e` and from commands I ran myself.

**Round 12's fix, verified directly, not trusted from its own changelog entry.** `lessons/05-verify.md` line 49 now reads `"33 failed"` / `"which thirty-three"`, not `"8 failed"` / `"which eight"`. Ran the suite myself:

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
...
33 failed, 8 passed, 2 warnings in 0.61s

$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
...
41 passed, 2 warnings in 0.47s
```

Matches every current-tense claim in the module tree. `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.

**Coverage-contract citations, re-derived cell by cell against `items.md`'s own `Claim assessed`/`Outcome` tags, both tables.** Teaching-claims table: C1 → items {1,4,6,7,8}, C2 → {2,4,7}, C3 → {3,7,8}, C4 → {5,7}, C5 → {6,7,8} — every one matches `spec.md`'s own citation exactly, derived independently from each item's tag rather than from `spec.md`'s claim of them. Outcome-coverage table: all four rows' item citations (`#1,#2,#4`; `#3`; `#5`; `#6,#7,#8`) likewise match exactly against each item's own `Outcome:` text. No mismatch found, consistent with every round since the third.

**Metadata honesty on the fields I am specifically forbidden from setting except on a pass: `module.yaml` still reads `status: draft` (L7), `reviewer: pending independent quality and lab-safety review` (L160), `lastReviewedAt: null`, `nextReviewAt: null` (L161-162)** — unchanged by round 12's prose-only fix, as required.

**Shared `content/standards/pins.yaml` vs `origin/deepen/2.3`, re-fetched fresh via `git show origin/deepen/2.3:content/standards/pins.yaml` rather than from any local cache.** Both branches' `owasp-asvs-5.0.0` entries still agree on `url: "https://owasp.org/projects/asvs"` and on every requirement-ID claim; the only difference remains `reviewedAt` (`2026-09-22` here vs. `2026-09-23` on `deepen/2.3`) and `deepen/2.3`'s own additional narration appended to the shared `notes` field describing its own pass. Same benign, factual-agreement-only conflict recorded for thirteen rounds now. Not touched, per this task's instruction.

**My own fresh, broad numeric sweep — not limited to test-pass-count sentences — across `spec.md`, all eight `lessons/*.md`, `assessment/items.md`, `assessment/rubric.md`, the lab `README.md`, and `content/assessment/keys/2.2.md`.** Checked every live (non-historical) numeric or spelled-out count against the current 41-test suite: `05-verify.md` L25's Mermaid annotation (`"8 of 41 pass"`, correctly describing `vulnerable`'s 8 passing tests, not a stale count — I confirmed 8 is in fact `41 - 33`), L28 (`"41 of 41 pass"`), L35/L49 (both now correctly `"33"`-anchored per round 12's fix), L57/L63/L65 (`"Fifteen constructed fakes"` / `"remaining twelve"` / `"all fifteen fakes"` — 3 + 12 = 15, and this matches `module.yaml`'s own changelog count of fifteen *reviewer-found* fakes, distinct from the two demonstration-only fakes the sweep-generality entry names, which the lesson prose correctly never counts among the fifteen). `items.md` L104 and `rubric.md` L13 both read `"33 of 41 fail"` / `"41 of 41 pass"`, correct. The lab `README.md` mirrors the same counts. `content/assessment/keys/2.2.md`'s many non-41 numbers (13 through 40) are exclusively inside historical bullets narrating each past round's suite size at the time, in the past tense, exactly like every other entry in that file's established pattern — none reads as a live claim about the current suite. I also checked module IDs, standard-citation numbers, and ASVS requirement IDs for staleness: `module.yaml`'s nine `v5.0.0-*` requirement IDs match `spec.md`'s "Standards references" section and `pins.yaml`'s verified list exactly (`4.1.3, 12.1.1, 12.2.1, 12.2.2, 12.3.2, 14.2.2, 14.3.2, 14.2.5, 16.3.4`); `estimatedMinutes: 300` matches `spec.md`'s "~300 min" time budget; `masteryGate: '2'` matches `phase: 2`. No lesson claims a word count. No new stale number of any kind found in this sweep, in contrast to rounds 11 and 12.

**One genuinely fresh code-level angle, per this task's instruction, actually constructed and run rather than reasoned about abstractly.** Asked whether any of the 41 tests depends on Python dict iteration or insertion order in a way that is an accident rather than a guarantee, and whether `reset()`'s own clearing behavior is ever verified by a test rather than merely assumed.

- `API_KEYS` in `fixed/app.py` has four distinct string keys mapping to four distinct company values; nothing in `_resolve_company` or any test iterates `API_KEYS` (it is looked up by exact key, `API_KEYS[api_key]`), so there is no code path where key collision or iteration order could matter even under a hypothetically different dict implementation. `_CACHE` and `_ORIGIN_STORE` are both looked up and written by exact tuple key, never iterated, in every test and in both `app.py` variants. No ordering dependency found.
- Read `conftest.py` in full: `_load()` performs a fresh `importlib.util.module_from_spec` / `exec_module` for every single test that requests the `client` fixture (function-scoped by pytest's default, no explicit `scope=`), which means `_ORIGIN_STORE = {}` and `_CACHE = {}` are freshly re-initialized to empty by module execution *before* `mod.reset()` is ever called. I confirmed this by tracing the actual execution order: module exec (dicts already empty) → `mod.reset()` (clears already-empty dicts) → `TestClient(mod.create_app())`. This means `reset()`'s own clearing behavior — as distinct from the fresh module load's own effect — is never actually exercised against non-empty state anywhere in the current 41-test suite; a `reset()` that did nothing at all, or that cleared the wrong dict, would still pass every test today, because the module is never reused across tests to begin with. I verified this is not merely theoretical by constructing the fake directly: replacing `reset()`'s body with `pass` (a no-op) and re-running the full suite.

```
$ cp labs/2.2/2.2-request-path/fixed/app.py /tmp/app.py.orig
$ python3 - <<'PY'
import re, pathlib
p = pathlib.Path("labs/2.2/2.2-request-path/fixed/app.py")
s = p.read_text()
s2 = s.replace("def reset() -> None:\n    _ORIGIN_STORE.clear()\n    _CACHE.clear()",
                "def reset() -> None:\n    pass")
assert s2 != s
p.write_text(s2)
PY
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
...
41 passed, 2 warnings in 0.47s
$ cp /tmp/app.py.orig labs/2.2/2.2-request-path/fixed/app.py
$ md5sum labs/2.2/2.2-request-path/fixed/app.py
ebdf983ea6753464cffd6dee0a669513  labs/2.2/2.2-request-path/fixed/app.py
$ git status --porcelain
```

Confirmed: `41 passed` unchanged, and `fixed/app.py` restored byte-identical to the hash every prior round has recorded, `git status --porcelain` clean afterward. This is a real, demonstrated gap in what the suite's `reset()`-clearing line is actually accomplishing under the current test design — but it is **not a security defect**, and I am treating it as a non-blocking observation rather than a new finding, for a reason distinct from every prior round's severity judgement calls: the fresh-module-per-test design is *itself* the isolation mechanism, and it isolates perfectly regardless of what `reset()` does or does not do (confirmed above — no cross-test leakage is possible, matching round 12's own structural argument for exactly this). `reset()`'s call site is dead-in-practice defensive code, not a load-bearing security control any learner-facing claim depends on; nothing in `spec.md`'s coverage contract, `items.md`, or any lesson asserts that `reset()` itself is tested, and no C1–C5 claim rests on it. I record this so a fourteenth round does not have to re-derive it, but it does not by itself withhold a grant.

**The actual finding this round: `module.yaml`'s first learning outcome is a syntactically incomplete sentence, and has been since it was written.**

`content/modules/2/2.2/module.yaml` lines 18–20 (the first entry under `outcomes:`) read, in full and verbatim:

```
Given SecureCollab's Phase 2 request-path fixture, identify every point where a
shared cache or a forwarded-identity header could substitute a wrong company for
the caller's own bound identity, and justify why each point does or does not
```

The sentence ends there — with `"does or does not"` and no object, no predicate, nothing after it. This is not a stylistic quirk of the codebase: I checked `outcomes:` in the two named quality-reference modules (`1.2`, `1.3`) and in the two other Phase-2 siblings currently sharing this module's un-reviewed status (`2.3`, `2.4`); all eight outcome strings across those four modules are complete, grammatical sentences with no dangling clause anywhere. This module's own remaining three outcomes (lines 21–29) are likewise complete. Only this one, first outcome — the one that maps directly onto this module's primary teaching claims C1 and C2 and anchors the first row of `spec.md`'s coverage-contract table (`"Identify every point where a shared cache or a forwarded-identity header could substitute a wrong company for the caller's own bound identity"`, itself a *different*, complete rephrasing that conveniently never carries the broken tail) — is missing its predicate.

I confirmed this is not a rendering artifact of my own reading: `sed -n '17,20p' module.yaml | cat -A` shows each of the three lines terminated by a plain `$` (end-of-line) with no YAML continuation character, and the next `-` list item begins immediately after, so the YAML plain scalar for this outcome is exactly, and only, the text quoted above.

I traced this by history rather than assuming it was always this way: `git log -S "why each point does or does not" --oneline -- content/modules/2/2.2/module.yaml` returns exactly one commit, `6c49552`, titled **"WIP: deepen module 2.2 (rate-limited mid-pass, continuing separately)"** — the same rate-limited checkpoint the very first review round's header already names as part of what it reviewed (`c26f92b`, "which continues the rate-limited checkpoint at `6c49552`"). The outcome was written mid-sentence when that session was cut off by a rate limit, and the sentence was never finished by any of the twelve subsequent authoring or re-authoring passes — every one of which was responding to a specific reviewer-named code or prose gap and had no reason to re-read `module.yaml`'s `outcomes:` block for grammatical completeness on its own.

**Why this survived twelve rounds of real adversarial pressure.** Every round's forensic attention — mine included, until this pass — went to `labs/2.2/2.2-request-path/`'s code (the version check, the hostname check, the cache mechanism, the credential lookup) and to the test-count prose in `lessons/05-verify.md`, `README.md`, and `keys/2.2.md` that narrates those results. `module.yaml`'s `outcomes:` block is neither code nor a number; it is Pass-A-era metadata that nothing in the module's own remediation history had a specific reason to revisit, since no round's finding was ever about outcome wording. This round's task explicitly asked for a broader sweep than "any number that looks wrong," and reading every learner/curriculum-facing field for whether it is internally coherent — not only whether a digit is correct — is what surfaced a defect class none of the prior twelve rounds' methodologies would have caught: none of them grepped for sentence completeness, only for numbers.

**Why this is a real, blocking defect and not a cosmetic nit.** `module.yaml`'s changelog itself (2026-09-22 entry) states this pass "[r]ewrote outcomes as observable behaviours" as one of its explicit remediation actions (D12) — the module's own authoring history treats outcome wording as a load-bearing metadata field, not decoration. An outcome that does not parse cannot be an observable behavior: a learner, an assessor, or a future `deepen-module` pass reading this field cannot tell what "each point does or does not" is supposed to be justified *against*. This is the same class of defect `AGENTS.md`'s `metadata-honesty` concern and this module's own `quality-gate` step 2 ("[a]ny empty cell is a blocker") are aimed at, one level up: not an empty cell, but a cell whose content cannot be read as a complete claim at all. It sits in the single most central outcome of the module — the one gating C1/C2, the module's primary security property — not in a peripheral field.

**To close:** complete the sentence at `module.yaml` lines 18–20 so `"...and justify why each point does or does not"` has an object (e.g., "...and justify why each point does or does not require trusting an unauthenticated header or a path-only cache key" — the reviewer does not prescribe the exact wording, only that it must state a complete, checkable claim, consistent with the other three outcomes' shape). No other file needs a change for this finding; `spec.md`'s own paraphrase of this outcome is already complete and does not need to change.

**Other checks performed, all confirmed clean, no regression from round 12:** full fresh read of all eight `lessons/*.md`, `spec.md`, `items.md`, `rubric.md`, `keys/2.2.md`, and the lab `README.md` for internal coherence beyond numbers (dates, requirement IDs, file paths, cross-reference link targets) — no other incomplete sentence, dangling clause, or broken cross-reference found anywhere else in this module's tree. `vulnerable/app.py` re-read fresh and re-confirmed unchanged from what all thirteen rounds now assume about it (`_resolve_company` reads `x_company` when present; `_CACHE` keyed on `note_id` alone; `hop_is_trustworthy` returns only `cert_trusted_ca is True`).

**Verdict: neither gate is granted, for the thirteenth time running.** Round 12's fix is genuinely closed, the full mechanical checklist (pytest both variants, lint, lab matrix, coverage-contract citations cell by cell, metadata honesty, `pins.yaml` cross-branch state) is independently re-confirmed clean, a fresh code-level angle on dict ordering and `reset()` was actually constructed and run rather than reasoned about abstractly and found nothing security-relevant, and my own broad numeric sweep found no third stale count. But a genuinely different class of defect — not a number, not a code fake, a syntactically incomplete outcome statement dating to the module's original rate-limited WIP checkpoint — was found in `module.yaml` itself, in the single outcome most central to this module's teaching claims. Per this repository's governing rule, this withholds every grant regardless of how strong and how independently reconfirmed the rest of the module now is.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated defect was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry is left untouched.

STATUS was updated by: nobody. A thirteenth re-review that found a live blocker may not write this line.

## Addendum, 2026-09-24 — fourteenth-round re-review at `eba2e50`: round-13 finding genuinely closed, but a new, previously-undetected blocker found — a miscounted sweep size that propagated identically across four files

**Independence statement.** I fetched and checked out `origin/deepen/2.2` fresh into a clean worktree (`git fetch origin deepen/2.2 && git checkout -B deepen/2.2 origin/deepen/2.2`) rather than trusting any local state. `HEAD` is `eba2e50`, "2.2: complete a broken outcome sentence left by a rate-limited WIP commit" — the commit closing round 13's finding above. I read all thirteen prior rounds' addenda in full before touching any file, and read `AGENTS.md` at the repo root first, as instructed.

**Round 13's fix, re-verified directly.** `content/modules/2/2.2/module.yaml`'s first outcome (lines 19–22) now reads in full: "Given SecureCollab's Phase 2 request-path fixture, identify every point where a shared cache or a forwarded-identity header could substitute a wrong company for the caller's own bound identity, and justify why each point does or does not actually let a wrong company through in the code as written." This is a complete, grammatical sentence with a clear object ("actually let a wrong company through in the code as written"), and it cross-references cleanly against `spec.md`'s C1/C2 coverage-contract row, which the round-13 addendum correctly predicted would need no change. The module's other three outcomes remain complete, as they were.

**Metadata-honesty fields, confirmed untouched pre-my-edit.** `module.yaml` L8 reads `status: draft`; L162 reads `reviewer: pending independent quality and lab-safety review`; L163–164 read `lastReviewedAt: null` and `nextReviewAt: null`. No `depth` or `quality` key exists in `module.yaml` at all (confirmed against `content/schema/module.schema.json`, which has no such property and sets `additionalProperties: false` — those two labels live only in `content/progress/STATUS.yaml`'s per-module `modules:` entries, not in this file, contrary to a literal reading of this task's own framing). `content/progress/STATUS.yaml`'s `2.2` entry still reads `quality: developing` / `depth: map-complete`, unchanged.

**Full suite re-run myself, both variants:**

```
$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl vulnerable -q
...
33 failed, 8 passed, 2 warnings in 0.54s

$ python3 -m pytest labs/2.2/2.2-request-path/tests --impl fixed -q
...
41 passed, 2 warnings in 0.43s
```

Matches every current-tense "33 failed"/"8 passed"/"41 passed" claim in the module tree. `fixed/app.py` re-hashed: `md5sum` reads `ebdf983ea6753464cffb6dee0a669513`, matching every prior round's recorded digest — I made no edit to it this round, so this merely confirms no drift occurred. `python3 scripts/lint_content.py content/modules/2/2.2 --no-baseline` → `0 error(s), 0 warning(s)`. `bash scripts/run_labs.sh 2.2` → `vulnerable failed, fixed passed, RESULT OK`.

**The actual finding this round: the "near-miss version sweep" is repeatedly and consistently described as having nineteen cases, in four separate learner-and-examiner-facing files, when it has eighteen.**

I counted the sweep's own source list directly, twice, by two independent methods:

1. Reading `_VERSION_NEAR_MISSES` in `labs/2.2/2.2-request-path/tests/test_tls_hop.py` (lines 282–301) by eye and by a small script that parses the list literal: 18 string entries — `" 1.2"`, `"1.2 "`, `"\t1.2"`, `"1.3\t"`, `"1.2\r\n"`, `"01.2"`, `"1.02"`, `"1.20"`, `"1.2.0"`, `"+1.3"`, `"1.2e0"`, `"1.23"`, `"1.234"`, `"TLSv1.2"`, `"v1.3"`, `"1.2 or 1.3"`, `"１.２"` (fullwidth), `"1.2​"` (zero-width space).
2. `python3 -m pytest tests/test_tls_hop.py --impl fixed --collect-only -q | grep -i sweep`: exactly 18 parametrized `test_anti_fake_version_near_miss_sweep_is_rejected[...]` node IDs are collected, one per entry above, confirming pytest itself sees 18 cases, not 19.

Both counts agree: **18**, not 19. Yet the following four files all claim 19, in a mutually reinforcing way that would read as internally consistent to anyone checking only that they agree with each other rather than with the actual test file:

- `content/modules/2/2.2/spec.md` L47 (the C3 coverage-contract row, a *live*, current-tense claim, not a historical narration): "a nineteen-case version-string near-miss sweep including a Unicode-confusable case and a zero-width-character case".
- `content/modules/2/2.2/lessons/05-verify.md` L17 (the coverage table, live): `test_anti_fake_version_near_miss_sweep_is_rejected` "(19 parametrized cases)".
- `content/modules/2/2.2/lessons/05-verify.md` L77 (the Practice section, live): "the nineteen parametrized near-miss-sweep cases all evidence the same row — treat them as one piece of evidence repeated nineteen ways, not nineteen independent findings".
- `labs/2.2/2.2-request-path/README.md` L34 (live): "one nineteen-case parametrized sweep covering ASCII and Unicode-adjacent version-string near-misses in one pass".
- `content/assessment/keys/2.2.md` L11 (live, the examiner summary, not a historical bullet): "one nineteen-case parametrized version-string near-miss sweep".

**Root cause, traced through `module.yaml`'s own changelog.** The sweep started at 15 cases (the "proactive strategy change" entry). Round 6's closure correctly added `"1.02"` "as a sixteenth sweep case" (15→16, correct arithmetic). Round 7's closure correctly added `"１.２"` "as a seventeenth version-sweep case" (16→17, correct). Round 8's closure then added `"1.2​"` (the zero-width case) and labeled it **"a nineteenth sweep case"** — but 17 + 1 = 18, not 19. This one-off arithmetic slip in round 8's own closure commit (`content/assessment/keys/2.2.md` L26 and the matching `module.yaml` changelog entry both say "nineteenth") is where the error originates; it did not affect the *total suite size* arithmetic at the time (37→39 is correct: +1 sweep case +1 new dedicated hostname test = +2, and that total-count math is right), only the sweep's own internal item-number label. Every subsequent round's numeric sweeps (round 11's spelled-out-number grep, round 12's "N failed/passed" grep, round 13's sentence-completeness read) checked different things and had no reason to recount a list of string literals inside a test file, so this one mislabeling was copied forward at face value into `spec.md`, `05-verify.md` (twice), the lab `README.md`, and `keys/2.2.md` without anyone re-deriving it from the actual code — the exact discipline this module's own changelog claims to apply to *test totals* ("[r]e-derived every one of the ... fakes' pass/fail counts against the new N-test suite from scratch rather than incrementing the prior entries' numbers") was never applied to the sweep's own case count, only to the *consequences* of that count (which fakes fail how many sweep cases).

**Why this is a real, blocking defect and not a cosmetic nit, consistent with this module's own established bar.** This is the same defect *class* rounds 11 and 12 each already treated as a blocker — a stale/incorrect count in learner- and examiner-facing prose that a reviewer or learner would use to verify the module's own evidentiary claims — except this one is not stale (left over from an earlier suite size) but simply miscounted from the day it was introduced, and it is wrong in *four* files simultaneously rather than one, because every later file's author copied the number from an earlier file's claim rather than from the code. A learner or a future reviewer told "this is a nineteen-case sweep" and then counting 18 entries in the actual list has been given a factually false claim about the size of the evidence base — precisely the harm rounds 11/12 named ("a reviewer who reads only '33 failed' without reading which thirty-three ... has not verified anything — they have counted," `05-verify.md` L49, ironically the same file that itself miscounts the sweep two paragraphs later). It also means the suite's own internal narrative in `content/assessment/keys/2.2.md` — the file explicitly positioned as "the complete, unabridged forensic account" that `05-verify.md` L65 and the lab `README.md` L38 both defer to as the authoritative count — contains the same error, so a reviewer or learner who follows the module's own advice to "go check the examiner key for the exact numbers" lands on the same wrong number, not a correction.

**To close:** replace "nineteen"/"19"/"nineteenth" with "eighteen"/"18"/"eighteenth" (as appropriate to each sentence) in `spec.md` L47, `lessons/05-verify.md` L17 and L77, `labs/2.2/2.2-request-path/README.md` L34, and `content/assessment/keys/2.2.md` L11 and L26 (the L26 instance is a historical bullet narrating round 8's own action and should be corrected the same way round 11 corrected historical-sounding-but-actually-current prose — the module's own established convention treats a bullet's arithmetic as something to get right, not as sacred historical record, since round 9's own entry explicitly redid "seven arithmetic slips a blanket find-and-replace introduced"). I have not made this edit myself, per this task's instruction; per that same instruction I have touched no file except this one.

**Other checks performed this round, all confirmed clean.** Read `fixed/app.py` and `vulnerable/app.py` in full, fresh (both unchanged from every prior round's description: `_resolve_company` in `vulnerable/` reads `x_company` when present; `_CACHE` there is keyed on `note_id` alone; `hop_is_trustworthy` there returns only `cert_trusted_ca is True`). Read both test files (`test_cache_key.py`, `test_tls_hop.py`) in full rather than sampling, and independently re-derived the "ten anti-fake tests" claim for C3 by counting distinct test *functions* (not parametrized cases) in `test_tls_hop.py` whose name starts `test_anti_fake_`: nine dedicated functions (hostname prefix, substring-anywhere, suffix-without-boundary, unicode-confusable, zero-width; version sorts-high, forward-compatible-allow-list, parses-in-range, incidental-whitespace) plus the one sweep function itself = 10, matching `spec.md` L47's "ten anti-fake tests" and confirming that specific count is right even though the sweep's own internal case count, cited in the same sentence, is wrong. Re-derived the C1/C2 "two anti-fake tests" counts in `spec.md` L45–46 against `test_cache_key.py`'s actual function names: also correct. Checked `module.yaml`'s `standardsRefs` block and `spec.md`'s "Standards references" section against each other: all nine `v5.0.0-*` requirement IDs match exactly, and both correctly label `v5.0.0-14.2.5` as the Level-3 exception among otherwise-Level-2 citations. Skimmed all eight `lessons/*.md` files fresh for the seven-step loop (Property/Model/Break/Build/Verify/Operate/Generalize): each lesson's filename and `Loop step:` frontmatter line map onto exactly one step, `08-review.md` maps onto step 5 (Verify) a second time as a seeded code-review exercise rather than an eighth step, consistent with `spec.md`'s own lesson-inventory table, and I found no lesson claiming a step it does not deliver. Checked lab-safety conventions: `README.md`'s "Authorized scope" line, `SECURITY.md` in both `vulnerable/` and `fixed/`, and `conftest.py`'s per-test fresh-module-load-plus-`reset()` pattern are all present and consistent with rounds 1–13's own descriptions; no real target, no real secret, no production coupling found anywhere in this module's tree. Did not re-examine the `pins.yaml` cross-branch conflict with `deepen/2.3` beyond confirming it is still the same benign, note-text-only divergence every prior round has recorded — out of scope for this module per this task's own framing, and not touched.

**Verdict: neither gate is granted, for the fourteenth time running.** Round 13's fix is genuinely closed, the full mechanical checklist (pytest both variants, lint, lab matrix, metadata honesty) is independently re-confirmed clean, the anti-fake-test-count claims I re-derived from scratch (ten for C3, two each for C1/C2) are correct, and the seven-step loop and lab-safety conventions both check out across every lesson file. But a new, previously-undetected blocker — a miscounted test-sweep size, wrong by one and repeated identically across `spec.md`, `lessons/05-verify.md` (twice), the lab `README.md`, and the examiner key — survived fourteen rounds of otherwise-thorough numeric scrutiny because every prior round's numeric sweep checked *suite totals* or *spelled-out words*, never a raw count of literal entries inside a parametrized test's own source list. Per this repository's governing rule, this withholds every grant.

**I am not writing `depth`, `quality`, `reviewer`, `lastReviewedAt`, `nextReviewAt`, or `status: published` anywhere** — not in this file, not in `module.yaml`, not in `content/progress/STATUS.yaml` — because a real, demonstrated defect was found in this pass. `content/modules/2/2.2/module.yaml`'s `reviewer: pending independent quality and lab-safety review` and `status: draft` are left exactly as they were; `content/progress/STATUS.yaml`'s `2.2` entry (`quality: developing` / `depth: map-complete`) is left untouched.

STATUS was updated by: nobody. A fourteenth re-review that found a live blocker may not write this line.
