# Examiner notes — 8.1 (not learner-facing)

## Property

A client `integrity=ok` field must not authorize export. The APK is outside the TCB. Server-verified attest is a signal used by the trusted layer (`v5.0.0-8.3.1`), not Play Integrity as 1.2. MASVS 2.1.0 `MASVS-PLATFORM`; `MASVS-RESILIENCE-1` is cost-raising residual. Do not accept obsolete MASVS L1/L2/R as current levels.

## Lab

- `labs/8.1/8.1-lab/vulnerable/` must fail: `test_client_integrity_claim_is_not_authorization`
- Honest-path `test_server_attest_may_allow_export` may pass on both
- `test_missing_client_claim_does_not_authorize` may pass on both
- `labs/8.1/8.1-lab/fixed/` must pass all
- Root cause: policy evaluated on the attacker’s CPU

## Seeded review intended findings

- `if integrity==ok: export`
- No server-attest test
- Secrets in the APK (8.4)
- MASVS as a sticker / L1/L2/R

## Transfer

Reject Top 10 / CWE / Play-Integrity-as-grant / live Frida. Accept clinic `hipaaMode`; APK feature flags; `premium=true`.

## Operate

Accept a detect/recover note that does not log bodies or attestation blobs. Example: `attest_fail_export_denied`.
