# Examiner notes — 3.3 (not learner-facing)

## Property

The FastAPI runtime DB role must not SELECT another tenant’s rows even if a handler forgets a WHERE. Architecture is a second mediation, not a substitute for 1.2. SQLAlchemy, VPC, and microservices are not this sentence.

ASVS anchors: `v5.0.0-8.4.1`, `v5.0.0-8.2.2`, `v5.0.0-8.3.1`; `v5.0.0-15.2.5` is **Level 3, advanced**. CISA Secure by Design pin is **unverified**. SSDF 1.1 final; 1.2 draft.

## Lab

- `labs/3.3/3.3-lab/vulnerable/` must fail `test_app_role_cannot_read_other_tenant` (and migrator/runtime-role tests)
- `labs/3.3/3.3-lab/fixed/` must pass, including own-tenant allow
- Root cause: one omnipotent DB user shared by app and migrate

## Seeded review intended findings

- DATABASE_URL uses superuser
- Comment “RLS later” in production path
- Analytics role SELECT *
- No test can_select(app, tB, tA) is False

## Transfer

Reject Top 10 / CWE as the definition of security.
Acceptable direction: serverless shared admin connection string; clinic billing replica (invoices ≠ chart text).
WCAG only if a human-mediated control is in the rewritten claim.

## Operate

Accept detect/recover that does not log bodies. Example: `grant_drift` / `wrong_db_role` plus role name; rotate.
Reject “we are in a private VPC.”
