# Examiner notes — 6.6 (not learner-facing)

## Property

An invite token must be single-use. The second `accept('t1')` is denied. TOCTOU and retries (2.4) are the same family with a different “once.” ASVS anchors: `v5.0.0-2.3.4`, `v5.0.0-2.3.3`, `v5.0.0-16.5.3`; `v5.0.0-16.5.4` is **Level 3, advanced**. Top 10 A10 is awareness after the cause.

## Lab

- `labs/6.6/6.6-lab/vulnerable/` must fail: `test_invite_token_is_single_use` (and the distinct-token second-accept on `t2`)
- First accept of a fresh token may pass on both
- `labs/6.6/6.6-lab/fixed/` must pass all
- Root cause: token not marked used

## Seeded review intended findings

- `accept` always true
- No unique constraint / no used write
- Fail-open on DB error
- Token in query logs (4.3)

## Transfer

Reject Top 10 / CWE as the definition. Accept clinic invite-guardian; password reset; 2.4; 7.4.

## Operate

Accept a detect/recover note that does not log tokens. Example: `invite_replay_denied`.
