# Examiner notes — 6.1 (not learner-facing)

## Property

A filename or list target is data, not a shell program. argv_for_list must not invoke a shell. Structural APIs (argv list, parameterized SQL in 5.5) are the mechanism; denylists of metacharacters are incomplete.

## Lab

- `labs/6.1/6.1-lab/vulnerable/` must fail: User-controlled name executed via a shell string
- `labs/6.1/6.1-lab/fixed/` must pass.
- Root cause: Concatenating untrusted data into a shell grammar.

## Seeded review intended findings

- shell=True or sh -c concatenation
- Blacklist of ; as the fix
- No uses_shell test
- Comment “user is trusted internally”

## Transfer

Reject answers that name Top 10 / CWE Top 25 items as the definition of security.
Require a system-specific invariant, attacker, trust, residual risk.
Acceptable transfer direction: Jinja, SQL, mail headers.

## Operate

Accept a detect/recover note that does not log bodies/secrets. Example signal: child_process_anomaly.
