# Examiner notes — 6.5 (not learner-facing)

## Property

The lab fetcher must not allow a link-local metadata URL. SSRF is a trust-boundary fail: the server’s network is not the user’s to steer. HTTPS to the named lab host may be allowed. Tests must not fetch. ASVS anchors: `v5.0.0-1.3.6`, `v5.0.0-13.2.4`, `v5.0.0-3.7.2`; `v5.0.0-3.7.3` is **Level 3, advanced**. API7 is awareness after the cause.

## Lab

- `labs/6.5/6.5-lab/vulnerable/` must fail: `test_link_local_metadata_is_denied` and `test_loopback_is_denied`
- Honest-path `test_lab_host_https_ok` may pass on both
- `labs/6.5/6.5-lab/fixed/` must pass all
- Root cause: server would fetch attacker-chosen authority

## Seeded review intended findings

- `requests.get(user_url)` / scheme-only allow
- https-only regex still allows a metadata IP
- Follows redirects off allow-list
- No link-local deny test

## Transfer

Reject Top 10 / CWE as the definition. Accept clinic PDF URL; webhooks (7.3).

## Operate

Accept a detect/recover note that does not log full URLs with tokens. Example: `egress_denied`. Never rotate a real instance role in this course.
