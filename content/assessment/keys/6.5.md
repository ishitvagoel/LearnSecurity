# Examiner notes — 6.5 (not learner-facing)

## Property

The lab fetcher must not allow http://169.254.169.254/ (link-local metadata). SSRF is a trust-boundary fail: the server’s network is not the user’s to steer. HTTPS to a named lab host may be allowed.

## Lab

- `labs/6.5/6.5-lab/vulnerable/` must fail: Server-side fetch to link-local metadata is allowed
- `labs/6.5/6.5-lab/fixed/` must pass.
- Root cause: Server fetches attacker-chosen authority.

## Seeded review intended findings

- requests.get(user_url)
- https-only regex still allows metadata IP
- Follows redirects off allow-list
- No 169.254 test

## Transfer

Reject answers that name Top 10 / CWE Top 25 items as the definition of security.
Require a system-specific invariant, attacker, trust, residual risk.
Acceptable transfer direction: Webhook delivery (7.3) is egress too.

## Operate

Accept a detect/recover note that does not log bodies/secrets. Example signal: egress_denied{host}.
