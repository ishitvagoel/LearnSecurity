# Examiner notes — 7.1 (not learner-facing)

## Property

A member PATCH must not set `is_admin` from the client document. Extra keys are not writable fields. ASVS anchors: `v5.0.0-15.3.3`, `v5.0.0-4.3.2` (introspection off — inventory), `v5.0.0-4.3.1` (query cost — different grain / 6.7); `v5.0.0-4.1.4` is **Level 3, advanced**. OpenAPI 3.1.1 is inventory. API8/API9 are awareness after the cause.

## Lab

- `labs/7.1/7.1-lab/vulnerable/` must fail: `test_is_admin_cannot_be_patched`
- Honest-path `test_display_name_can_be_patched` may pass on both
- `test_unknown_key_does_not_appear` must fail on vulnerable, pass on fixed
- `labs/7.1/7.1-lab/fixed/` must pass all
- Root cause: binder maps any key (`user.update(body)`)

## Seeded review intended findings

- `user.update(body)` / `__dict__.update`
- Undocumented route not in inventory
- No `is_admin` deny test
- OpenAPI not generated from running handlers

## Transfer

Reject Top 10 / CWE / OpenAPI-as-control as the definition. Accept clinic PATCH `is_staff`; GraphQL mutation arguments; gRPC unknown fields.

## Operate

Accept a detect/recover note that does not log the PATCH body. Example: `unknown_field_rejected`.
