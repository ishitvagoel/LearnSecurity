# Examiner notes — 7.4 (not learner-facing)

## Property

A leftover user session is not worker identity. Exports run as `worker-sc`. ASVS anchors: `v5.0.0-13.2.1`, `v5.0.0-13.2.2`; `v5.0.0-8.3.3` is **Level 3, advanced**. NIST SP 800-207 is architecture guidance, not a product.

## Lab

- `labs/7.4/7.4-lab/vulnerable/` must fail: `test_user_session_is_not_worker_identity`
- Honest-path `test_service_principal_is_worker_identity` may pass on both
- `test_alice_and_wrong_service_is_rejected` must fail on vulnerable, pass on fixed
- `labs/7.4/7.4-lab/fixed/` must pass all
- Root cause: ambient user context (`user_session or service`)

## Seeded review intended findings

- `user_session or service` / copy request cookies into the job
- Worker uses superuser DATABASE_URL
- No leftover-session deny test
- Retry duplicates (2.4)

## Transfer

Reject Top 10 / CWE / zero-trust-as-product / internal-queue-is-trusted as the definition. Accept clinic batch-export; outbox; event schemas.

## Operate

Accept a detect/recover note that does not log cookies or bodies. Example: `worker_identity_wrong`.
