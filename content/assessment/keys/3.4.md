# Examiner notes — 3.4 (not learner-facing)

## Property

A note share grant cannot be applied enough times to exceed the product cap (5 members). The cap is on the write path. HTML `max` is not enforcement. Rate limits (6.7) are a different cell. Idempotency of the same share (2.4) is a different cell.

ASVS anchors: `v5.0.0-2.1.3`, `v5.0.0-2.2.2`, `v5.0.0-2.3.2`, `v5.0.0-2.3.4`; `v5.0.0-2.3.5` is **Level 3, advanced**. API Top 10:2023 API4/API6 are **awareness**. WCAG 4.1.3 is the denial announcement, not the cap.

## Lab

- `labs/3.4/3.4-lab/vulnerable/` must fail `test_share_cap_is_enforced` and `test_sixth_does_not_increment`
- `labs/3.4/3.4-lab/fixed/` must pass, including five honest shares
- Root cause: policy only in the UI

## Seeded review intended findings

- Cap in React only
- No transaction around count+insert
- Test loops 8 times and expects success
- Support tool bypasses cap without audit

## Transfer

Reject Top 10 / CWE-799 as the definition of security.
Acceptable direction: clinic max 3 guardians; invite one-use (6.6); export quota (6.7).
WCAG 4.1.3 if a human sees the denial.

## Operate

Accept detect/recover that does not log bodies. Example: `share_cap_denied` plus note id and count; trim extras.
Reject “we have a WAF.”
