# Examiner notes — E4 (not learner-facing)

## Property

A copy into a 4-byte lab buffer must not return more than 4 bytes. Length is complete mediation of the buffer object.

## Lab

- `labs/E4/e4-lab/vulnerable/` must fail: Copy returns more bytes than the destination length
- `labs/E4/e4-lab/fixed/` must pass.
- Root cause: Trusting n over dst.

## Seeded review intended findings

- copy returns full src
- No dest length check
- unsafe FFI in Kotlin
- “Python so we are memory safe” with a C wheel

## Transfer

Reject answers that name Top 10 / CWE Top 25 items as the definition of security.
Require a system-specific invariant, attacker, trust, residual risk.
Acceptable transfer direction: Image parser; protobuf C.

## Operate

Accept a detect/recover note that does not log bodies/secrets. Example signal: overlong_copy_denied.
