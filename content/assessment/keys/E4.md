# Examiner notes — E4 (not learner-facing)

## Property

A copy into a 4-byte lab buffer must not return more than 4 bytes. Length is complete mediation of the buffer object. CISA Case for Memory Safe Roadmaps (2023-12-06) is manufacturer guidance, not the lab oracle. ASVS `v5.0.0-5.3.1` related; `v5.0.0-5.3.3` labeled Level 3 advanced. CWE-119 / CWE-787 are awareness after the cause. Do not mark Gate 7 or M2 complete.

## Lab

- `labs/E4/e4-lab/vulnerable/` must fail: `test_copy_does_not_exceed_buffer`
- Honest-path `test_short_copy_may_fit` may pass on both
- `labs/E4/e4-lab/fixed/` must pass all
- Root cause: declared length trusted over destination size

## Seeded review intended findings

- Copy returns declared_len plus slack / full src
- No destination length check
- Unsafe FFI treated as bounded because the app is Kotlin
- "Python so we are memory safe" with a C wheel

## Transfer

Reject Top 25 / CWE-as-syllabus / language-as-property / native PoC / “Gate 7 complete.” Accept clinic DICOM parser; protobuf C extension.

## Operate

Accept a detect/recover note that does not log file bytes. Example: `copy_length_denied`.
