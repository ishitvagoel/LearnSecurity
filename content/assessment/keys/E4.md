# Examiner — E4

Vulnerable `--impl` fails the property test. Fixed passes.

Forbidden: copy returns more bytes than the buffer. Teaching bounds, not an exploit.

Do not publish this file on the learner site.
