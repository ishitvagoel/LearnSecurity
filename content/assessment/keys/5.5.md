# Examiner notes — 5.5 (not learner-facing)

## Property

`fetch_sql` must bind tenant and note id as parameters, not concatenate a string the SQL interpreter will parse as code. Application 1.2 is necessary; it is not interpreter isolation. ASVS anchors: `v5.0.0-1.2.4`, `v5.0.0-8.4.1`, `v5.0.0-13.2.2`; `v5.0.0-16.3.2` Level 3 clause is **advanced**. PostgreSQL RLS is platform, not the property.

## Lab

- `labs/5.5/5.5-lab/vulnerable/` must fail: `test_query_is_bound_not_concatenated` (and the honest bound-shape test, because concatenation is still a `str`)
- `labs/5.5/5.5-lab/fixed/` must pass all
- Root cause: data mixed into SQL grammar

## Seeded review intended findings

- f-string `SELECT` interpolating `note_id`
- ORM `.filter` with raw strings
- RLS disabled in tests “for speed” and forgotten
- No `is_bound` assertion

## Transfer

Reject Top 10 / CWE as the definition. Accept clinic search box; NoSQL/GraphQL as 7.1 same shape.

## Operate

Accept a detect/recover note that does not log bodies or SQL-with-values. Example: `sql_error_spike`.
