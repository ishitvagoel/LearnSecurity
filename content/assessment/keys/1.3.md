# Examiner notes — 1.3 (not learner-facing)

## Lab

`pytest tests/test_boundary.py --impl vulnerable` fails the client-header test. `--impl fixed` passes all.

## Seeded findings

1. `X-SecureCollab-Internal` as TCB — false assurance.
2. Two “layers” sharing one identifier — not defense in depth.
3. Client in the same trust box as API — blast radius lie.

## Transfer

Reject port lists / Top 10 as the surface definition.
