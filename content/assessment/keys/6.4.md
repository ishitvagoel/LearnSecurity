# Examiner notes — 6.4 (not learner-facing)

## Property

A user-supplied path must not resolve outside the lab root. `../etc/passwd` is data that tried to become a different object. This is not a weaponized exploit lesson — we assert prefix.

## Lab

- `labs/6.4/6.4-lab/vulnerable/` must fail: Resolved path escapes the lab root
- `labs/6.4/6.4-lab/fixed/` must pass.
- Root cause: Path grammar mixed with data; no canonicalization.

## Seeded review intended findings

- open(user_path)
- Blacklist of '..' only
- Trust Content-Type
- No prefix test

## Transfer

Reject answers that name Top 10 / CWE Top 25 items as the definition of security.
Require a system-specific invariant, attacker, trust, residual risk.
Acceptable transfer direction: XML entity expansion; pickle; YAML load.

## Operate

Accept a detect/recover note that does not log bodies/secrets. Example signal: path_escape_denied; malware-scan is extra.
