# Examiner notes — 6.4 (not learner-facing)

## Property

A user-supplied path must not resolve outside the lab root. `../` is data that tried to become a different object. Assert prefix; do not read host files. ASVS anchors: `v5.0.0-5.3.2`, `v5.0.0-5.3.1`, `v5.0.0-5.2.2`; `v5.0.0-5.3.3` is **Level 3, advanced**. CWE-22/434/502 are awareness after the cause.

## Lab

- `labs/6.4/6.4-lab/vulnerable/` must fail: `test_dotdot_does_not_escape_root`
- Honest-path `test_honest_relative_stays_under_root` may pass on both
- `labs/6.4/6.4-lab/fixed/` must pass all
- Root cause: path grammar mixed with data; no canonicalization

## Seeded review intended findings

- join / `open(user_path)` without canonicalize
- Blacklist of `..` only
- Trust Content-Type
- No prefix test

## Transfer

Reject Top 10 / CWE as the definition. Accept clinic scan upload; XML/pickle/YAML as other parsers.

## Operate

Accept a detect/recover note that does not log PHI filenames. Example: `path_escape_denied`.
