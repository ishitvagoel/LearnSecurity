# Examiner notes — 4.5 (not learner-facing)

## Property

A bearer JWT with the wrong audience must be rejected. Tokens for other-api are not sessions for securecollab-api. Delegation is not authentication theater. ASVS anchors: `v5.0.0-10.3.1`, `v5.0.0-10.1.1`, `v5.0.0-10.2.1`, `v5.0.0-10.5.4`; `v5.0.0-10.3.5` is **Level 3, advanced**. RFC 9700, RFC 10017 (August 2026), RFC 8252 are final. OAuth 2.1 is **draft**.

## Lab

- `labs/4.5/4.5-lab/vulnerable/` must fail: `test_wrong_audience_is_rejected`, `test_missing_audience_is_rejected`
- Honest-path `test_expected_audience_is_accepted` may pass on both
- `labs/4.5/4.5-lab/fixed/` must pass all
- Root cause: `sub` (or signature) accepted without audience

## Seeded review intended findings

- verify signature, skip aud
- ID token used as API access token
- Implicit flow in SPA README
- No test other-api aud

## Transfer

Reject answers that name Top 10 / CWE Top 25 items as the definition of security.
Require a system-specific invariant, attacker, trust, residual risk.
Acceptable transfer direction: Clinic wrong-aud FHIR token; RFC 8252 native redirect vs BFF/SPA storage.

## Operate

Accept a detect/recover note that does not log bodies/raw tokens. Example signal: `jwt_aud_mismatch`; `client_revoked`.
