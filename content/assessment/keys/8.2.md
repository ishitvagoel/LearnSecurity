# Examiner notes — 8.2 (not learner-facing)

## Property

A cached note must not be plaintext on disk. Private app directories are not encryption. Biometric lock is local AUTH-2, not 4.2 server MFA. MASVS 2.1.0 `MASVS-STORAGE-1` / `STORAGE-2` / `CRYPTO-2` / `AUTH-2`. Lab `aead:` prefix is a stand-in, not real AES. MASTG 2.0.0 for tests.

## Lab

- `labs/8.2/8.2-lab/vulnerable/` must fail: `test_cached_note_is_not_plaintext_on_disk`
- Honest-path `test_other_body_is_not_reported_as_plaintext_secret` may pass on both
- `labs/8.2/8.2-lab/fixed/` must pass all
- Root cause: bodies written as text files

## Seeded review intended findings

- Write body to cache.txt
- Backup allowed for the app
- No wipe on logout
- Note in notification text

## Transfer

Reject Top 10 / CWE / private-dir-as-encryption / live device imaging. Accept clinic offline chart; iOS Keychain vs Android Keystore; Electron.

## Operate

Accept a detect/recover note that does not log bodies. Example: `logout_wipes_cache`.
