# Examiner notes — 10.2 (not learner-facing)

## Property

A dependency whose digest does not match the lockfile must not install. Integrity of build inputs is the cell — not “we have Dependabot.”

## Lab

- `labs/10.2/10.2-lab/vulnerable/` must fail: Dependency installed when digest mismatches lockfile
- `labs/10.2/10.2-lab/fixed/` must pass.
- Root cause: Name-only install.

## Seeded review intended findings

- install_ok True on hash mismatch
- Unpinned action
- Secrets in PR from forks
- SBOM generated but never used

## Transfer

Reject answers that name Top 10 / CWE Top 25 items as the definition of security.
Require a system-specific invariant, attacker, trust, residual risk.
Acceptable transfer direction: GitHub Actions third-party action@v1.

## Operate

Accept a detect/recover note that does not log bodies/secrets. Example signal: hash_mismatch_denied.
