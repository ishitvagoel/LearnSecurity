# Examiner notes — 2.1

Vulnerable `--impl` fails `test_duplicate_tenant_keys_are_one_meaning`. Fixed passes both tests.

Findings: first-key vs `json.loads` last-key; “parse success” as false assurance.
