# Examiner notes — 7.2 (not learner-facing)

## Property

A member must not resolve `secret_internal`. Identifiers locate; they do not authorize fields. ASVS anchors: `v5.0.0-8.2.3`, `v5.0.0-8.2.1`, `v5.0.0-8.2.2`; `v5.0.0-8.3.2` is **Level 3, advanced**. API1/API3/API5 are awareness after the cause (also 4.4). Distinct from 7.1 extra-key *writes*.

## Lab

- `labs/7.2/7.2-lab/vulnerable/` must fail: `test_member_cannot_resolve_internal_field`
- Honest-path `test_member_can_resolve_display_name` may pass on both
- `test_service_can_resolve_internal_field` may pass on both
- `labs/7.2/7.2-lab/fixed/` must pass all
- Root cause: resolver/serializer dumps without a role×field matrix

## Seeded review intended findings

- Resolve always true / ORM `__dict__` dump
- GraphQL expose all columns
- IDOR test only on object not field
- UUID as capability

## Transfer

Reject Top 10 / CWE / UUID-as-grant as the definition. Accept clinic SSN; bulk update; search snippets.

## Operate

Accept a detect/recover note that does not log the secret value. Example: `field_denied`.
