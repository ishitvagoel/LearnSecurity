# Examiner notes — 7.2 (not learner-facing)

## Property

A member must not resolve secret_internal. Function/property authorization is not “they can call GET /notes.” Identifiers locate; they do not authorize.

## Lab

- `labs/7.2/7.2-lab/vulnerable/` must fail: Member resolves secret_internal
- `labs/7.2/7.2-lab/fixed/` must pass.
- Root cause: Serializer dumps the ORM object.

## Seeded review intended findings

- return orm.__dict__
- GraphQL expose all columns
- IDOR test only on object not field
- UUID as “capability”

## Transfer

Reject answers that name Top 10 / CWE Top 25 items as the definition of security.
Require a system-specific invariant, attacker, trust, residual risk.
Acceptable transfer direction: Bulk update; search highlighting leaking snippets.

## Operate

Accept a detect/recover note that does not log bodies/secrets. Example signal: field_denied{field}.
