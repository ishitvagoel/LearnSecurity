# Examiner notes — 7.3 (not learner-facing)

## Property

A missing signature is rejected. Path hit and TLS are not provider-message authenticity. MAC is over raw body bytes. ASVS anchors: `v5.0.0-11.2.1`, `v5.0.0-2.3.4`, `v5.0.0-2.3.3`; `v5.0.0-4.1.5` is **Level 3, advanced**. API10 is awareness after the cause. HMAC is a teaching stand-in, not a vendor claim.

## Lab

- `labs/7.3/7.3-lab/vulnerable/` must fail: `test_missing_signature_is_rejected`
- Honest-path `test_matching_signature_is_accepted` may pass on both
- `test_wrong_signature_is_rejected` must fail on vulnerable, pass on fixed
- `labs/7.3/7.3-lab/fixed/` must pass all
- Root cause: callback trusted because it hit the path
- Secret `lab-secret` is disposable and local

## Seeded review intended findings

- Accept always true / process because path matched
- JSON parsed before MAC
- No missing-sig test
- Secret in query string (4.3)

## Transfer

Reject Top 10 / CWE / TLS-as-authenticity / live vendor attacks as the definition. Accept clinic lab-result webhook; signed redirects; outbound SSRF (6.5).

## Operate

Accept a detect/recover note that does not log bodies or secrets. Example: `webhook_sig_fail`.
