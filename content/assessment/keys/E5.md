# Examiner notes — E5 (not learner-facing)

## Property

A request body tenant:B must not switch the bound tenant A. Tenant is taken from the session/binding, not from the JSON body (1.3 confused deputy).

## Lab

- `labs/E5/e5-lab/vulnerable/` must fail: JSON body switches the bound tenant
- `labs/E5/e5-lab/fixed/` must pass.
- Root cause: Client-chosen tenant.

## Seeded review intended findings

- tenant from body
- RLS session var from JSON
- Cache key without tenant (2.2)
- Support impersonation silent

## Transfer

Reject answers that name Top 10 / CWE Top 25 items as the definition of security.
Require a system-specific invariant, attacker, trust, residual risk.
Acceptable transfer direction: Zanzibar tuple vs this binding.

## Operate

Accept a detect/recover note that does not log bodies/secrets. Example signal: body_tenant_mismatch.
