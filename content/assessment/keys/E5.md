# Examiner notes — E5 (not learner-facing)

## Property

A request body tenant B must not switch bound tenant A. Session binding is mediation. RLS/ReBAC are extra layers. ASVS `v5.0.0-8.2.1` / `v5.0.0-8.2.2`; `v5.0.0-15.3.3` related; `v5.0.0-8.3.2` labeled Level 3 advanced. API1 is awareness after the cause. Do not mark Gate 7 or M2 complete.

## Lab

- `labs/E5/e5-lab/vulnerable/` must fail: `test_body_cannot_switch_tenant`
- Honest-path `test_matching_body_may_keep_session_tenant` may pass on both
- `labs/E5/e5-lab/fixed/` must pass all
- Root cause: client-chosen tenant treated as binding

## Seeded review intended findings

- Tenant taken from the body
- RLS session var set from JSON
- Cache key without tenant
- Support impersonation silent

## Transfer

Reject Top 10 / CWE / RLS-as-1.2 / live tenant / “Gate 7 complete.” Accept clinic `org_id` in JSON; Zanzibar tuple vs this binding.

## Operate

Accept a detect/recover note that does not log note bodies. Example: `body_tenant_mismatch`.
