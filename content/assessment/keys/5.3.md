# Examiner notes — 5.3 (not learner-facing)

## Property

A disposable lab API key that is a hardcoded default must not authenticate after rotation. The old value fails. Inventory + rotation is the property, not “we have a secrets manager” as a sticker.

## Lab

- `labs/5.3/5.3-lab/vulnerable/` must fail: Hardcoded default API key still authenticates after rotation
- `labs/5.3/5.3-lab/fixed/` must pass.
- Root cause: Default credential never invalidated.

## Seeded review intended findings

- DEFAULT = 'sk-lab-hardcoded' still accepted
- Secret in README “for convenience”
- No rotation test
- Same key for all tenants

## Transfer

Reject answers that name Top 10 / CWE Top 25 items as the definition of security.
Require a system-specific invariant, attacker, trust, residual risk.
Acceptable transfer direction: Envelope encryption DEK vs KEK; compromise runbook.

## Operate

Accept a detect/recover note that does not log bodies/secrets. Example signal: auth_default_denied; image_rebuild after rotate.
