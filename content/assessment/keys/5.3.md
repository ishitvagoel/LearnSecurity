# Examiner notes — 5.3 (not learner-facing)

## Property

A hardcoded default must not authenticate after rotation. Inventory + rotation is the property, not a vault sticker. ASVS anchors: `v5.0.0-13.3.1`, `v5.0.0-13.2.3`, `v5.0.0-11.1.1`; `v5.0.0-13.3.3` and `v5.0.0-13.3.4` are **Level 3, advanced**. Lab string `sk-lab-hardcoded` is disposable.

## Lab

- `labs/5.3/5.3-lab/vulnerable/` must fail: `test_hardcoded_default_does_not_auth`, `test_missing_current_denies`
- Honest-path `test_current_secret_authenticates` may pass on both
- `labs/5.3/5.3-lab/fixed/` must pass all
- Root cause: default credential never invalidated

## Seeded review intended findings

- DEFAULT = 'sk-lab-hardcoded' still accepted
- Secret in README “for convenience”
- No rotation test
- Same key for all tenants

## Transfer

Reject Top 10 / CWE as the definition. Accept clinic gist leak; envelope DEK vs KEK sketch.

## Operate

Accept a detect/recover note that does not log secret values. Example: `default_secret_used` with secret_id only.
