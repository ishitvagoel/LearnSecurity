# Examiner notes — 8.3 (not learner-facing)

## Property

A deep-link `as=` parameter must not switch the signed-in principal. The Intent/query is untrusted input. MASVS 2.1.0 `MASVS-PLATFORM-1` / `PLATFORM-2` / `AUTH-1` / `NETWORK-1`. RFC 8252 claimed HTTPS redirects. ASVS `v5.0.0-8.3.1`. App Links verification is not identity.

## Lab

- `labs/8.3/8.3-lab/vulnerable/` must fail: `test_deeplink_as_param_does_not_switch_user`
- Honest-path `test_note_deep_link_keeps_session` may pass on both
- `labs/8.3/8.3-lab/fixed/` must pass all
- Root cause: identity taken from the link

## Seeded review intended findings

- `current_user = extras['as']`
- Exported Activity without permission
- WebView `addJavascriptInterface` too wide
- No `as=` test

## Transfer

Reject Top 10 / CWE / App-Links-as-identity / live malware APKs. Accept clinic `as=doctor`; OAuth native redirect (4.5).

## Operate

Accept a detect/recover note that does not log full URLs or tokens. Example: `deeplink_identity_ignored`.
