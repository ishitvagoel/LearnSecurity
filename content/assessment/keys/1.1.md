# Examiner notes — 1.1 (not learner-facing)

## Lab

- `vulnerable/security_claim.yaml` must fail `test_claim_is_property_shaped` (missing property-shaped fields).
- `fixed/security_claim.yaml` must pass.

## Seeded SECURITY.md intended findings

1. “Passwords are hashed (bcrypt)” — mechanism, false assurance if used as whole-app security.
2. “We use TLS” — mechanism; does not state confidentiality of notes at rest or in logs.
3. “The scanner is green” — not a property; not evidence of an invariant.
4. Missing: tenant-specific invariants, attacker, trust, time horizon.

## Transfer (LO-07)

Reject answers that name Top 10 items as the definition of security. Require at least five system-specific invariants for the clinic booking sketch.
