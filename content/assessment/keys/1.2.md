# Examiner notes — 1.2 (not learner-facing)

## Lab

From `labs/1.2/1.2-authority-matrix/`:

```text
python3 -m pytest tests/test_mediation.py --impl vulnerable
```

Must **fail** `test_cross_tenant_read_is_denied` (bob reads n1).

```text
python3 -m pytest tests/test_mediation.py --impl fixed
```

Must **pass** all tests.

## Seeded review intended findings

1. `read_note` uses `is_authenticated` as if it were an object ACL — mechanism / false assurance.
2. No tenant (subject–object) predicate — complete-mediation miss.
3. “Logged in” in SECURITY.md treated as authorization.
4. Missing: fail-safe on unknown notes as an explicit policy (fixed tree has it).

## Transfer (LO-07)

Reject answers that name a Top 10 item as the definition of authorization. Require a new subject, delegation bounds, and at least one stressed Saltzer principle.
