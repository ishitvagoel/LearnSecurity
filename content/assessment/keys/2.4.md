# Examiner notes — 2.4 (not learner-facing)

## Property

A retried share with the same idempotency key must not create a second share. Timeouts are a security property (integrity of the share graph), not only UX.

## Lab

- `labs/2.4/2.4-state-time/vulnerable/` must fail: Retry creates a second share grant
- `labs/2.4/2.4-state-time/fixed/` must pass.
- Root cause: Non-idempotent side effect + retry = extra grant.

## Seeded review intended findings

- INSERT share on every POST
- Idempotency key in a log comment only
- Test only happy-path single click
- Fail-open on idempotency store timeout

## Transfer

Reject answers that name Top 10 / CWE Top 25 items as the definition of security.
Require a system-specific invariant, attacker, trust, residual risk.
Acceptable transfer direction: Payment capture (E3) and invite tokens (6.6) are the same shape.

## Operate

Accept a detect/recover note that does not log bodies/secrets. Example signal: share_count vs unique keys; never fail-open if the key store is down.
