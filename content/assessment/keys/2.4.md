# Examiner notes — 2.4 (not learner-facing)

## Property

A retried share with the same idempotency key must not create a second share. Timeouts are integrity of the share graph, not only UX. RFC 9110 POST is not idempotent. ASVS `v5.0.0-2.3.3` (transactions) and `v5.0.0-2.3.4` (limited-quantity locking, clinic transfer). Top 10:2025 A10 is awareness only.

## Lab

- `labs/2.4/2.4-state-time/vulnerable/` must fail `test_retry_does_not_duplicate_side_effect`
- `labs/2.4/2.4-state-time/fixed/` must pass that test and `test_single_share`
- Root cause: non-idempotent side effect plus retry

## Seeded review intended findings

- INSERT share on every POST
- Idempotency key in a log comment only
- Test only happy-path single click
- Fail-open on idempotency store timeout

## Transfer

Reject Top 10 / CWE as the definition of security.
Acceptable direction: clinic last slot; payment capture (E3); invite tokens (6.6); worker stale grant (7.4).
WCAG 4.1.3 status messages must reuse the same key if they retrigger work.

## Operate

Accept detect/recover that does not log bodies. Example: replay metric plus note/key ids; never fail-open if the key store is down.
Reject “HTTP 201 means once.”
